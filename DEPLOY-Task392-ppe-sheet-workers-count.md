# DEPLOY — Task 392: строка «без скобок» + раздел «СИЗ» (лист табель_КИП_ИОС)

**Дата:** 2026-09-22
**Репозиторий:** kip8test (тестовый PWA)
**SW:** kipia-test-v619 → **kipia-test-v620**
**Заявка:** «"Работников на текущий момент (12) (2 мастера, 5 дневных, 5 сменных)." — 12 без скобок. Нужно
добавить новые данные каждому работнику, в файл табель_КИП_ИОС добавить новый лист "СИЗ" с таблицей данных
по средствам индивидуальной защиты (СИЗ), пример таблицы и перечень СИЗ приложены в файле, должна быть
возможность редактирования данных из приложения в личных данных каждого работника и добавление и удаление СИЗ.»

---

## ЧТО СДЕЛАНО (клиент — работает сразу после пуша)

### 1. Строка «Общей» вкладки «Работники» — число БЕЗ СКОБОК
- Было: «Работников на текущий момент **(12)** (2 мастера, 5 дневных, 5 сменных).»
- Стало: «Работников на текущий момент **12** (2 мастера, 5 дневных, 5 сменных).»
  (ведущее число = автосумма категорий; разбивка остаётся в скобках — Task 391).

### 2. Раздел «СИЗ» — средства индивидуальной защиты
Новый лист **«СИЗ»** в таблице **табель_КИП_ИОС** (та же Google-таблица, что «Сотрудники»/«Отпуска»).
Структура — по образцу файла «Таблица СИЗ работникам КИП ИОС.xlsx» (Приказ Минтруда России
от 29.10.2021 N767н):

| A | B | C | D | E | F | G | H | I |
|---|---|---|---|---|---|---|---|---|
| id | таб_номер | работник | должность | наименование_СИЗ | дата_выдачи | срок_годности | дата_окончания | примечание |
| авто | FK, текст | копия для листа | копия | «Костюм…», «Ботинки»… | Date/пусто | «1 год»/«1,5 года»/«2 года»/«3 года»/«6 мес.»/«До износа» | **АВТО: выдача + срок** | «До износа»… |

- **Карточка работника** (страница «Работники» → вкладка работника) — новая секция
  «СИЗ · средства индивидуальной защиты»: записи с мета-строкой
  («выдано 17.08.2026 · срок 1 год · до 17.08.2027 · «примечание»»), кнопки
  **✎ (правка)** и **✕ (удаление)** у каждой записи с id, строка **«+ СИЗ…»**.
- **Шторка «Новое СИЗ» / «Правка СИЗ»**: работник (префилл из карточки),
  наименование (datalist со стандартными 8 позициями образца + свободный ввод),
  дата выдачи, срок годности («— / 6 мес. / 1 год / 1,5 года / 2 года / 3 года / До износа»),
  примечание. **Дата окончания считается автоматически** (дата выдачи + срок, кламп
  к длине месяца: 31.08 + 6 мес → 28.02) и показывается строкой под полями.
- **Удаление** — с подтверждением (kipConfirm). Зрители видят записи БЕЗ кнопок.
- До серверного деплоя раздел не ломает приложение: список СИЗ пуст («нет выданных СИЗ»),
  ошибки добавления показываются тостом.
- Кэш Task 314 хранит СИЗ (поле `ppe`) — офлайн-открытие работает.

---

## ⚠️ СЕРВЕРНЫЕ ШАГИ — ВЫПОЛНИТЬ ВРУЧНУЮ В APPS SCRIPT
(ОДИН бэкенд Apps Script на kip8 и kip8test)

Новые эндпоинты `listPpe/addPpe/updatePpe/deletePpe` требуют обновления Apps Script.
До обновления кнопки «Добавить»/«Сохранить»/«✕» в разделе СИЗ вернут ошибку
`Unknown action: workSchedule.listPpe/addPpe/updatePpe/deletePpe` в тосте —
приложение продолжит работать (секция покажет «нет выданных СИЗ»).

1. Открыть редактор Apps Script (проект развёртывания AKfycbyt… — тот же,
   где `Code.gs`, `WorkSchedule.gs`).
2. **`Code.gs`** — в `doPost` после `case 'workSchedule.updateVacation':` добавить
   4 case (копия из репозитория `scripts/Code.gs`):
   ```js
   case 'workSchedule.listPpe':
     return _json(WorkSchedule.listPpe(payload));

   case 'workSchedule.addPpe':
     return _json(WorkSchedule.addPpe(payload));

   case 'workSchedule.updatePpe':
     return _json(WorkSchedule.updatePpe(payload));

   case 'workSchedule.deletePpe':
     return _json(WorkSchedule.deletePpe(payload));
   ```
3. **`WorkSchedule.gs`** — заменить целиком на версию из репозитория
   `scripts/WorkSchedule.gs` (добавлены: константа `PPE_SHEET`, хелперы
   `_ppeLookupEmployee`/`_ppeTermMonths`/`_ppeExpiry`, методы
   `listPpe`/`addPpe`/`updatePpe`/`deletePpe` — после `deleteVacation`;
   остальное без изменений).
4. **Создать лист «СИЗ» и предзаполнить перечень** — новый файл
   **`PPEInit.gs`** (копия из репозитория `scripts/PPEInit.gs`):
   + → «PPEInit.gs» → вставить → выбрать `ppeDeployAll` → ▶ Run (один раз).
   Создаст лист «СИЗ» с заголовками и добавит каждому АКТИВНОМУ работнику
   стандартный перечень из 8 позиций образца (даты выдачи пустые):
   костюм (1 год), ботинки (1,5 года), бельё нательное, куртка утеплённая
   (2 года, «До износа»), каска защитная (2 года, «До износа»), подшлемник,
   противогаз, очки закрытые («До износа»). Повторный запуск безопасен
   (работников с записями пропускает). Диагностика — `ppeStatus`.
5. Нажать **«Сохранить»**, затем **«Выполнить» → «Новое развёртывание»**
   (или Управление развёртываниями → Изменить → Новая версия) — тип
   «Веб-приложение», доступ «Любой», выполнение «от моего имени».
6. Проверка: «Работники» → вкладка работника → секция «СИЗ» с 8 записями →
   «+ СИЗ…» → «Добавить» → тост «СИЗ добавлено работнику», запись в карточке.

### Семантика новых эндпоинтов (для аудита)
- `listPpe {token}` — гейт `workschedule.view` (как listVacations); возвращает все
  записи листа «СИЗ» (даты ISO; «До износа» — строкой; пустые строки пропускаются).
- `addPpe {таб_номер, наименование, дата_выдачи, срок_годности, примечание}` —
  гейт `workschedule.edit`; ищет работника в «Сотрудниках» (колонки-копии C/D
  заполняются автоматически); **дата_окончания (H) считается на сервере**
  (выдача + срок; «До износа» → текст; без даты — пусто); id = max+1;
  B — текстовый формат (Task 304); аудит `WORKSCHEDULE_ADD_PPE`.
- `updatePpe {id, …}` — обновляет B..I по id (копии C/D освежаются из справочника,
  дата окончания пересчитывается); аудит `WORKSCHEDULE_UPDATE_PPE`.
- `deletePpe {id}` — удаляет строку по id; аудит `WORKSCHEDULE_DELETE_PPE`.

---

## ПРОВЕРКИ

- Тесты: `node tests/run-all.js` → **3597 passed / 0 failed**
  (было 3550/0; +47 `tests/test-task392.js`: SRC (строка без скобок, шторка HTML/
  datalist/опции срока, секция карточки, CSS, загрузчик/кэш, сервер, Code.gs,
  PPEInit.gs, SW v620) + VM-клиент (формат строки, карточка с записями/кнопками/
  зрителем, сроки и авто-дата с клампом месяца, режимы шторки, submit add/update,
  валидации, удаление) + VM-сервер (мок-листы: listPpe ISO/текст/«До износа»,
  addPpe копии C/D + авто-дата + валидации, updatePpe, deletePpe);
  адаптации: test-task391/390/389 (формат «N (…)»), test-task388 (×2),
  test-tab-numbers (_appendRowKeepText 4→5), test-task314 (loadGrid(true) 15→18).
- Браузер: `python3 scripts/task392-browser-check.py` (порт 9006) →
  **28/28**: строка «…момент 6 (2 мастера…)» без скобок (старого формата нет);
  карточка: секция СИЗ, записи с мета-строкой, 4 кнопки ✎/✕, «+ СИЗ…»;
  шторка: режимы/префилл/datalist 8/опции 7/авто-дата «17.08.2027
  (заполнится автоматически)» и «До износа»; добавление (addPpe с payload,
  тост, запись в карточке), правка (updatePpe с id), удаление (kipConfirm →
  deletePpe → запись исчезла); мобайл 375: секция + шторка в границах;
  0 JS-ошибок ×2.
- VLM-пруфы: `task392-proof-card-siz.png` (карточка с секцией СИЗ и кнопками),
  `task392-proof-ppe-sheet.png` (шторка «Новое СИЗ», все поля),
  `task392-proof-mobile-sheet.png` (мобайл, шторка в границах),
  `task392-proof-confirm.png` (подтверждение удаления).

## ПЕРЕНОС В БОЕВОЙ kip8 (после приёмки)

| Файл | Действие |
|------|----------|
| `index.html` | Скопировать (скриптом переноса; `isolateLocalStorage()` удаляется автоматически) |
| `sw.js` | kipia-test-v620 → kipia-v468 (заменить суффиксы) |
| `tests/` | Скопировать (test-task392.js + адаптации + бамп) |
| `scripts/*.gs` | Справочные копии — синхронизировать (WorkSchedule.gs, Code.gs, **PPEInit.gs** — новый) |
| Сервер Apps Script | Один деплой на оба сайта (см. раздел выше — если ещё не сделан) |
