# Task 405 — разделение таблицы инструктажей («Инструктажи» + «Мероприятия»); блок «Повторные инструктажи и периодическая проверка знаний» в картах

**Заявка**: «В файле табель_КИП_ИОС необходимо таблицу инструктажи
разделить на две таблицы. В текущей таблице оставить только
информацию которая касается проведения инструктажей и проверки
заний, а остальные данные, обучение и примечание, нужно переместить
в новую таблицу под названием "Мероприятия". Тоесть в личных картах
работников блок мероприятия остаётся и в нём отображаются только
данные из новой таблицы "Мероприятия", а данные из таблицы
"Инструктажи" отображать в новом блоке под названием "Повторные
инструктажи и периодическая проверка знаний" на текущий год».

**Репозиторий**: kip8test (тестовый сайт)
<https://bloknett-design.github.io/kip8test/>

**Версия SW**: kipia-test-v631 → **v632**
**Тесты**: 3845 passed / 0 failed (было 3817/0; +29 в
test-task405.js, адаптация 13 файлов: work-events / tab-numbers /
306 / 384 / 385 / 388 / 393 / 394 / 395 / 396 / 403 / 404 / 405)
**Браузер-чек**: 38/38 ×3 контекста (тёмная view / светлая edit /
мобайл 375), 0 JS-ошибок; VLM ×3 (glm-5v-turbo) — подтверждено
визуально (карточка 3 колонки, стек мобайл, зум-кроп блока).

## Что изменилось

### 1 — СЕРВЕР (WorkSchedule.gs): лист «Мероприятия»

- **`EVENTS_SHEET: 'Мероприятия'`** — вторая половина разделённой
  таблицы; формат столбцов A..H — как у «Инструктажей» (id,
  таб_номер, тип, тема, дата_начала, дата_окончания,
  длительность_дней, комментарий).
- **`TRAINING_EVENTSHEET_TYPES`** — типы, живущие в новом листе:
  обучение / прогул / примечание. Всё остальное (инструктаж,
  проверка_знаний И неизвестные типы) остаётся в «Инструктажах».
- **`listTrainings`** — ОБЪЕНДИНЁННЫЙ список ОБОИХ листов
  (`_readTrainingsSheet` — общее чтение одного листа; фильтр года/
  месяца как прежде): бейджи ячеек, окно мероприятий, печать и
  генерация месяца видят все мероприятия ВМЕСТЕ, нумерация id
  сквозная — фронтенд не меняется до переноса данных.
- **`addTraining`** — маршрутизация по ТИПУ
  (`_trainingsSheetForType`): инструктаж/проверка_знаний → лист
  «Инструктажи»; обучение/прогул/примечание → лист «Мероприятия»
  (автосоздание `_ensureEventsSheet`: заголовки — копия строки 1
  листа «Инструктажи», если заполнена, иначе канонические; жирный
  шрифт, тёмная шапка, закреплённая строка 1; идемпотентно).
- **id — СКВОЗНОЙ по обоим листам** (`_maxTrainingsId` по каждому,
  максимум + 1): связки Записи_графика.инструкция и правка/удаление
  по id на клиенте однозначны и после разделения.
- **`deleteTraining`** — id ищется в ОБОИХ листах (лист неизвестен
  клиенту).
- **`splitTrainingsSheet`** (+ топ-левел `trainingsSplitInit`) —
  РАЗОВЫЙ перенос строк обучение/прогул/примечание из «Инструктажей»
  в «Мероприятия»: строки переносятся ЦЕЛИКОМ (id и значения
  сохраняются — связки не ломаются; таб_номер текстом, Task 304),
  удаление перенесённых — снизу вверх; идемпотентно (повторный
  запуск — moved: 0); НЕИЗВЕСТНЫЕ типы остаются в «Инструктажах»
  (переносятся только явные «мероприятийные»). Через API приложения
  НЕ доступен — запуск вручную из редактора Apps Script.

### 2 — КАРТОЧКА: блоки по типу записи

- **`_isInstrType`** — «инструктажный» тип (инструктаж /
  проверка_знаний; толерантно к регистру/пробелам: «Проверка знаний»
  → «проверка_знаний»). Всё остальное — «мероприятийный» тип.
- **Блок «Мероприятия · год»** (b3) — теперь только
  обучение/примечание/прогул (данные НОВОЙ таблицы «Мероприятия»).
- **НОВЫЙ блок b5 «Повторные инструктажи и периодическая проверка
  знаний · год»** — инструктаж/проверка_знаний (данные таблицы
  «Инструктажи», текущий год шахматки): строки формата мероприятий
  (код И/ПЗ + тема + период, ✎/✕ редакторам по id) + кнопка
  «+ Инструктаж…» в шапке (`onEmpAddInstruction`); длинный заголовок
  — с классом `.ws-whead-wrap` (перенос строк в шапке).
- **`_renderWorkerCard`** возвращает `[b1, b2, b3, b4, b5]` (asBlocks,
  страница «Работники») или `b1 + b2 + b3 + b5` (попап шахматки —
  БЕЗ СИЗ b4, Task 403; НОВОЕ: + секция повторных инструктажей).
- **`_renderWorkerCardPanels`**: колонка 3 (.ws-wcol-tr) — ДВА окна:
  мероприятия, под ним — повторные инструктажи (colTr). Три колонки
  в одну линию — как Task 404; мобайл — стек из ПЯТИ блоков
  (профиль → отпуска → СИЗ → мероприятия → инструктажи).

### 3 — «Общая» сводка и форма

- **Сводная «Общая»**: колонка «Мероприятия · год» — только
  не-инструктажи; НОВАЯ колонка **«Инструктажи · год»** правее.
- **`openTrainingForm`** — 4-й аргумент **prefillType**: тип по
  умолчанию из кнопки входа («+ Мероприятие…» → обучение,
  «+ Инструктаж…» → инструктаж; без указания — прежний дефолт
  «инструктаж»). `onEmpAddTraining` зовёт с 'обучение'.

## Проверки

- `tests/test-task405.js` (+29): SRC (сервер: EVENTS_SHEET/типы/
  маршрутизация/сквозной id/deleteTraining оба листа/split;
  карточка: b3 только НЕ-инструктажи, b5 заголовок/кнопка/строки,
  попап b1+b2+b3+b5; сводная 2 колонки; openTrainingForm 4-й
  аргумент; CSS .ws-whead-wrap) + VM (карточка/панели/сводка;
  серверные моки листов: объединение, маршрутизация, перенос,
  идемпотентность) + SW (v632).
- Адаптация: test-work-events (сигнатура openTrainingForm),
  test-tab-numbers (_appendRowKeepText 5→6), test-task306 (G:
  маршрутизация в лист «Мероприятия», мок insertSheet),
  test-task384 (onEmpAddTraining → 'обучение'), test-task385/388
  (хосты +_isInstrType; сводная 2 колонки), test-task393/394/395/
  396/403/404 (хосты +_isInstrType; блоки/окна 4→5; кнопки 5→6;
  легаси-строки 5→6; зебра b5; сводная; колонка 3 — 2 окна; ✎/✕
  обучение в b3 + инструктаж в b5; новый тест блока 5).
- Браузер: попап ячейки — разделение по типам (C4–C11: в
  «Мероприятиях» обучение+примечание, НЕТ инструктажа/ПЗ; в
  «Инструктажах» инструктаж+ПЗ, НЕТ обучения/примечания; «Проверка
  знаний» с пробелом — тоже инструктажный); карточка — 3 колонки,
  колонка 3 = 2 окна; «+ Мероприятие…» → wsTrType=обучение;
  «+ Инструктаж…» → wsTrType=инструктаж; сводная — 2/2 у Иванова,
  прочерки у Петрова; мобайл — стек 5 блоков.
- VLM ×3 (glm-5v-turbo): колонки/заголовки/записи/кнопки
  подтверждены; мобайл-зум-кроп блока «Повторные инструктажи и
  периодическая проверка знаний · 2026» (записи И Охрана труда,
  Экзамен).

## ⚠ СЕРВЕРНЫЕ ШАГИ (пользователь, после деплоя)

1. **Обновить WorkSchedule.gs** в проекте Apps Script таблицы
   табель_КИП_ИОС (замена всего кода файла на версию из репозитория
   — scripts/WorkSchedule.gs).
2. **Разовый перенос данных**: в редакторе Apps Script выбрать
   функцию **`trainingsSplitInit`** → ▶ Run (первый запуск может
   попросить авторизацию — разрешить). Результат — в журнале
   (Ctrl+Enter): `{"ok":true,"moved":N,"sheet":"Мероприятия"}`.
   Переносит строки обучение/прогул/примечание из листа
   «Инструктажи» в лист «Мероприятия» (id сохраняются). Повторный
   запуск безопасен (moved: 0). После переноса функцию больше
   запускать не нужно.

До шага 2 карточки уже делят записи ПО ТИПУ (данные читаются
объединённым списком) — приложение остаётся рабочим; шаг 2 наводит
порядок в самой таблице.

## Деплой

- Страницы GitHub (CI соберёт и опубликует автоматически после
  push в main).
- Обновление на клиентах: Ctrl+Shift+R ×1–2 (SW v632).
- Apps Script и листы табель_КИП_ИОС — см. серверные шаги выше.
