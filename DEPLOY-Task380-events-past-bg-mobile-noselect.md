# DEPLOY — Task 380 (kip8test)

## Заявки пользователя

1. «В окне «Мероприятия» в баре, цвет фона в окне где расположен текст
   мероприятий дата которых прошла, должен быть светлее, а дата которых ещё
   не наступила или не прошла — фон в окне темнее.»
2. «В мобильной версии, при нажатии с задержкой на ячейки шахматки и с
   фамилиями и ячейки итогов учёта, не должен выделяться текст в ячейках для
   копирования, поделиться, поиска в гугл и остальные манипуляции с
   выделенным текстом (пример на скрине).»

## Что изменилось (только клиент, index.html — серверной части НЕТ)

### 1. Окно «Мероприятия» бара — фон строк ПО СРОКУ

Строка мероприятия (.ws-ep-item) получает фоновую «плашку», цвет которой
зависит от срока:

| Срок | Класс | Тёмная тема | Светлая тема |
|---|---|---|---|
| ПРОШЛО (дата_окончания строго < «сегодня») | `.ws-ep-past` | rgba(255,255,255,0.12) ≈ rgb(43,50,60) — **СВЕТЛЕЕ** окна #0e1621 | rgba(255,255,255,0.55) ≈ rgb(245,244,240) — **СВЕТЛЕЕ** окна #e9e7de |
| идёт сегодня / ещё не наступило | база `.ws-ep-item` | rgba(0,0,0,0.45) ≈ rgb(7,12,18) — **ТЕМНЕЕ** окна | rgba(0,0,0,0.12) ≈ rgb(204,203,195) — **ТЕМНЕЕ** окна |

- Граница «сегодня» — `var todayIso = this._isoDate(new Date())` в
  `_renderMonthEventsPanel` (тот же приём, что маркер колонки «сегодня»);
  лексикографическое сравнение ISO-строк. Однодневные без «дата_окончания»
  сравниваются по «дата_начала» (как в фильтре месяца).
- Плашка: `padding: 2px 6px; margin: 0 -6px;` — во всю ширину окна, текст
  не липнет к краю.
- Порядок CSS-правил (каскад специфичностей 0,1,0 → 0,2,0 → 0,2,0 → 0,3,0):
  база → .ws-ep-past → [light] база → [light] .ws-ep-past — светлые правила
  ПОСЛЕ базовых, иначе [light] .ws-ep-item (0,2,0) перебил бы тёмный
  .ws-ep-past.
- Контраст past↔upcoming ≈ 36–44 ед. RGB в обеих темах (первая итерация
  0.32/0.07 была ≈ 22–26 — усилено по пиксельному замеру).
- Точка-цвет кода, дата, текст, заголовок окна — НЕ тронуты.

### 2. Мобайл — запрет выделения текста в шахматке и итогах

```css
@media (max-width: 1023px) {
    .ws-grid,
    .ws-tt-table {
        -webkit-user-select: none; -moz-user-select: none;
        -ms-user-select: none; user-select: none;
        -webkit-touch-callout: none;
    }
}
```

- Действует на ВСЕ ячейки: значения шахматки, ФИО, шапку сетки, таблицы
  итогов (месяц/год/архив). Долгое нажатие больше НЕ выделяет текст —
  системный попап «Копировать / Поделиться / Поиск в Google» (пример на
  скрине пользователя) не появляется.
- Breakpoint ≤1023px — тот же, что мобильная компоновка приложения (окна
  бара за чипами). ДЕСКТОП НЕ ТРОНУТ: выделение мышью работает как прежде
  (проверено computed user-select ≠ none на 1280px + программная Selection
  на мобиле пуста).
- Окна бара и попапы НЕ тронуты (не входят в .ws-grid/.ws-tt-table).

## Сборка / версии

- SW: kipia-test-v608 → **kipia-test-v609** (guard v610), 68 тест-файлов
  бампнуто.
- Тесты: **3246 passed / 0 failed** (+20 test-task380.js: CSS фоны/порядок
  каскада/плашка, SRC todayIso+eIso+класс, VM на динамических датах
  текущего месяца с инвариантами «весь месяц» и «последний день» НИКОГДА
  не прошедшие; адаптации 315/316 — заглушка _isoDate в 4 VM-хостах).
- Браузер-проверка: **30/30** (порт 8988; десктоп dark+light: классы
  past = расчёту, computed фоны точные, яркость past > окна > upcoming
  blend-расчётом, плашка padding/margin, десктоп-выделение живо; мобайл
  375: user-select:none сетка/ФИО/значения/шапка/итоги, программное
  выделение ПУСТО; 0 JS-ошибок).
- Пиксельные пруфы: тёмная — полосы строк rgb(43,50,60)×2 (past) и
  rgb(7,12,18)×3 (upcoming) на окне rgb(14,22,33); светлая —
  rgb(245,244,240)×2 и rgb(204,203,195)×3 на окне rgb(233,231,222).
- VLM: бинарное сравнение чистых фоновых полос — «верхняя (прошедшая)
  светлее» в ОБОИХ темах ✓ (полноразмерные скриншоты модель оценивает
  нестабильно — авторитетны пиксельные замеры); мобайл с открытой шторкой
  итогов — сетка/шторка целы, наложений нет.

## Серверных действий НЕТ

Чистый клиентский патч (CSS + один метод рендера окна). Apps Script,
FlowmeterArchive.gs и прочие .gs НЕ менялись.

## Развёртывание

1. `git pull` на боевом kip8 (перенос — отдельным коммитом Task 380-transfer).
2. Пользователям: обновить страницу 2 раза (смена SW v608→v609).
