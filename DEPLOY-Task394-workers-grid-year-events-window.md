# DEPLOY — Task 394: сетка 2×2 карточки + мероприятия за год + окно мероприятий (отпуска/СИЗ)

**Дата:** 2026-09-22
**Репозиторий:** kip8test (тестовый PWA)
**SW:** kipia-test-v621 → **kipia-test-v622**
**Заявка:** «В десктопном представлении, блок отпусков переместить справа от блока профиля,
а блок СИЗ разместить под ним, все четыре блока разместить равномерно по горизонтали на весь
экран. В картах работников мероприятия указывать на весь год. В табеле учёта рабочего времени,
в окне мероприятий на текущий месяц так же указывать информацию по СИЗ на текущий месяц, и по
отпускам (если отпуск попадает частично на два месяца, то информация по нему должна отображаться
в окне мероприятий обоих месяцев).»

**Контекст:** серверный деплой Task 392 (Code.gs + WorkSchedule.gs + PPEInit.gs → ppeDeployAll →
«Новая версия») ВЫПОЛНЕН пользователем — раздел «СИЗ» работает в бою; открытых серверных шагов нет.

---

## ЧТО СДЕЛАНО (клиент — работает сразу после пуша)

### 1. Десктоп: четыре блока карточки — СЕТКА 2×2 НА ВЕСЬ ЭКРАН
На странице «Работники» (≥1024px) вкладка работника — обёртка `.ws-wgrid2`
(`_renderWorkersPage`): `grid-template-columns: 1fr 1fr` (РАВНЫЕ колонки),
`gap: 12px`, `align-items: start` (окна не тянутся по высоте чужих строк):

```
[ Профиль + «Правка данных…»/«Уволить…» ] [ Отпуска · 2026 ]
[ Мероприятия · 2026 ]                    [ СИЗ ]
```

- **Отпуска — СПРАВА от профиля** (ряд 1), **СИЗ — ПОД отпусками** (ряд 2, правая
  колонка), мероприятия — под профилем (ряд 2, левая колонка);
- кап ширины страницы 1020px (Task 388) СНЯТ — блоки идут на весь экран;
- «Общая» вкладка — без сетки (сводная таблица, просто шире);
- мобильный (≤1023px) — прежний вертикальный стек (обёртка без правил).

### 2. В КАРТАХ работников мероприятия — НА ВЕСЬ ГОД
- `_loadTrainings` теперь запрашивает `{ year }` БЕЗ месяца — сервер `listTrainings`
  отдаёт все записи, ПЕРЕСЕКАЮЩИЕ год (исходная способность сервера, Apps Script
  не менялся);
- блок 3 карточки: заголовок **«Мероприятия · 2026»** (было «· Сентябрь 2026»),
  записи всего года по хронологии; пустое состояние — «нет мероприятий за год»;
- попап карточки у сетки — та же секция года (общий рендер `_renderWorkerCard`);
- колонка «Общей» вкладки — **«Мероприятия · 2026»**, счётчик — годовой;
- сверка пересечения с годом на клиенте — защита от устаревшей локальной копии
  (кэш Task 314 прежних версий лежал месячным срезом; после «Обновить»
  перезапишется годовым списком).

### 3. Окно мероприятий месяца (тулбар табеля): + ОТПУСКА и + СИЗ
После секции мероприятий (как прежде) — две НОВЫЕ секции (пустые — скрыты):

- **«Отпуска · сентябрь 2026 · N»** — периоды листа «Отпуска», ПЕРЕСЕКАЮЩИЕ месяц;
  **частично попадающие на два месяца показываются в окнах ОБЕИХ месяцев**
  (29.09–02.10 виден и в сентябре, и в октябре); строки «точка · диапазон ·
  Отпуск · ФИО»;
- **«СИЗ · сентябрь 2026 · N»** — записи листа «СИЗ», у которых срок годности
  ИСТЕКАЕТ в этом месяце (дата_окончания внутри месяца): «точка · до 17.09 ·
  СИЗ · наименование · ФИО»; «До износа»/без даты — не показываются;
- режим выбранного дня (Task 316): отпуска — только накрывающие день, СИЗ —
  истекающие ровно в выбранный день;
- ПРОШЕДШИЕ строки (дата раньше «сегодня») — прозрачный фон `.ws-ep-past`
  (правила Task 380/389 сохранены);
- плашки секций — свои цвета (мероприятия — зелёная, отпуска — серо-синяя,
  СИЗ — янтарная); в светлой теме текст плашек чёрный (Task 330).

**Печать графика не менялась:** её секция мероприятий по-прежнему про месяц
шахматки (фильтр по датам работает с годовым списком так же).

---

## СЕРВЕРНЫЕ ШАГИ — НЕТ
Изменения чисто клиентские (index.html). Apps Script не трогался: `listTrainings`
без `month` умеет отдавать весь год С САМОГО НАЧАЛА («если month не указан — все
мероприятия года» — исходный код, задеплоенный вами Task 392). «Новая версия» НЕ
требуется.

---

## ПРОВЕРКИ
- Тесты: **3648 passed / 0 failed** (было 3621; +27 — test-task394.js: SRC
  загрузка-года/карточка/сводка/обёртка/CSS-сетка/окно-секций, VM карточка-год
  (вне года скрыты, хронология), VM страница (обёртка+порядок, «Общая»-год),
  VM окно: отпуск через границу В ОБОИХ месяцах, «До износа» скрыто, день,
  прошлые, порядок секций; SW);
- Адаптация 4 правок в 2 тестах (393/309): заголовок «· 2026», «за год»;
- Браузер-чек **25/25** (порт 9006, десктоп тёмная 1280 + мобайл 375 светлая):
  окно: 3 секции месяца, отпуск 29.09–02.10 — в окнах СЕНТЯБРЯ и ОКТЯБРЯ, СИЗ
  «до 12.09»/«до 05.10» по месяцам, день 12 (накрывающий отпуск + СИЗ ровно в
  день); «Работники»: display grid, колонки равные, отпуска справа профиля, СИЗ
  под отпусками, мероприятия под профилем, ширина ≥900px, «Мероприятия · 2026»
  с записями марта/июня/сентября; «Общая» — колонка-год; мобайл — стек, чип
  «Мероприятия» с секциями; 0 JS-ошибок ×2;
- VLM ×3 (glm-5v-turbo): сетка 2×2 читаема/дефектов нет; окно с тремя секциями;
  мобильный стек — ОК;
- CI ×4 success (Tests / Sync-to-desktop / Pages / pages-build); прод kip8test —
  SW kipia-test-v622, все маркеры Task 394 в живом index.html.

**Коммит:** 8d11ff7 · **Пруфы:** task394-proof-workers.png, task394-proof-events.png,
task394-proof-mobile.png
