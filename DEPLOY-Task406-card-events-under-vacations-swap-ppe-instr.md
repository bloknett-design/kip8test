# Task 406 — карточка «Работники»: мероприятия под отпусками; СИЗ и инструктажи поменяны местами

**Заявка**: «В картах работников, блок мероприятия перемести под
блок отпуска, а блоки СИЗ и инструктажей поменяй местами».

**Репозиторий**: kip8test (тестовый сайт)
<https://bloknett-design.github.io/kip8test/>

**Версия SW**: kipia-test-v632 → **v633**
**Тесты**: 3857 passed / 0 failed (было 3845/0; +12 в
test-task406.js, адаптация 7 файлов: 393 / 394 / 395 / 403 / 404 /
405 + run-all)
**Браузер-чек**: 41/41 ×3 контекста (тёмная view / светлая edit /
мобайл 375), 0 JS-ошибок; VLM ×3 (glm-5v-turbo) — подтверждено
визуально (колонки карточки, попап без СИЗ, стек мобайла).

## Что изменилось

### Раскладка карточки (страница «Работники»)

Было (Task 404/405):

```
| колонка 1        | колонка 2 | колонка 3            |
| профиль          | СИЗ       | мероприятия          |
| отпуска         |           | повторные инструктажи |
```

Стало (Task 406):

```
| колонка 1        | колонка 2                | колонка 3 |
| профиль          | повторные инструктажи     | СИЗ       |
| отпуска         |                          |           |
| мероприятия     |                          |           |
```

- **`_renderWorkerCardPanels`** — единственная функция раскладки:
  `if (bi < 3) colMain` (профиль + отпуска + **мероприятия** —
  блок мероприятий ПОД блоком отпуска), `else if (bi === 4)
  colInstr` (повторные инструктажи — **вторая** колонка
  `.ws-wcol-instr`), `else colPpe` (СИЗ — **третья** колонка
  `.ws-wcol-ppe`; блоки СИЗ и инструктажей поменяны местами).
- Легаси-сборщик `colTr` и класс `.ws-wcol-tr` удалены.
- **CSS не менялся**: три равные колонки `flex: 1 1 0` в одну
  линию (Task 404) сохранены; `.ws-wcol-instr` / `.ws-wcol-ppe` —
  маркеры без собственных правил.
- **Мобайл ≤1023px** — вертикальный стек колонок в новом порядке:
  профиль → отпуска → мероприятия → инструктажи → СИЗ.
- **Попап шахматки НЕ менялся**: b1+b2+b3+b5 (профиль → отпуска →
  мероприятия → инструктажи, без СИЗ — Task 403) уже соответствует
  заявке.
- **Сервер (WorkSchedule.gs) не трогался** — меняется только
  раскладка, данные те же.

### Тесты

- `tests/test-task406.js` (+12): SRC — раскладка/порядок возврата/
  удаление легаси/CSS/попап-регресс; VM — колонки 3|1|1, стек
  мероприятий под отпусками, обучение в 1-й колонке, мобайл-порядок
  DOM, зритель без кнопок, попап-регресс; SW v633.
- Адаптация (task406-adapt-tests.py + -2.py): test-task403 (SRC +
  VM), test-task404 (SRC + VM), test-task405 (SRC «панели»),
  test-task393 (VM порядок DOM), test-task394 (VM DOM-порядок
  страницы), test-task395 (SRC + VM + страница).
- `task406-bump-sw.py` — v632 → v633 (92 файла тестов).

## Серверные шаги

**Нет** — серверная часть (WorkSchedule.gs) в этой задаче не
менялась. GAS уже на версии Task 405 (пользователь задеплоил и
запустил trainingsSplitInit).

## Пруфы

- `task406-proof-workers-light.png` — карточка: 3 колонки,
  мероприятия под отпусками, инструктажи 2-я, СИЗ 3-я.
- `task406-proof-grid-dark.png` — попап ячейки (регресс 405/403).
- `task406-proof-instr-form.png` — префилл «+ Инструктаж…».
- `task406-proof-mobile.png` — мобайл 375, стек 5 блоков.
