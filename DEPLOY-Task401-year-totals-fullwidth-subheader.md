# Task 401 — окно итогов «Год»: полная ширина + пояснение «дней/часов» + итоговые столбцы

**Дата:** 23.09.2026. **Заявка:** «В десктопной версии приложения, в
разделе Табель учёта рабочего времени, окно итогов учёта за год
должно раскрываться полностью закрывая собой шахматку табеля,
вплотную примыкая к правому краю столбца с фамилиями работников,
и в шапке окна итогов учёта за год, под числами должно быть
пояснение данных в ячейках "дней/часов", и три крайних правых
столбца (дней, часов, перераб. (дни)) выделить фоном немного
другого оттенка от остальных ячеек со столбцами месяцев, и
разделить их вертикальной линией таблицы такой же как с левого
края окна итогов учёта за год».

## Что это

Клиентский фикс (только index.html, серверных шагов НЕТ): четыре
изменения окна итогов учёта за год (шторка справа в разделе
«Табель учёта рабочего времени», вкладка «Год»).

## 1) Полная ширина шторки «Года» (десктоп)

`_fitTtDrawer` во вкладке «Год» (шторка, НЕ мобильная страница
итогов) ставит ширину = `#wsWsBody.clientWidth` − `--ws-emp-w`
(натуральная ширина колонки ФИО по тексту — её мерит
`_measureEmpCol`; на десктопе сужения ФИО не бывает: `_narrowApply`
гасит его при ≥1024px). Окно раскрывается от правого края столбца
фамилий до правого края рабочей области — шахматка дней полностью
закрыта. Класс `ws-tt-yearfull` на странице (ставит/снимает
`_fitTtDrawer`, снимает `_ttCloseCleanup` при закрытии):

- кап `max-width: 60%` снят;
- таблица года — `width: 100%` (растягивается по шторке; селектор
  только `#wsTotalsPanel` — мобильная страница итогов и прямой
  заход по `#hash` не тронуты);
- полоса-разделитель ФИО сетки (`::after`, Task 333) скрыта — у
  левого края окна остаётся ЕДИНСТВЕННАЯ линия `.ws-tt-edge`
  (иначе на стыке две линии 2px + 2px);
- ползунок прокрутки шахматки (`.ws-grid-hbar`) погашен — дни
  закрыты шторкой, скроллить нечего (мертвый UI); 12px-зона
  остаётся — бордюры сетки и шторки на одном уровне (Task 331).

Ресайз окна — пересчёт ширины (ResizeObserver/fallback в
`_attachFitResize`, сходится: повторный вызов с теми же входными
не меняет ширину). Вкладка «Месяц» и закрытие шторки — прежняя
ширина по столбцам таблицы (Task 329).

## 2) Подстрока шапки «дней/часов»

Шапка годовой таблицы стала двухстрочной: под месяцами («янв…дек»)
— одна на все 12 месяцев ячейка-пояснение `th.ws-tt-sub`
(colspan=12, компактная: высота auto, 10px): формат данных ячеек
месяцев «11/132» = слева дни, справа часы. «Работник» и три
итоговых заголовка — `rowspan="2"` (вертикально по центру обеих
строк). У ОБОИХ таблиц (главная + архив). `_applyTtHeadVar`
меряет thead уже с двумя строками — `--ws-tt-head-h` растёт,
шапка сетки синхронно (`_fitGrid`), строки итогов остаются по
строкам сетки.

## 3) Три итоговых столбца — фон другого оттенка

Класс `ws-tt-sum` на th+td столбцов «Дней», «Часов», «Перераб.
(дни)» (главная + архив):

- шапка: `#243048` тёмная (светлее стали `#1e293b`) / `#b4c1cd`
  светлая (чуть темнее `#bfcad5`);
- ячейки: полупрозрачная тонировка `rgba(255,255,255,0.05)` тёмная
  / `rgba(0,0,0,0.05)` светлая — поверх зебры строк (зебра видна).

## 4) Разделитель 2px

`ws-tt-sum-edge` (th+td столбца «Днев»): `border-left: 2px solid`
`#4a8fc7` тёмная / `#6e8ba4` светлая — та же линия, что левый край
окна итогов `.ws-tt-edge`. В `border-collapse` стык 2px побеждает
1px границу «дек» — линия непрерывна по всей высоте таблицы,
включая подстроку шапки.

## Затронутые места index.html

| Место | Правка |
|---|---|
| CSS `.ws-tt-drawer` (десктоп, ≥1024) | + блок `ws-tt-yearfull` (кап/100%/`::after`/ползунок) |
| CSS `::after` ФИО сетки | + блок Task 401 ПОСЛЕ базовых правил (первое вхождение селектора — базовое, тесты Task 336) |
| CSS `.ws-tt-table.ws-tt-year th` | + `th.ws-tt-sub`, `th/td.ws-tt-sum`, `.ws-tt-sum-edge` (обе темы) |
| JS `_fitTtDrawer` | годовой режим: полная ширина + класс `ws-tt-yearfull`; месяц — прежняя логика |
| JS `_ttCloseCleanup` | + снятие `ws-tt-yearfull` |
| JS `_attachFitResize` | + пересчёт годовой ширины в ResizeObserver и resize-fallback |
| JS `_renderTotalsYearTable` | шапки главной+архива: rowspan=2, ws-tt-sum(-edge), подстрока «дней/часов»; yearRow: классы итоговых td |

## Версии

- SW: `kipia-test-v627` → `kipia-test-v628` (guard в тестах: v629)
- Тесты: 3726 → **3749** (+23, `tests/test-task401.js`: SRC 14 / VM 7 / SW 2),
  прогон **3749 passed, 0 failed**
- Адаптация старых тестов (новая разметка шапки года): test-task321
  (rowspan, `<tr>` 4→6, итоговые th), test-task323 (`<tr>` 4→6),
  test-task333 (regex rowspan) — `scripts/task401-adapt-tests.py`

## Проверки

- `node tests/run-all.js` — 3749/0.
- Браузер-чек `scripts/task401-browser-check.py` (порт 8901, мок
  2 активных + 1 архивный): **37/37** ×4 контекста — тёмная
  (геометрия: ширина = область − ФИО ±4px, левый край = правый край
  ФИО ±3px, правый = край области ±2px, дни закрыты, подстрока,
  фоны, 2px rgb(74,143,199), `::after` скрыт, ползунок погашен,
  таблица 100%, ресайз 1100 переширотился), светлая (цвета
  rgb(110,139,164), фоны), регресс «Год»→«Месяц» (класс снят,
  шторка сужена, полоса ФИО вернулась, закрытие чистое), мобильный
  375 (страница итогов: подстрока ×2, классы, архив, шторки нет);
  0 JS-ошибок во всех контекстах. Пруфы: `task401-proof-*.png`.
- VLM ×3 (glm-5v-turbo): тёмная/светлая/мобильная — все три
  требования заявки подтверждены визуально, дефектов вёрстки нет.

## Деплой

1. `git push` в `main` kip8test (CI: Tests → Sync desktop → Pages).
2. Обновление у пользователей: Ctrl+Shift+R ×1–2 (новый SW
   `kipia-test-v628` активируется со второй перезагрузкой).
3. СЕРВЕРНЫХ ШАГОВ НЕТ (Apps Script и Google Sheets не менялись).

## Перенос в kip8 (после приёмки)

`index.html` + `sw.js` (замена `kipia-test-v*` → `kipia-v*`,
`v475` → `v476`) + `tests/` (включая test-task401.js и
адаптации 321/323/333). Сервер/листы не менялись.
