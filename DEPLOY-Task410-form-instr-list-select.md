# Task 410 — форма «+ Инструктаж…»: выбор пункта «Список_И_и_ПЗ» без старых типов

**Заявка:** «Не работает: в форме добавления инструктажа или проверки знаний, в карточке
работников, в блоке инструктажей и проверки знаний, старые выпадающие списки, в том
числе с обучение, прогул, примечание, а нужно, чтобы я мог выбрать один из инструктажей
или проверки знаний из таблицы Список_И_и_ПЗ».

## Что сделано (только клиент — index.html; сервер НЕ менялся)

Шторка `#wsTrSheet` («Новое мероприятие») разделена на ДВА РЕЖИМА:

### 1) Instr-режим — форма «Новый инструктаж / проверка знаний»

Вход: кнопка **«+ Инструктаж…»** блока «Повторные инструктажи и периодическая
проверка знаний» (карточка работника, страница «Работники» + попап сетки);
правка записи инструктаж/проверка_знаний (кнопка ✎).

- поле **«Тип» скрыто** (вместе с группой; «Таб. №» растягивается на всю
  ширину) — старых выпадающих списков обучение/прогул/примечание в форме НЕТ;
- **«Тема» → «Инструктаж / проверка знаний»**: СТРОГИЙ select ВСЕХ пунктов
  «Список_И_и_ПЗ» ОДНИМ списком с группами `<optgroup>` **«Инструктажи»**
  (2 пункта) / **«Проверка знаний»** (3 пункта) — заявка: «выбрать один из
  инструктажей или проверки знаний из таблицы Список_И_и_ПЗ»;
- заголовок: «Новый инструктаж / проверка знаний» (создание) /
  «Правка инструктажа / проверки знаний» (правка);
- подсказка выбранного пункта — как в Task 408/409:
  «раз в год · Основание: инструкция № 53-ОТ»;
- **тип записи определяется выбранным пунктом** — «вид» пункта шаблона
  (новый хелпер `_instrTypeOfTheme`, нормализация 407: регистр/пробелы/«ё»);
- «вне списка» (правка старой записи): тема — отдельный пункт
  «… (вне списка)», тип записи СОХРАНЯЕТСЯ (`_trEditType`);
- тосты: «Запись добавлена» / «Запись обновлена»;
- шаблона нет (лист не создан / сервер старый) — деградация в свободный
  ввод (как в 407/408).

### 2) Форма «Новое мероприятие» (событие)

Вход: «+ Мероприятие…» блока «Мероприятия» и **попап ячейки шахматки**
(дефолт типа изменён с «инструктаж» на «обучение»); правка записей
мероприятий.

- поле «Тип» — только **обучение / прогул / примечание** (инструктаж и
  проверка знаний УБРАНЫ из опций: записи «Инструктажей» создаются только
  через блок инструктажей — Task 405/409 «больше никаких видов мероприятий
  в блоке данного раздела»);
- «Тема» — свободный текстовый ввод (datalist 407 — деградация);
- заголовки прежние: «Новое мероприятие» / «Правка мероприятия».

### Механика

- `_trInstrMode` (режим) + `_trEditType` (тип правимой записи) в состоянии
  WorkSchedule; `_applyTrFormMode()` применяет режим к разметке (скрытие
  группы «Тип», ширина «Таб. №», ярлык темы);
- `_syncTrTitleField` — select всех пунктов с группами по виду (вместо
  фильтра по типу из Task 408); «вне списка» — как прежде;
- `submitTrainingForm`: тема из select (instr), `тип =
  _instrTypeOfTheme(тема) || _trEditType || 'инструктаж'`;
- маршрутизация на сервере НЕ менялась: `addTraining` сам раскладывает
  инструктаж/проверка_знаний → лист «Инструктажи», обучение/прогул/
  примечание → лист «Мероприятия» (Task 405).

## Тесты — 3960/0 (было 3946)

- `test-task410.js` +14: SRC (3 опции типа без И/ПЗ; id групп; режимы
  openTrainingForm; _applyTrFormMode; submit-вывод типа и тосты; входы;
  свойства); VM `_instrTypeOfTheme` (вид пункта/нормализация/«вне списка»);
  VM `_applyTrFormMode` (instr/event); SW v637.
- Адаптация к новой семантике: test-task306 (опции типа 5 → 3),
  test-task405 (prefillType → режим), test-task408 (SRC useSel/submit;
  VM formHost с явным режимом, все пункты + группы),
  test-task409 (VM: 5 пунктов + группы вместо 2/3 по виду),
  test-task309 / test-work-events (заголовки — тернарник по режиму),
  test-work-schedule (попап ячейки — «обучение»).

## Браузер 44/44 ×3 (порт 8911) + VLM ×3

Тёмная edit: попап ячейки (тип обучение, дата ячейки, 3 опции);
карточка: «+ Инструктаж…» — заголовок, «Тип» скрыт, «Таб. №» на всю
ширину, select 5 пунктов (2+3 в группах), валидация пустого, подсказки
«раз в 6 месяцев» / «раз в год · Основание: инструкция № 53-ОТ», submit →
тип из вида пункта (инструктаж/проверка_знаний), тост «Запись добавлена»;
«+ Мероприятие…» — прежняя форма (обучение); правка «вне списка» (тип
сохранён) и правка примечания. Светлая view: регресс блока (5 групп,
«раз в год»), у зрителя кнопок нет. Мобайл 375 edit: форма instr
(без «Тип», 5 пунктов, «раз в 3 месяца», submit). VLM (glm-5v-turbo):
подтверждено ×3 (заголовок/подсказка процитированы точно; на мобайл-форме
нет ни «Тип», ни обучение/прогул/примечание).

## SW

`kipia-test-v636` → `kipia-test-v637` (guard-файлы по git HEAD: v637 → v638;
73 + 97 файлов).

## Серверные шаги

**НЕТ** — WorkSchedule.gs не менялся. Актуальны шаги Task 409, если ещё
не выполнены: обновить WorkSchedule.gs в Apps Script табель_КИП_ИОС и
разово запустить `instrListInit` (приведёт «Список_И_и_ПЗ» к 5 пунктам).
Если лист уже заполнен — ничего делать не нужно: форма читает список
кнопкой «Обновить» (общая загрузка данных).
