# DEPLOY — Task 387: панель «Обозначения» до 500px; канон справочника кодов в списках выбора; пояснения; «Выходной» без точки

**Дата:** 2026-09-21
**Репозиторий:** kip8test (тестовый PWA)
**SW:** kipia-test-v614 → **kipia-test-v615**
**Заявка:** «Скорректируй, панель плавно разворачивается шире (до 500px) с
подробными наименованиями кодов и секцией пояснений, также скорректируй в
списках выбора кодов дней и мероприятий с правками в описании и в следующей
последовательности по группам: День 8-час…; День 7,2-час…; День 12-час…;
Ночь 12-час…; День/Ночь в вых./праздник…; Отпуск…; Учебный отпуск…; Отгул…;
Больничный…; Выходной…; Прогул…; Инструктаж…; Обучение…; Проверка знаний…;
Примечание…. Удали "Код мероприятия ставится в угол ячейки ПОВЕРХ плановой
смены (Д/Н); ручная правка дня сильнее.", "В отпусках: «12 дней (−2
праздн.)» — вычтены праздники (ст. 120 ТК РФ); годовой лимит — 42 дня
(ст. 125 ТК РФ, до 3 частей).", "Зебра строк — чередование фона по
работникам; перекрестье — подсветка строки и столбца при наведении (кнопка
в ряду 3).". Измени "Рамка 2px вокруг группы ячеек — выходные и праздники;
«*» у числа (24*) — сокращённый предпраздничный день." на "Красная рамка
вокруг группы ячеек — выходные и праздники; «*» у числа (пример - 24*) —
сокращённый предпраздничный день.". В коде "· Выходной, плановый выходной
день" я убрал точку, теперь просто пустая ячейка белого цвета, и в шахматке
должно отображаться так же, проверь.»»

---

## ЧТО СДЕЛАНО (клиент — работает сразу после пуша)

### 1. Панель «Обозначения» — развёрнутый вид до 500px
- Шеврон `#wsLgChv` разворачивает панель до **min(500px, 45vw)** (было
  min(400px, 45vw)): CSS-правило `.ws-legend-drawer.ws-lg-wide
  .ws-legend-inner` + JS-кап `_legendWidthPx` (согласованы). Плавная
  анимация width 0.28s (слот + inner) — прежняя, «плавно разворачивается
  шире»; на узких экранах капится 45vw (например @700 → 315px).

### 2. Списки выбора кодов дней и мероприятий — канон справочника
- Новый канон **`_STATUS_CODES_CANON`** (16 кодов Т-12/Т-13): порядок
  **по группам заявки** (Д8 → Д7,2 → Д → Н → д → н → ОТ → У → ОВ → Б →
  Выходной → ПР → И → ОБ → ПЗ → *) и **полные наименования** (Д8/Д7,2 —
  «…в пятницу (предпраздничный день) на час короче»; ОВ — «…оплачиваемые,
  за ранее отработанное время, без содержания — с указанием в
  комментарии»; и т. д. по заявке).
- Новый метод **`_normalizeStatusCodes(list)`** — нормализация ЛЮБОГО
  источника (сервер / кэш localStorage / офлайн-фолбэк):
  * известные канону коды — в каноническом порядке, с каноническими
    наименованиями, ЦВЕТА — живые из листа «Коды_статусов» (свотчи);
  * «Выходной» — слот с ПУСТЫМ кодом: строка листа с «» ИЛИ легаси-«.»;
    отсутствует в листе — добавляется (выходной = пустая ячейка сетки);
  * коды вне канона (лист расширен пользователем) — в конце списка,
    живые имена/цвета.
- Затронуты ВСЕ списки: попап клика по ячейке (коды дней), select
  «Дополнительно…», легенда «Обозначения» (шторка + мобильная страница),
  печать (перечень кодов месяца). Фолбэк офлайна = канон (прежний
  литерал 16 строк удалён).

### 3. Пояснения легенды (Task 387: −3, «красная рамка»)
- УДАЛЕНЫ: «Код мероприятия ставится в угол ячейки ПОВЕРХ плановой смены
  (Д/Н); ручная правка дня сильнее.», «В отпусках: «12 дней (−2 праздн.)»
  — вычтены праздники (ст. 120 ТК РФ); годовой лимит — 42 дня (ст. 125 ТК
  РФ, до 3 частей).», «Зебра строк — чередование фона по работникам;
  перекрестье — подсветка строки и столбца при наведении (кнопка в ряду
  3).».
- «Рамка 2px…» → **«Красная рамка вокруг группы ячеек — выходные и
  праздники; «*» у числа (пример - 24*) — сокращённый предпраздничный
  день.»**. Живы: бейдж плановой смены, «сегодня».

### 4. «Выходной» — пустая ячейка белого цвета (точка убрана)
- Строка «Выходной» в попапе и легенде — **БЕЗ кода-символа «·»**:
  свотч — фон ПУСТОЙ ячейки темы (`.ws-swatch-dot` /
  `.ws-lg-swatch-dot`, светлая — #FFFFFF), имя «Выходной, плановый
  выходной день».
- Клик по «Выходному» в попапе — очистка ячейки (пустой статус, путь
  шита — как «— выходной —»); легаси-«.»-ячейка подсвечивает строку
  «Выходного» как активную.
- Select «Дополнительно…»: «— выходной —» (первая опция); текущее
  значение «.» записи маппится в неё (дубль-опции и «(нет в
  справочнике)» для «.» больше нет).
- **Шахматка: проверено** — «.»-ячейки рендерятся ПУСТЫМИ белыми (классы
  `ws-dot-code`/`ws-status-empty`, центр без «·» — Task 356 жив);
  браузер-чек K1–K4 + VLM подтверждают.
- Печать: «.»-данные месяца раскрываются строкой «Выходного» (без
  кода-символа).

---

## РАЗВЁРТЫВАНИЕ

1. **Git push** в `main` kip8test — GitHub Pages задеплоит сайт, CI
   прогонит тесты и синхронизирует десктоп-зеркало (автоматически).
2. **Сервис-воркер:** версия поднята `kipia-test-v614 → kipia-test-v615` —
   при следующем открытии приложения клиент обновится сам.
3. **Сервер (Google Apps Script): шаги ОПЦИОНАЛЬНЫ** — клиент корректен и
   без них (нормализация/канон полностью на клиенте; «Выходной» добавляется
   даже если строка листа не проходит через старый `getStatusCodes`).
   Рекомендуются для консистентности данных:
   * **WorkSchedule.gs** — в `getStatusCodes` строка с ПУСТЫМ кодом
     колонки A (пользователь убрал «.» из листа «Коды_статусов») больше не
     пропускается: `if (!row[0] && !row[1]) continue;` (было
     `if (!row[0]) continue;`). Вставить правку в Apps Script → «Сохранить»
     → «Управление развёртываниями» → «Изменить» → «Новое развёртывание»
     (та же механика, что в Task 384).
   * **StatusCodesInit.gs** (по желанию — выровнять лист под канон):
     эталон `SC_STATUS_CODES` обновлён (порядок по группам, полные
     формулировки, «Выходной» — пустой код, #EEF0F2); `_scValidateData`
     допускает пустой код (дубль ловится seen-набором). Запуск
     `statusCodesReplace()` перезапишет лист «Коды_статусов» (прежний —
     в резерв); актуализация формулировок в самой таблице НЕ обязательна
     для отображения — клиент показывает канон Task 387.

⚠️ Открытый пункт из Task 384 остаётся в силе (если ещё не выполнен):
ручной деплой Apps Script — `Code.gs` (2 case doPost) + `WorkSchedule.gs`
(updateEmployee/updateVacation) + «Новая версия». Задача 387 НЕ зависит от
него, но если делаете серверный шаг 3 — используйте СВЕЖУЮ копию
`WorkSchedule.gs` из репо (в ней уже обе правки: Task 384 и Task 387).

---

## ПРОВЕРКА

- Тесты: **3447 passed / 0 failed** (было 3414; +33 `test-task387.js`,
  адаптации в 7 файлах — `scripts/task387-adapt-tests.py` + 2 точечных
  фикса, SW-бамп 74 файла — `scripts/task387-bump-sw.py`).
- Браузер (Playwright, порт 8998): **54/54** — десктоп/мобайл, включая:
  разворот ~500px; **нормализация доказана на «старом» листе** (мок в
  порядке Task 298 с короткими именами и «.»-выходным → списки показывают
  порядок по группам и ПОЛНЫЕ формулировки: Д8 первый, «на час короче»,
  ОВ «за ранее отработанное время»); «Выходной» — пустой код + белый
  свотч (шторка/попап/мобильная страница); пояснения −3/«красная рамка
  (пример - 24*)»; легаси-«.»-ячейка в сетке — ПУСТАЯ белая без «·»;
  Esc/взаимоисключение; 0 JS-ошибок ×2.
- VLM (glm-5v-turbo) ×4: широкий вид с наименованиями (Д8 первый);
  прокрученная панель — «Выходной» с белым свотчем без «·», «Красная
  рамка… (пример - 24*)», ст. 120/125/«Зебра» отсутствуют; сетка без
  «·»-символов; мобильная страница с полным контентом.

**Файлы:** `index.html` (17 правок — `scripts/task387-patch.py`),
`scripts/WorkSchedule.gs` (1 правка — справочная копия), 
`scripts/StatusCodesInit.gs` (2 правки — справочная копия), `sw.js` (v615),
`tests/test-task387.js` (+33), адаптации тестов
(`scripts/task387-adapt-tests.py`), `scripts/task387-browser-check.py`,
`scripts/task387-bump-sw.py`.
