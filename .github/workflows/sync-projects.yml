name: Update Projects (TEST)

# ВНИМАНИЕ: это тестовый репозиторий kip8test.
# Автоматический cron ОТКЛЮЧЁН, чтобы не дублировать запросы к Яндекс Диску
# с основным репозиторием kip8. Запуск только вручную через workflow_dispatch.
# При необходимости можно временно включить schedule, раскомментировав блок ниже.
#
# КАК НАСТРОИТЬ:
# По умолчанию используется публичная ссылка:
#   https://disk.yandex.ru/i/hfIfJ3EaHMWp9A
#   лист: Проекты
#   (файл «Перечень проектов КИП пр-ва ИОС.xlsx»)
# Можно переопределить через переменные окружения в шаге "Sync":
#   PROJECTS_PUBLIC_KEY, PROJECTS_SHEET_NAME
#
# Секреты НЕ требуются — публичная ссылка вшита в DEFAULT_PUBLIC_KEY.

on:
  workflow_dispatch:
  # schedule:
  #   - cron: '0 3 * * *'

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install openpyxl requests

      - name: Sync from Yandex Disk and convert to JSON
        run: python scripts/sync-projects.py

      - name: Check for changes
        id: check_changes
        run: |
          git diff --exit-code data/projects.json || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/projects.json
          git commit -m "auto: update projects data from Yandex Disk (test repo)"
          git push

      - name: Summary
        if: always()
        run: |
          echo "## Projects Sync Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.check_changes.outputs.changed }}" == "true" ]; then
            echo "✅ Данные обновлены и закоммичены" >> $GITHUB_STEP_SUMMARY
          else
            echo "ℹ️ Изменений нет" >> $GITHUB_STEP_SUMMARY
          fi
          if [ -f data/projects.json ]; then
            COUNT=$(python3 -c "import json; d=json.load(open('data/projects.json')); print(d.get('total_projects', 'N/A'))")
            echo "**Всего проектов:** $COUNT" >> $GITHUB_STEP_SUMMARY
            echo "**Источник:** Yandex Disk (public)" >> $GITHUB_STEP_SUMMARY
            echo "**Лист:** Проекты" >> $GITHUB_STEP_SUMMARY
          fi
