# Task 396 — зебра строк + компактные кнопки в шапках блоков + оглавления ярче

**Дата:** 23.09.2026
**Репо:** kip8test (тестовый сайт)
**SW:** kipia-test-v623 → **kipia-test-v624**
**Коммит:** feat: Task 396 (index.html + tests)

## Что сделано (заявка, 3 части)

1. **Зебра строк в блоках карточек.** В каждом блоке-окне
   карточки работника (профиль / отпуска / мероприятия / СИЗ)
   чередующиеся строки получили чередующийся фон: вторая,
   четвёртая… строка — класс `ws-row-alt`, оттенок — как зебра
   сводной таблицы «Общей» вкладки (тёмная тема
   `rgba(255,255,255,0.045)`, светлая `rgba(0,0,0,0.05)`).
   Строки — «пилюли» со скруглением 6px и боковым паддингом
   10px (у всех строк одинаковым — текст не «пляшет»). Зебра
   ставится ТОЛЬКО у блоков-окон страницы «Работники»
   (`asBlocks=true`); попап карточки у сетки шахматки — БЕЗ
   зебры (прежняя компактная типографика). Зебра не зависит от
   прав: видна и зрителям (это отображение, не правка).
   Счёт полей профиля — по РЕНДЕРЯЩИМСЯ строкам (пустые значения
   не сдвигают чередование).
2. **Компактные кнопки в верхнем правом углу блоков.** Действия
   переехали из строк внизу блоков в шапки (`.ws-whead-a`,
   `margin-left:auto` — при переносе на узком экране остаются
   справа): «Правка данных…» (акцентная) и «Уволить…»
   (красная опасная) — в шапке блока профиля; «+ Отпуск…»,
   «+ Мероприятие…», «+ СИЗ…» — в шапках своих блоков.
   Кнопки `.ws-wbtn` — компактные 26px, шрифт 12px, стиль
   сайта (тёмная — синий `var(--accent-blue)`, светлая —
   оранжевый #C6613F — как «Добавить работника» Task 391);
   «Уволить…» — #ef5350/#c62828 (как ✕ удалений).
   **Контракты сохранены**: прежние onclick
   (`openEmpEditForm`/`openDismissForm`/`onEmpAddVacation`/
   `onEmpAddTraining`/`onEmpAddPpe`) и классы-маркеры
   (`ws-emp-editdata`/`ws-emp-dismiss`/`ws-emp-addvac`/
   `ws-emp-addtr`/`ws-emp-addppe`). Строки-действия прежнего
   вида остаются для рендера БЕЗ asBlocks (легаси-путь,
   гейт `withEdit && !asBlocks`) — попап шахматки не задет.
3. **Оглавления блоков — шрифт крупнее/ярче + другой фон.**
   Шапка-полоса `.ws-whead` на всю ширину окна (отрицательные
   поля гасят паддинг панели; верхние углы скруглены 8px —
   внутренний радиус панели). Фон — ДРУГОЙ, как выделенная
   шапка сводки «Общей» вкладки (`.ws-wgen-head`, Task 390):
   тёмная #1E2B42, светлая #E4E0D3 + нижняя линия-разделитель.
   Заголовок ФИО — 16px/700/первичный цвет, без капса
   (`ws-whead-name`; было 15px/600/вторичный); секции
   («ОТПУСКА · 2026» и т.д.) — 13px/700/первичный, капс
   (было 12px/600/вторичный).

## Изменённые файлы

- `index.html` — JS `_renderWorkerCard`: шапки b1–b4 тернарные
  `asBlocks ? .ws-whead(кнопки в углу) : .ws-popup-title/sec`
  (легаси-ветки попапа сохранены); зебра `ws-row-alt` в 4
  циклах строк; легаси-строки — гейт `withEdit && !asBlocks`;
  CSS: `.ws-whead`/`.ws-whead-t`/`.ws-whead-name`/`.ws-whead-a`,
  `.ws-wbtn`/`.ws-wbtn-danger`, зебра `ws-row-alt` (обе темы),
  паддинги строк 10px, «пилюли» radius 6px, пустые строки
  «нет …» — вровень со строками.
- `tests/test-task396.js` — +19 (SRC: шапки/кнопки/гейты/зебра/
  CSS/SW; VM: блоки с шапками, 5 кнопок в шапках, зебра 1/3-2/4,
  зритель без кнопок с зеброй, легаси-вызов со строками, попап
  прежний, страница 4 шапки + 5 кнопок).
- Адаптации: `test-task393.js` (4 ассерта «var bX =» — на
  проверку обеих веток), `test-task318.js` (сегмент ±400 вокруг
  «Уволить…» не содержит гейт — ассерт переписан на «гейт
  РАНЬШЕ кнопки по тексту»).
- `sw.js` — kipia-test-v624; 83 тест-файла (ассерты v624,
  guards v625).

## Проверки

- Тесты: **3685/0** (3666 + 19 новых).
- Браузер (порт 9006, Playwright): **32/32** — тёмная десктоп
  (4 шапки #1E2B42 во всю ширину, ФИО 16px/700, секции 13px/700,
  5 кнопок 26px: синяя/красная в профиле, «+ …» в блоках, правый
  край у рамки; зебра 2/4-х строк всех 4 блоков; легаси-строк
  внизу нет; попап прежний; 0 JS-ошибок), view (шапки+зебра без
  кнопок), мобайл 375 светлая (шапка #E4E0D3, кнопки #C6613F/
  #c62828, зебра 0.05, перенос кнопок ниже заголовка справа;
  0 JS-ошибок). VLM ×3 (glm-5v-turbo — все пункты подтверждены,
  дефектов нет).
- Пруфы: `task396-proof-workers-dark.png`,
  `task396-proof-workers-view.png`,
  `task396-proof-mobile-light.png`.

## Серверные шаги

**НЕТ** — Apps Script не тронут (чистый клиент).

## ⚠️ Статус деплоя

Локальный коммит Task 396 готов; push в GitHub заблокирован
утратой PAT после отката песочницы (см. worklog: восстановление
Task 395 из origin). После переотправки PAT пользователем:
`git push` → CI ×4 → прод проверит SW kipia-test-v624 и маркеры.
