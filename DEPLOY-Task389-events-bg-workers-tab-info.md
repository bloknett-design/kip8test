# DEPLOY — Task 389 (kip8test)

## Заявка пользователя

1. «В окне мероприятий, цвет общего фона за мероприятиями с прошедшими
   датами сделай как раньше был общий фон (как сейчас общий фон выше
   за оглавлением окна).»
2. «На странице "Работники", кнопку "Добавить работника" перенеси с
   бара на общую вкладку работников, и в общей вкладке допиши
   информацию что, на текущий момент 12 работников (автоматический
   подсчёт), по штату 14 из которых 2 мастера, 5 сменных и 7 дневных.»
3. «Фон ярлыков и окон вкладок сделай не прозрачным, и расположи их
   с левого края.»

## Что изменилось (только клиент, index.html — серверной части НЕТ)

### 1. Окно «Мероприятия» — прошедшие = общий фон окна

Строки мероприятий с ПРОШЕДШИМИ датами (класс `.ws-ep-past`, дата
окончания строго раньше «сегодня») — теперь **ПРОЗРАЧНЫЕ**
(`background: transparent`, обе темы): сквозь них виден **общий фон
окна** `var(--bg-tertiary)` — тот же, что выше заголовка-оглавления
`.ws-ep-cap` («как раньше», до Task 380). «Светлее окна» из Task
380/381 для прошедших ОТМЕНЕНО. Текущие/будущие — ТЕМНЕЕ окна как
прежде (rgba-тинты Task 380/381 не тронуты). Класс `.ws-ep-past`,
каскад специфичностей и склейка строк сохранены — видимый край цвета
остаётся только на СТЫКЕ прошедших (общий фон) и идущих/будущих
(тёмная зона).

### 2. Страница «Работники» — кнопка на «Общей» вкладке

- Кнопка «Добавить работника» **УДАЛЕНА из шапки страницы**
  (`#page-ws-workers .page-inline-header`) — рендерится в шапке
  сводки «Общей» вкладки (`_renderWorkersGeneral`; id
  `wsWorkersAddBtn` СОХРАНЁН; только у редакторов — гейт `_canEdit`).
- Пустой список работников больше **НЕ прерывает рендер** — «Общая»
  вкладка (кнопка + численность + подсказка) показывается и без
  работников (прежде — ранний выход с текстом про «шапку страницы»).

### 3. «Общая» вкладка — численность и штат

Шапка сводки (flex: инфо-блок слева, кнопка справа):

- «**На текущий момент: N …**» — N — **АВТОМАТИЧЕСКИЙ ПОДСЧЁТ** по
  живому списку (склонение `_plural`);
- «**По штату: 14, из которых 2 мастера, 5 сменных и 7 дневных.**» —
  штат — константа заявки (2 + 5 + 7 = 14).

### 4. Вкладки — сплошные фоны, левый край

- Ярлыки `.ws-wtab` — **СПЛОШНОЙ** цвет (было rgba-тинты — фоновая
  сетка-«миллиметровка» страницы просвечивала): неактивный —
  `var(--bg-primary)` #1a2233 / #E4E0D3 (тёмная/светлая), hover —
  #243048 / #DBD6C8; активный — как ОКНО вкладки (`var(--bg-tertiary)`,
  «пристыкован», как прежде).
- ОКНО «Общей» вкладки — новая панель `.ws-wgen` со СПЛОШНЫМ фоном
  `var(--bg-tertiary)` + рамка/радиус (как карточка `.ws-wcard` —
  окно вкладки работника уже сплошное).
- Блок ярлыков и окон вкладок — **с ЛЕВОГО КРАЯ**
  (`.ws-workers-body`: `margin: 0 auto` → `margin: 0`; прежде блок
  центрировался на широких экранах).

## Сборка / версии

- SW: kipia-test-v616 → **kipia-test-v617** (guard v618), 76
  тест-файлов бампнуто (`scripts/task389-bump-sw.py`).
- Тесты: **3512 passed / 0 failed** (+24 `tests/test-task389.js`:
  SRC — transparent-правила/отмена «светлее»/каскад/комментарий,
  кнопка не в шапке/рендер в `_renderWorkersGeneral`/id сохранён,
  автоподсчёт/штат/склонение, ранний выход удалён, CSS
  margin/сплошные/панель/flex-шапка/SW; VM — «Общая» вкладка с
  численностью+штатом+кнопкой, зритель без кнопки, пустой список
  рендерится с кнопкой, вкладка работника = карточка / «Общая» =
  панель, прямые вызовы; адаптации 5 файлов — task385/386/388
  (кнопка/тексты), task380/381 (прошедшие transparent) + 4 точечных
  фикса итерации).
- Браузер-проверка: **37/37** (порт 8999; десктоп тёмная: инвариант
  «прошедшие → rgba(0,0,0,0), текущие → rgba(0,0,0,0.45)», фон окна
  #0e1621; ПРЕДЫДУЩИЙ месяц — все строки прошедшие и прозрачные
  (детерминизм); «Работники»: в шапке кнопки НЕТ, «На текущий
  момент: 3 работника (автоматический подсчёт).» + штат, кнопка
  внутри «Общей» (клик → шторка «Новый работник»/закрытие), ярлыки
  #1a2233/#0e1621 сплошные, панель .ws-wgen сплошная, margin:0 +
  отступ от края 12px, вкладка работника — карточка; десктоп
  светлая: окно #E9E7DE, прошедшие прозрачные/текущие 0.12, прошлый
  месяц — все прозрачные; мобайл 375 светлая: лента ярлыков #E4E0D3,
  окно #F0EEE6, численность+штат+кнопка; 0 JS-ошибок ×3).
- VLM: ×3 (glm-5v-turbo) — «Общая» вкладка (сплошные ярлыки/
  тексты/кнопка/левый край/без просвета сетки — всё ✓), окно
  мероприятий прошлого месяца (единый фон, светлых полос НЕТ ✓),
  мобайл+шторка создания (✓; геометрия вкладок дополнительно
  проверена JS-пробой: vertical=true, left=12 при открытой шторке).

## Серверных действий НЕТ

Чистый клиентский патч (CSS + рендер страницы «Работники» + окно
мероприятий). Apps Script и прочие .gs НЕ менялись.

## Развёртывание

1. `git pull` на боевом kip8 (перенос — отдельным коммитом
   Tasks 385–389-transfer).
2. Пользователям: обновить страницу 2 раза (смена SW v616→v617).
