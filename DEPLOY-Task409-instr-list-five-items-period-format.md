# Task 409 — эталон «Список_И_и_ПЗ» (5 пунктов заявки) + периодичность «раз в год»

**Заявка:** «В разделе инструктажей и проверки знаний должны содержаться: Повторный
инструктаж по рабочим инструкциям ОТ — 6 мес.; Повторный инструктаж по инструкции
№ 9-ОГЭ — 3 мес.; Периодическая проверка знаний на допуск к самостоятельной работе —
1 год; Периодическая проверка знаний на допуск к проведению работ в электроустановках
до 1000 В — 1 год; Периодическая проверка знаний по охране труда при выполнении
работ на высоте, по инструкции № 53-ОТ. Больше никаких видов мероприятий в блоке
данного раздела и таблицы Список_И_и_ПЗ быть не должно. Давай вначале доработаем
этот раздел, затем перейдём к разделу Мероприятия.»

**Уточнения (все ответы = рекомендованные):** периодичность пункта «работы на
высоте» — 1 год; «по инструкции № 53-ОТ» — в столбец «основание»; 12 мес.
отображаются как «раз в год» (24 → «раз в 2 года», 36 → «раз в 3 года»; 3/6 мес. —
«раз в 3 месяца»/«раз в 6 месяцев»); instrListInit при каждом запуске ЗАМЕЩАЕТ
строки листа эталоном; записи «вне списка» — показываются (как в Task 407).

## Что сделано

### 1) Сервер — WorkSchedule.gs: `instrListInit` = эталон заявки (замещение)

- **Ровно 5 пунктов** (типовые образцы Task 407 «Охрана труда»/«Пожарная
  безопасность»/«Электробезопасность»/«Проверка знаний по специальности» удалены):

  | # | название | вид | периодичность | основание |
  |---|----------|-----|--------------|-----------|
  | 1 | Повторный инструктаж по рабочим инструкциям ОТ | инструктаж | 6 | |
  | 2 | Повторный инструктаж по инструкции № 9-ОГЭ | инструктаж | 3 | |
  | 3 | Периодическая проверка знаний на допуск к самостоятельной работе | проверка_знаний | 12 | |
  | 4 | Периодическая проверка знаний на допуск к проведению работ в электроустановках до 1000 В | проверка_знаний | 12 | |
  | 5 | Периодическая проверка знаний по охране труда при выполнении работ на высоте | проверка_знаний | 12 | инструкция № 53-ОТ |

- **Замещение вместо идемпотентности:** лист уже есть → строки 2..lastRow
  очищаются (`clearContent`) и пишется эталон (заголовки/стили также приводятся);
  листа нет → создаётся с заголовками + эталон. Ответ: `{ok, created, reset,
  rows:5}`; аудит `…SHEET_CREATED` / `…SHEET_RESET`.
- ⚠ Повторный запуск ПОСЛЕ ручного заполнения листа заменит ваши правки на
  эталон — правьте список прямо в таблице (строки добавляются/удаляются), функцию
  больше запускать не нужно.

### 2) Клиент — index.html: `_fmtPeriodRu` («раз в год»)

- Новый самодостаточный хелпер `_fmtPeriodRu(n)` (рядом с `_plural`):
  12 → «раз в год», 24 → «раз в 2 года», 36 → «раз в 3 года», 60 → «раз в 5 лет»,
  6 → «раз в 6 месяцев», 3 → «раз в 3 месяца», 1 → «раз в месяц»,
  0/пусто → '' (разовый — не показывается); корректные формы множественного
  числа (год/года/лет, месяц/месяца/месяцев); периодичность из листа строкой
  («12») тоже принимается.
- Переведены оба места вывода:
  - шапка группы блока «Повторные инструктажи и периодическая проверка знаний»
    (карточка + попап) — `раз в год` вместо `раз в 12 месяцев`;
  - подсказка выбранного пункта в форме («Тема») —
    «раз в год · Основание: инструкция № 53-ОТ».

### 3) Тесты — 3946/0 (было 3927)

- `test-task409.js` +19: SRC (эталон 5 пунктов; образцы удалены; reset+clearContent;
  _fmtPeriodRu в обоих местах; SW v636); VM `_fmtPeriodRu` (месяцы/годы/строка из
  листа/пустой); VM блок по эталону (5 групп 2И+3ПЗ; подписи; «раз в год» ×3;
  без «раз в 12 месяцев»; срок = запись + периодичность); VM форма (select 2/3
  пункта вида; подсказка «раз в год · Основание: инструкция № 53-ОТ»; без
  основания — только периодичность); GAS-VM instrListInit (создание с 5 пунктами;
  замещение образцов эталоном; повторный запуск без дублирования).
- Адаптация `test-task407.js`: тест instrListInit → «создание + замещение
  эталоном (Task 409)»; GAS-VM создание (6 строк, эталон, чтение 5) и замещение
  вместо идемпотентности; ассерт шапки → `_fmtPeriodRu`; VM-хосты (sectionHost +
  cardHost) + `_fmtPeriodRu`; MockSheet + `clearContent`.
- Адаптация `test-task408.js`: formHost + архивный хост + `_fmtPeriodRu`.
- `run-all.js` +require.

### 4) SW-бамп — kipia-test-v635 → **v636**

- guard'ы «v636 не должен появиться» → v637 (73 файла, классификация по git HEAD);
  основная версия → v636 (96 файлов: sw.js + тесты).

### 5) Браузер — 52/52 ×3 контекста (порт 8910) + VLM ×3

- Тёмная view: тултипы бейджей сетки («И — Повторный инструктаж по рабочим
  инструкциям ОТ», «И — Повторный инструктаж по инструкции № 9-ОГЭ», «ПЗ —
  Внеплановый по наряду №4», «* — Перенос по приказу»); попап — 4 группы (пустой
  пункт скрыт), подписи «раз в 6 месяцев»/«раз в 3 месяца»/«раз в год» ×2, БЕЗ
  «раз в 12 месяцев», «вне списка», след. сроки актуальные/просроченные, без
  навигатора (год табели).
- Светлая edit: карточка 3 колонки (регресс 406); блок года табели — РОВНО 5 групп
  эталона; «— не проводился» (до 1000 В) / «— за 2026 год не проводился» (высота);
  ‹ 2025: запись высоты в группе, «след. срок (на конец 2025): 10.03.2026»
  нейтрально, без ⚠; «Мероприятия · 2025» (Курс АСУ ТП); ‹‹ 2024 min (‹ погашена);
  ›› возврат; форма: «+ Инструктаж…» — select РОВНО 2 пункта вида (+ пустой),
  пустой submit → API не зван, подсказка ОТ «раз в 6 месяцев», submit — тема из
  select, ПЗ → РОВНО 3 пункта, подсказка высоты «раз в год · Основание: инструкция
  № 53-ОТ», обучение → свободный ввод, правка «(вне списка)»; сводная (регресс
  405).
- Мобайл 375: стек 5 блоков (регресс 406); блок И-и-ПЗ — 5 групп + «раз в год»;
  навигатор работает (‹ архив 2025); element-скрин блока.
- 0 JS-ошибок ×3. Пруфы ×6. VLM ×3 (glm-5v-turbo): 5 групп, подписи
  «раз в 6 месяцев/3 месяца/год», отсутствие старого формата, подсказка формы
  процитирована точно, мобильный стек/навигатор — подтверждено.

## СЕРВЕРНЫЕ ШАГИ (пользователь, в Apps Script табель_КИП_ИОС)

1. Заменить содержимое `WorkSchedule.gs` на версию из репозитория (файл
   `scripts/WorkSchedule.gs`).
2. В редакторе Apps Script выбрать функцию `instrListInit` → **Run** (один раз):
   - листа «Список_И_и_ПЗ» нет → создастся с заголовками и 5 пунктами эталона;
   - лист уже есть (в т.ч. с образцами Task 407) → строки будут ЗАМЕЩЕНЫ эталоном
     5 пунктов.
3. В приложении нажать «Обновить» — блок «Повторные инструктажи и периодическая
   проверка знаний» соберётся по эталону: 2 инструктажа (И) + 3 проверки знаний
   (ПЗ), подписи «раз в 6 месяцев» / «раз в 3 месяца» / «раз в год».

## Примечания

- Таблицу можно править и вручную (строки добавляются/удаляются прямо в листе):
  «название» = «тема» записей «Инструктажей» (регистр/пробелы/«ё» не важны),
  «вид» — инструктаж/проверка_знаний, «периодичность» — число месяцев (пусто =
  разовый), «основание» — справочно (показывается в подсказке формы).
- Записи «Инструктажей», тема которых не совпала ни с одним из 5 пунктов,
  отображаются в секции «вне списка» блока (данные не теряются).
- Следующий шаг (по заявке): проработка раздела «Мероприятия».
