# Task 397 — ОБЩЕЕ ПРАВИЛО: кнопка раздела без доступа не отображается

**Дата:** 23.09.2026
**Репо:** kip8test (тестовый сайт)
**SW:** kipia-test-v624 → **kipia-test-v625**
**Коммит:** feat: Task 397 (index.html + tests)

## Что сделано (заявка)

«Примени общее правило к приложению: если у пользователя, согласно
его роли, нет доступа к определённому разделу, то кнопка данного
раздела не должна отображаться» — обобщение правила Task 395
(кнопка «Работники» по матрице) на ВСЁ приложение.

Аудит показал: сайдбар/меню-кнопки/вкладки верхнего бара уже
гейтятся списком allowed, но оставались точки-нарушения:

1. **«Инженерные калькуляторы» нижнего бара главной** — «видна
   всегда»: у роли БЕЗ `calc.view` («Запрет» или снятая галочка
   в матрице) кнопка вела на экран «Нет доступа». Теперь гейт
   симметричен «Документации»: нет доступа — не отображается.
2. **Композит docs|library|kip-ios** (нижний бар + вкладка
   «Документация» десктопа) мог ПОКАЗЫВАТЬ кнопку роли, которой
   page-docs недоступен (гипотетический kipios без library.view) —
   клик вёл на «Нет доступа». Упрощён до `isAll || docs`:
   library и так влечёт docs (одна группа _LIBRARY_PAGES).
3. **Нижний бар целиком** — если ОБЕ кнопки скрыты («Запрет»),
   бар не висит пустой плашкой.

## Механизм (общее правило, а не точечные патчи)

- **Универсальный проход в `_applyRoleToUI` (начало):** ЛЮБОЙ
  элемент с `onclick="navigateTo('страница')"` виден ТОГДА И
  ТОЛЬКО ТОГДА, когда его пропустит сам `navigateTo` (`canAccess` —
  тот же гейт: спец-случаи админ-страниц и Electron-графиков
  учтены). Проход идёт ПЕРВЫМ и СИММЕТРИЧЕН (показ/скрытие при
  смене роли); спец-логика ниже может только ДОПОЛНИТЕЛЬНО
  скрыть (фильтр 4 ИТР ТОКЕМ, «Графики» вне Electron, пустые
  родительские страницы Task 115) — конфликтов показа нет.
  ИСКЛЮЧЕНИЕ: хлебные крошки `.breadcrumb-link` — контекстная
  навигация внутри уже доступной страницы.
- **Карта JS_NAV_TARGETS** — кнопки с onclick через
  addEventListener (входы КИП ИОС: Приборы/Блокировки/Клапана/
  Регуляторы/Проекты/Каб. журнал/План 114 + «Графики»): проход
  по атрибуту их не видит — явная карта «id → целевая страница»
  с тем же гейтом canAccess (страховочная сеть: живут внутри
  раздела, доступ к которому уже проверен).

## Изменённые файлы

- `index.html` — `_applyRoleToUI`: универсальный проход +
  JS_NAV_TARGETS (начало метода); нижний бар (calcBottomBtn
  гейт + docs-композит упрощён + скрытие бара без кнопок);
  вкладка «Документация» десктопа — композит упрощён;
- `sw.js` — v625;
- `tests/test-task397.js` (+14: SRC/VM/SW), адаптация
  `test-role-access.js` (docs-формула; +2 проверки калькуляторов
  и бара), бамп SW-ассертов (84 файла), `tests/run-all.js`;
- `scripts/task397-{patch,adapt-tests,bump-sw,browser-check}.py`.

## Проверки

- Тесты: **3701 passed / 0 failed** (3687 + 14).
- Браузер: **35/35** — 6 контекстов (матрица getMyAccess):
  «Запрет»-уровень (десктоп+мобайл — бар inline none/computed
  none, сайдбар пуст даже с раскрытыми группами, вкладки
  скрыты), только-calc (кнопка видна, бар восстановлен, клик
  ведёт на страницу — НЕ «Нет доступа»), Админ (всё видно,
  секретные кнопки после раскрытия), ИТР ТОКЕМ (фильтр 4 не
  перебит: «Кабельный журнал»/«Проекты» скрыты, «Приборы» виден,
  «Документация» видна через kipios.restricted); 0 JS-ошибок ×6.
- VLM (glm-5v-turbo) ×3 — пруфы `task397-proof-{zapret,admin,
  mobile(-click)}.png` (+ фокус-проверка бара).

## Серверные шаги

**НЕТ.** Apps Script не тронут (права берутся из уже работающей
матрицы `getMyAccess`; сервер по-прежнему гейтит каждый запрос —
клиентская видимость прав не даёт).

## После деплоя (пользователю)

Обновить PWA у клиентов: пункт «Обновить» в меню или повторное
открытие сайта (SW kipia-test-v625 подхватится автоматически).
