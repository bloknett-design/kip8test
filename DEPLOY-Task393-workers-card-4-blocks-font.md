# DEPLOY — Task 393: карточка работника — 4 блока + шрифт крупнее

**Дата:** 2026-09-22
**Репозиторий:** kip8test (тестовый PWA)
**SW:** kipia-test-v620 → **kipia-test-v621**
**Заявка:** «В картах работников нужно сделать шрифт текста больше, и разбить карты работников на
четыре блока: Галкин Д. Н. · таб. №2706 / Режим работы / Должность / Дата приёма / Правка данных… /
Уволить…; Отпуска · 2026 (Часть 1 / Часть 2 / + Отпуск…); Мероприятия · Сентябрь 2026 (нет
мероприятий в месяце / + Мероприятие…); СИЗ · средства индивидуальной защиты (записи / + СИЗ…).»

---

## ЧТО СДЕЛАНО (клиент — работает сразу после пуша)

### 1. Карточка работника на странице «Работники» — ЧЕТЫРЕ блока-окна
Прежде всё содержимое вкладки работника было ОДНИМ окном. Теперь — ЧЕТЫРЕ отдельных
окна-панели `.ws-wcard` (вертикальный стек, зазор 12px, у последнего — 0):

1. **Профиль**: «Галкин Д. Н. · таб. №2706», «Режим работы», «Должность», «Дата приёма»,
   «Комментарий» (если есть) + действия «Правка данных…» и «Уволить…»;
2. **«Отпуска · 2026»**: «Часть 1 / Часть 2 …» с ✎/✕ и строка «+ Отпуск…»;
3. **«Мероприятия · Сентябрь 2026»** (месяц/год шахматки): записи с ✎/✕ и «+ Мероприятие…»;
4. **«СИЗ · средства индивидуальной защиты»**: записи (✎/✕) и «+ СИЗ…».

Заголовки блоков 2–4 стали шапками панелей (подчёркнуты нижней линией, крупнее — 12px).
«Общая» вкладка (сводная таблица) и попап карточки у сетки шахматки — НЕ изменились.

### 2. Шрифт текста карточки — КРУПНЕЕ
| Элемент | Было | Стало |
|---|---|---|
| Шапка панели (ФИО · таб. №) | 11px | **15px** |
| Поля профиля и периоды отпусков | 12px | **14px** |
| Метка поля («Режим работы»…) | min-width 108px | **128px** |
| Заголовок блока (ws-popup-sec) | 10px | **12px** |
| Строки «Правка данных…»/«Уволить…»/«+ …» | 12px | **14px** |
| Пустые состояния («нет мероприятий…») | 11px | **13px** |
| Записи СИЗ (наименование) | 12px | **14px** |
| Мета-строка СИЗ (выдано · срок · до) | 10.5px | **12.5px** |
| Кнопки ✎/✕ | 22×22px | **26×26px** |

Панель: паддинг 14×16px (было 12×14), внутренние строки прижаты к краям окна.

### 3. Техническая реализация
- `_renderWorkerCard(tabNo, withEdit, asBlocks)` — общий рендер собирает ЧЕТЫРЕ блока
  (b1 профиль+действия / b2 отпуска / b3 мероприятия / b4 СИЗ): `asBlocks=true` →
  массив (страница), без флага → склеенная строка (попап шахматки — прежний вид);
- `_renderWorkerCardPanels(tabNo, withEdit)` — НОВЫЙ метод: каждый блок — отдельное
  окно `.ws-wcard` (вызывается из `_renderWorkersPage`);
- Шрифты — каскадом `.ws-wcard …` (попап `#wsEmpPopup` не внутри `.ws-wcard` —
  типографика попапа не тронута: 12px поля / 11px шапка);
- Мобильная версия (≤1023px): блоки стекаются в колонку, в границах экрана.

---

## СЕРВЕРНЫЕ ШАГИ — НЕТ
Изменения чисто клиентские (index.html). Apps Script не трогался: один бэкенд
обслуживает оба сайта, код не менялся — «Новая версия» НЕ требуется.

---

## ПРОВЕРКИ
- Тесты: **3621 passed / 0 failed** (было 3597; +24 — test-task393.js: структура
  блоков, CSS-размеры, VM строка/массив/изоляция блоков, «не найден», панели,
  страница, зритель, «Общая», SW);
- Адаптация 6 тестов (385/388/389/390/391/392): мок-панели, ассерты вызова,
  счётчик окон 1→4;
- Браузер-чек **19/19** (порт 9006, десктоп тёмная 1280 + мобайл 375 светлая):
  4 окна, состав по заявке, изоляция секций, шрифты 15/14/12/12.5px, зазоры,
  попап сетки — прежний вид, «Общая» — без окон, 0 JS-ошибок ×2;
- VLM ×1 (glm-5v-turbo): 4 панели читаемы, дефектов нет;
- CI ×4 success (Tests / Sync-to-desktop / Pages / pages-build); прод kip8test —
  SW kipia-test-v621, маркеры Task 393 в проде.

**Коммит:** cb4f6b0 (+ worklog-коммит) · **Пруфы:** task393-proof-workers.png,
task393-proof-grid-popup.png, task393-proof-mobile.png
