# DEPLOY — Task 390 (kip8test): «Общая» вкладка «Работников» — категории/выделенная шапка/примыкание/тёплые ярлыки

**Дата:** 2026-09-22 · **Коммит:** `99464ba` (rebase поверх авто-коммита `95d0786`) · **SW:** `kipia-test-v617` → `kipia-test-v618` (guard `v619`)

## Заявка

> В разделе Табель учёта рабочего времени / Работники, на общей вкладке убери «Сводка по всем работникам: сменных — 5, дневных — 7. Полная карточка каждого работника — на его вкладке.», переделай «На текущий момент: 12 работников (автоматический подсчёт). По штату: 14, из которых 2 мастера, 5 сменных и 7 дневных.» на «Работников по штату 14: 2 мастера; 7 дневных; 5 сменных.» и ниже строчкой «Работников на текущий момент: (N) мастера; (N) дневных; (N) сменных.», и выдели фон шапки с этими надписями и кнопкой добавить работника. Ярлыки должны примыкать к окну вкладок. В тёмной теме сделать цвета ярлыков с вкладками более светлее и другими по цвету ближе к светлой теме, чтобы они не сливались зрительно с общим фоном страницы.

## Что изменилось (КЛИЕНТ — только `index.html`, 11 правок)

1. **Сноска удалена** — «Сводка по всем работникам: сменных — N, дневных — N. Полная карточка каждого работника — на его вкладке.» (рендер в `_renderWorkersGeneral` + CSS-правило `.ws-wgen-note`).
2. **Шапка — две строки:**
   - `Работников по штату 14: 2 мастера; 7 дневных; 5 сменных.` — константа, ПЕРВОЙ строчкой (`.ws-wgen-staff`);
   - ниже `Работников на текущий момент: N мастера; N дневных; N сменных.` (`.ws-workers-count`) — **АВТОМАТИЧЕСКИЙ ПОДСЧЁТ ПО КАТЕГОРИЯМ**: мастера — по должности «Мастер КИПиА» (`_isMasterKipia`, толерантное сравнение как у фильтра шахматки), дневные/сменные — по типу **БЕЗ мастеров** (мастер проверяется первым, `else if` — категории не пересекаются, как структура штата 2+7+5); склонения `_plural`.
3. **Шапка ВЫДЕЛЕНА фоном** — `.ws-wgen-head`: тёмная `#1E2B42` + рамка `rgba(255,255,255,0.14)` + радиус 8px + паддинги; светлая `#E4E0D3` + рамка `rgba(0,0,0,0.14)`; кнопка «Добавить работника» внутри шапки (как Task 389).
4. **Примыкание** — `.ws-wtabs`: `padding-right: 10px` УБРАН (щель между ярлыками и окном вкладок); активный ярлык пристыкован (`margin-right: -1px`, правая рамка прозрачна).
5. **Тёмная тема ярлыков** — ТЁПЛЫЙ тон ближе к светлой теме, СВЕТЛЕЕ фона страницы `#1a2233` (не сливаются): неактивный `#4B4E46`, hover `#575A50`, активный `#63665B` (самый светлый — как активные вкладки браузеров), текст `rgba(255,255,255,0.78)`. **Светлая тема — без изменений** (`#E4E0D3`/`#DBD6C8`/`var(--bg-tertiary)`).

## Серверные шаги

**НЕТ** — чистый клиент (справочник кодов/сотрудников не менялся; Apps Script не трогался).

## Проверки

- Тесты: **3534/0** (`node tests/run-all.js`; +22 `tests/test-task390.js`, адаптации 385/388/389 — `scripts/task390-adapt-tests.py`).
- Браузер: **24/24** (`scripts/task390-browser-check.py`, порт 9002; мок 6 работников — 2 «Мастер КИПиА» с типами сменный/дневной: точные формулировки, старых строк нет, шапка `rgb(30,43,66)`+кнопка, ярлыки `rgb(75,78,70)`/актив `rgb(99,102,91)`, примыкание |щель|≤1, hover `rgb(87,90,80)`, шторка «Новый работник», светлая/мобайл, 0 JS-ошибок ×3).
- VLM ×3 (glm-5v-turbo): тёмная — шапка выделена, ярлыки тёплые серые не сливаются, примыкание, сноски нет; светлая — шапка бежевее панели, примыкание, активный подсвечен; мобайл — лента + строки + кнопка.
- CI ×4 success; прод: `CACHE_VERSION = 'kipia-test-v618'`, все маркеры Task 390 в живом `index.html`.

## Перенос в боевой kip8

См. `kip8/DEPLOY-Task390-transfer-from-test.md` (коммит `2df8aa0`, SW `kipia-v465` → `kipia-v466`, тесты 3540/0, браузер 24/24 — идентично).
