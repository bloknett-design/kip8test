# DEPLOY — Task 386: «Обозначения» (кнопка/два вида/мобильная страница), шапка «Работники», «Добавить работника»

**Дата:** 2026-09-21
**Репозиторий:** kip8test (тестовый PWA)
**SW:** kipia-test-v613 → **kipia-test-v614**
**Заявка (3 части):** «Кнопку легенда переименуй словом по проще, к примеру
"Обозначения" или как то по другому, и окно должно выезжать в сокращённом
виде обозначений, а при нажатии на значок с шевроном - шире и с подробным
наименованием кодов, а в мобильной версии переход на отдельную страницу. В
шапке столбцов работников переименуй "Работник +" в "Работники" и убери
функцию кнопки, просто надпись, кнопка теперь только в баре. На странице
работники переименуй кнопку "+" в "Добавить работника".»

---

## ЧТО СДЕЛАНО (клиент — работает сразу после пуша; серверных шагов НЕТ)

### 1. Кнопка «Обозначения» + два вида шторки + мобильная страница
- Кнопка ряда 2 бара переименована: «Легенда» → **«Обозначения»** (id
  `wsLegendBtn` и механика переключателя — без изменений).
- Шторка (десктоп) выезжает в **СОКРАЩЁННОМ виде**: узкая панель ~190px —
  свотч + код (Д, Д8, Н, ОТ…), БЕЗ наименований и пояснений.
- **Значок-шеврон #wsLgChv** на левом крае шторки (как «Ещё» у итогов,
  Task 329): клик — панель разворачивается **ШИРЕ** (min(400px, 45vw)) с
  подробными наименованиями кодов и секцией «Обозначения в шахматке»;
  повторный клик — назад к одним кодам. Анимация width 0.28s (слот и
  inner), стрелка доворачивается на 180°, `aria-pressed`/`aria-label`.
- **Мобильная версия: переход на отдельную страницу** `#page-ws-legend`
  («Обозначения», шапка с многострелочным шевроном — паттерн мобильных
  итогов Task 334); старый fixed-оверлей и класс `ws-legend-open`
  УДАЛЕНЫ, десктопная шторка на мобильном гасится (`display: none`).
  Страница показывает полный контент (наименования + пояснения).
- Контент вынесен в `_legendHtml` (шторка и страница рендерят одно и то
  же): коды дней Т-12/Т-13 и коды мероприятий из справочника
  «Коды_статусов» (живые свотчи); пояснения — в свёртываемом блоке
  `.ws-lg-notesec`.
- **Исправлен латентный баг Task 385:** замер ширины inner через
  `getBoundingClientRect` при свёрнутом слоте ловил flex-сжатие inner до
  min-content — шторка открывалась урезанной (~186px). Теперь
  `.ws-legend-inner { flex: none; width: 190px }` + класс `.ws-lg-wide`
  (min(400px, 45vw)), ширины задаются явно (`_legendWidthPx`).
- Взаимоисключение с «Итогами учёта» и закрытие по Esc — сохранены.

### 2. Шапка столбца сетки — «Работники» (просто надпись)
- Заголовок `th` — надпись **«Работники»** (сужение при горизонтальной
  прокрутке — «Рабо», прежний span-обмен Task 335).
- Функция кнопки СНЯТА: нет `onclick`, класса `ws-emp-head-add`,
  плюсика-индикатора и их CSS-правил (hover Task 318/330, узкий вид
  Task 335). Переход на страницу «Работники» — ТОЛЬКО кнопкой «Работники»
  в баре (ряд 1, редакторам).

### 3. Страница «Работники» — кнопка «Добавить работника»
- Значок «+» в шапке страницы заменён текстовой кнопкой
  **«Добавить работника»** (высота прежние 34px, авто-ширина, нейтральный
  стиль; открывает ту же шторку создания — «Новый работник»).
- Пустое состояние: «Добавьте первого кнопкой «Добавить работника»…».

---

## РАЗВЁРТЫВАНИЕ

1. **Git push** в `main` kip8test — GitHub Pages задеплоит сайт, CI
   прогонит тесты и синхронизирует десктоп-зеркало (автоматически).
2. **Сервис-воркер:** версия поднята `kipia-test-v613 → kipia-test-v614` —
   при следующем открытии приложения клиент обновится сам (перезагрузить
   страницу после сообщения об обновлении).
3. **Сервер (Google Apps Script): шагов НЕТ** — задача чисто клиентская.
   Backend-контракты не менялись.

⚠️ Открытый пункт из Task 384 остаётся в силе (если ещё не выполнен):
ручной деплой Apps Script — `Code.gs` (2 case doPost) + `WorkSchedule.gs`
(updateEmployee/updateVacation) + «Новая версия» — иначе кнопки
«Сохранить» правок данных/отпусков на странице «Работники» вернут
«Unknown action» в тосте. Задача 386 этого не касается.

---

## ПРОВЕРКА

- Тесты: **3414 passed / 0 failed** (было 3379; +35 `test-task386.js`,
  адаптации в 9 файлах — см. `scripts/task386-adapt-tests.py`).
- Браузер (Playwright, порт 8997): **54/54** — десктоп/зритель/мобайл,
  включая: узкий вид ~190px без наименований → шеврон → широкий ~400px с
  наименованиями → сворачивание; Esc; взаимоисключение; страница
  `#page-ws-legend` на мобильном (шторка погашена, шеврон «Назад»);
  клик по надписи «Работники» не уводит с табеля; «Добавить работника» →
  «Новый работник»; 0 JS-ошибок ×3.
- VLM (glm-5v-turbo) ×4: узкая шторка только с кодами; широкая с
  наименованиями и пояснениями; мобильная страница с полным контентом;
  текстовая кнопка «Добавить работника».

**Файлы:** `index.html` (26 правок — `scripts/task386-patch.py`),
`sw.js` (v614), `tests/test-task386.js` (+35), адаптации тестов
(`scripts/task386-adapt-tests.py`), `scripts/task386-browser-check.py`,
`scripts/task386-bump-sw.py`.
