# Task 395 — кнопка «Работники» по матрице доступа + фон/рамки блоков + десктоп-колонки

**Дата:** 23.09.2026
**Репо:** kip8test (тестовый сайт)
**SW:** kipia-test-v622 → **kipia-test-v623**
**Коммит:** feat: Task 395 (index.html + tests)

## Что сделано (заявка, 3 части)

1. **Кнопка «Работники» — матрица доступа ролей.** Пользователям
   БЕЗ доступа к разделу «Табель учёта рабочего времени» (уровень
   `null`) и с ОГРАНИЧЕННЫМ просмотром (`min`) кнопка «Работники»
   в ряду 1 тулбара табеля НЕ отображается. Редакторам (`edit`) и
   зрителям (`view`) — видна; у зрителей страница «Работники»
   открывается в read-only (без «Правка данных…»/«Уволить…»/
   «+ Отпуск…»/«+ Мероприятие…»/«+ СИЗ…» и «Добавить работника»).
   Гейт перехода `openWorkersPage` и прямой заход по URL
   (`_renderWorkersPage`) защищены тем же правилом (null/min —
   тело пустое). Легаси-запас: если уровень ещё не посчитан —
   производная от `_canEdit` (как до трёхуровневой схемы).
2. **Фон и рамки блоков карточек.** Фон панелей больше НЕ
   сливается с общим фоном окна: тёмная тема — #243349 (светлее
   фона страницы #1a2233; прежний #0e1621 был темнее и сливался),
   светлая — #ffffff (поверх кремового #FAF9F5). Рамки — толще
   (2px вместо 1px) и ярче, стиль «выступающего вверх бордюрчика»:
   верхняя кромка светлее боков (блик «света сверху») + тень снизу
   (окно приподнято над страницей) в обеих темах.
3. **Десктоп-раскладка карточки (≥1024px).** Вместо сетки 2×2
   (Task 394) — ДВЕ равные колонки на всю ширину окна вкладок:
   ЛЕВАЯ — профиль, ПОД ним блок отпуска, под отпусками блок
   мероприятий; ПРАВАЯ — блок СИЗ в ВЕРХНЕЙ ПРАВОЙ части экрана
   (не тянется по высоте левой). Мобайл ≤1023px — прежний
   вертикальный стек (профиль → отпуска → мероприятия → СИЗ).

## Изменённые файлы

- `index.html` — JS: init/_onRoleUpdate (видимость кнопки),
  openWorkersPage/_renderWorkersPage (гейты edit/view),
  _renderWorkerCardPanels (колонки .ws-wcol/.ws-wcol-ppe);
  CSS: .ws-wcard (фон/рамка/кромка/тень, обе темы),
  .ws-wgrid2 .ws-wcol + @media ≥1024px (flex-колонки);
- `sw.js` — v623;
- `tests/test-task395.js` (+18), адаптация test-task311/385/394,
  бамп SW-ассертов (82 файла), `tests/run-all.js`;
- `scripts/task395-{patch,adapt-tests,bump-sw,browser-check}.py`.

## Проверки

- Тесты: **3666 passed / 0 failed**.
- Браузер: **31/31** — 5 контекстов (edit / view / min / null /
  мобайл-светлая): видимость кнопки по матрице, read-only у
  зрителя, пустое тело при прямом URL у min/null, экран «нет
  доступа» к разделу при null, колонки и порядок блоков,
  вычисленные стили (фон/2px/кромка/тень) в обеих темах,
  мобильный стек, 0 JS-ошибок ×5.
- VLM (glm-5v-turbo) ×3 — пруфы `task395-proof-*.png`.

## Серверные шаги

**НЕТ.** Apps Script не тронут (права берутся из уже работающей
матрицы `getMyAccess`; серверный деплой Task 392 выполнен
пользователем ранее).

## После деплоя (пользователю)

Обновить PWA у клиентов: пункт «Обновить» в меню или повторное
открытие сайта (SW kipia-test-v623 подхватится автоматически).
