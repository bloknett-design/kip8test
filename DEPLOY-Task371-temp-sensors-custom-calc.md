# DEPLOY — Task 371 (kip8test)

## Заявка пользователя

> «На странице Главная / Инженерные калькуляторы / КИП и А /
> Датчики температуры, под блоком Таблица значений добавь блок
> Расчёт произвольных значений, по примеру как в разделе
> Шкала-сигнал.»

## Что изменилось (только клиент index.html + sw.js)

### 1. Блок «Расчёт произвольных значений» (страница «Датчики температуры»)
Под «Таблицей значений» в ОБЕИХ ветках расчёта (`calcTempSensor()`):
- **ТС (термометры сопротивления)** — поля «Температура (°C)» ↔
  «Сопротивление R(t), Ом»;
- **ТП (термопары)** — поля «Температура (°C)» ↔ «Термо-ЭДС E(t), мВ».

Разметка и поведение повторяют `scaleCustomCalcPanel` раздела
«Шкала-сигнал»: заголовок «Расчёт произвольных значений», подсказка
«Введите значение в любое поле — другое рассчитается автоматически»,
живой двусторонний пересчёт при вводе (`oninput`), очистка парного
поля при пустом/нечисловом вводе, панель появляется вместе с
результатами после «Рассчитать» и перерисовывается при повторном
расчёте.

### 2. Логика расчёта (новые функции)
- `getRtdSensorMap()` — карта типов ТС, вынесена из `calcTempSensor()`
  (единый источник, литерал карты в файле один);
- `tempCustomCalcHtml(valLabel, valPlaceholder)` — HTML панели
  (единый для ТС/ТП);
- `getTempSensorRange()` — диапазоны НСХ: Cu −50…200 °C,
  Pt −200…850 °C, ТП по типам (K −270…1372, J −210…1200,
  T −270…400, N −270…1300, E −270…1000, R/S −50…1768, B 0…1820);
- `tempCalcForwardValue(t)` — R(t)/E(t) по ТЕКУЩЕЙ форме (как
  `queryFromScaleLive` читает поля живьём — смена типа датчика
  применяется и к произвольным значениям);
- `tempCalcInvertValue(v)` — обратная задача (температура по R/E)
  бисекцией по монотонной НСХ: аналитически не обращается
  (у Pt ниже 0 °C член C, у ТП — полиномы высоких степеней);
- `tempQueryFromTemp()` / `tempQueryFromValue()` — живой расчёт
  в обе стороны; значения вне НСХ → тост «… вне диапазона НСХ
  (min … max)» с границами и очистка парного поля.

### 3. Service Worker
`CACHE_VERSION`: `kipia-test-v599` → `kipia-test-v600`.

## Серверные действия

**НЕ ТРЕБУЮТСЯ.** Все правки клиентские; страница «Датчики
температуры» — чистый клиентский калькулятор без обращения
к серверу.

## Тесты

- `tests/test-task371.js` +32 (SRC: панель в обеих ветках ПОСЛЕ
  таблицы, разметка «как Шкала-сигнал», рефакторинг карты,
  диапазоны НСХ, бисекция, тосты; VM: Cu50 55↔61,77 /
  границы 39,3–92,8, Pt100 55→121,32 / −50→80,3063 (член C) /
  100→138,51, Pt1000 100→1385,05, ТП K 400↔16,3971 /
  −100↔−3,5536, B 1000↔4,8343, T −100, вне НСХ → тост+очистка,
  пустой ввод → очистка; VM calcTempSensor: полный рендер
  таблица+панель в обеих ветках; SW v600).
- Прогон: **3012 passed / 0 failed** (2980 + 32).
- Браузер `scripts/task371-browser-check.py` — **24/24** (порт 8972;
  десктоп тёмная: Cu50 55→61,77, 92,8→200, 250 → тост+очистка,
  Pt100 −50→80,3063, 138,5→≈100, ТП K 400→16,3971, −3,5531→≈−100,
  B 1000→4,8343, 1821 → тост; панель строго ПОД таблицей, подписи
  полей по веткам; светлая: читаемость, расчёт; мобайл 375: панель
  не переполняет ширину; 0 JS-ошибок).
  VLM-пруфы: `scripts/task371-proof-{desktop,light,mobile}.png`
  — подтверждены (блок, значения, отсутствие дефектов).

## Откат

```bash
git revert <hash-коммита-Task-371>   # клиент + SW; серверных правок нет
```

## Перенос в боевой kip8

По заявке пользователя, после приёмки. Только клиентская часть
(index.html зона Task 371 + SW-бамп kipia-v447 → следующая),
маппинг версий в тестах: kipia-test-v600 → kipia-v448+.
