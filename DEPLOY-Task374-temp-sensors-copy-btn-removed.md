# DEPLOY — Task 374 (kip8test)

## Заявка пользователя

> «Убери кнопку "Копировать" над расчётными таблицами. И переноси все
> изменения в боевой kip8.»
> (Первая часть — этот деплой; вторая — перенос пакета Tasks 371–374
> в репозиторий kip8, отдельный коммит там.)

## Что изменилось (только клиент index.html + sw.js)

### Кнопка «Копировать» над таблицами значений (Датчики температуры)
В `calcTempSensor()` — ОБЕ ветки расчёта:
- **ТС (термометры сопротивления)** — таблица R(t), Ом;
- **ТП (термопары)** — таблица E(t), мВ.

Удалены flex-обёртка и кнопка `copyCalcTable('tempTableContainer')`
из заголовка «Таблица значений (шаг X°C)». Заголовок сохранён,
отступ сверху 12px перенесён на сам заголовок:

```html
<!-- было -->
<div style="display:flex; justify-content:space-between; …">
  <div class="converter-result-label-title">Таблица значений (шаг X°C)</div>
  <button … onclick="copyCalcTable('tempTableContainer')">Копировать</button>
</div>
<!-- стало -->
<div class="converter-result-label-title" style="margin-top:12px;">
  Таблица значений (шаг X°C)
</div>
```

### Что НЕ тронуто (важно)
- `copyCalcTable()` — функция осталась: её использует раздел
  «Весоизмерительные датчики» (3 вызова `wsTableContainer`);
- кнопка «Копировать» в «Шкала-сигнал» (`copyScaleTable`);
- кнопка «Копировать» в «Буйковом уровнемере» (`copyBuoyTable`).

Заявка относится к таблицам раздела «Датчики температуры» —
карточная страница (Task 372) и страница датчика.

### Service Worker
`CACHE_VERSION`: `kipia-test-v602` → `kipia-test-v603`.

## Серверные действия

**НЕ ТРЕБУЮТСЯ.** Правка клиентская; «Датчики температуры» — чистый
клиентский калькулятор без обращений к серверу.

## Тесты

- `tests/test-task374.js` +12 (SRC: кнопка и вызов удалены из обеих
  веток, заголовок с отступом, flex-обёртка убрана, колонки R(t)/E(t)
  не тронуты, copyCalcTable/wsTableContainer×3/copyScaleTable/
  copyBuoyTable на месте, зона раздела без кнопок; VM: живой расчёт
  ТС 50М — таблица+заголовок+R(50)=60,7 без кнопок, ТП ТХК (L) —
  E(100)=6,8617 без кнопок, ТП ТХА (K) — E(100)=4,096; SW v603
  guard v604);
- SW-бамп: ассерты v602→v603, guard-ы v603→v604 по всем 62 тест-файлам;
- `tests/run-all.js` +1 require.
- Итог: **3090 passed / 0 failed**.

## Браузерная проверка (scripts/task374-browser-check.py, порт 8975)

21/21: десктоп тёмная — ТС 50М и ТП ТХК (L): результаты показаны,
таблица и заголовок есть, «Копировать» нет, кнопок в результатах 0,
значения корректны; «Шкала-сигнал» — своя кнопка на месте; светлая —
ТХА (K) без кнопки, E(100)=4,096; 0 JS-ошибок в обоих контекстах.
VLM-пруф: скриншот desktop-tc — «кнопки Копировать нет, заголовок
читаем».

## Откат

```bash
git revert <commit>   # index.html + sw.js + tests
```
Или точечно: вернуть кнопку в 2 местах `calcTempSensor()` и снизить
`CACHE_VERSION` обратно (v602).
