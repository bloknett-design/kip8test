# DEPLOY — Task 381 (kip8test)

## Заявки пользователя

1. «Фон итогов верни как был до белого.»
2. «При прокрутки текста в свёрнутых окнах мероприятий и норм, значки
   раскрытия должны оставаться на месте.»
3. «Окно «Мероприятия» — не каждая строка мероприятия должна иметь фоновую
   «плашку» во всю ширину окна, цвет зависит от даты, а общий фон всего
   пространства окна на уровне данного текста должен быть закрашен
   соответствующим цветом.»

## Что изменилось (только клиент, index.html — серверной части НЕТ)

### 1. Итоги учёта — фон «как до белого» (возврат Task 378)

Фон ячеек значений таблиц итогов (`td.ws-tt-num`, таблицы месяца/года)
снова **ПРОЗРАЧНЫЙ** — сквозь ячейки виден фон панели итогов
(`var(--bg-tertiary)` тёмная / `#e9e7de` светлая) и **зебра чётных строк**
(восстановлена Task 322):

| Правило | Статус |
|---|---|
| `[data-theme="light"] .ws-tt-table tbody td.ws-tt-num { background: #FFFFFF; }` | УДАЛЕНО |
| `[data-theme="dark"] .ws-tt-table tbody td.ws-tt-num { background: #eef0f2; color: #141413; filter: brightness(0.88); }` | УДАЛЕНО |
| `[data-theme="dark"] .ws-tt-table td.ws-tt-hours { color: #1d7a37; }` | УДАЛЕНО (палитра светлой была нужна только под светлые ячейки) |
| `[data-theme="dark"] .ws-tt-table td.ws-tt-over { color: #a06a13; }` | УДАЛЕНО (аналогично) |
| `.ws-tt-table tbody tr:nth-child(even) { background: rgba(255,255,255,0.09); }` | ВОССТАНОВЛЕНО (Task 322) |
| `[data-theme="light"] .ws-tt-table tbody tr:nth-child(even) { background: rgba(0,0,0,0.07); }` | ВОССТАНОВЛЕНО (Task 322) |

- «Часы»/«Переработка» снова базовая палитра тем: тёмная — #4ac771/#e0a23c,
  светлая — #1d7a37/#a06a13.
- НЕ тронуты (остаются от Task 378): нули → пустые ячейки, линии как в
  основной шахматке (rgb(10,15,23)/rgba(0,0,0,0.30), сталь шапки/«Сотрудника»),
  яркость перекрестья = «сегодня» (0.30/0.22), подсветка строк при наведении
  на ФИО и ячейки итогов.
- НЕ тронута шахматка Task 379: пустые ячейки значений светлой темы #FFFFFF.

### 2. Значки раскрытия окон — «приколоты» при прокрутке

Значок `.ws-bar-exp` — `position: absolute` ВНУТРИ скроллера и вместе с
прокручиваемым слоем уезжал вверх. Компенсация (JS `_barExpSync`):

```js
if (!el._barExpPin) {
    el._barExpPin = true;
    el.addEventListener('scroll', function() {
        var b = el.querySelector('.ws-bar-exp');
        if (b) b.style.transform = 'translateY(' + el.scrollTop + 'px)';
    });
}
btn.style.transform = 'translateY(' + el.scrollTop + 'px)';
```

- Значок висит в правом нижнем углу видимой грани окна ВСЕГДА — сколько
  текста ни крути (колесо/свайп/клавиши ↑↓), в обоих окнах («Мероприятия»
  и «Нормы»).
- Слушатель вешается ОДИН раз на элемент окна (флаг `_barExpPin` — рендеры
  окон заменяют innerHTML, но не сам элемент); `_barExpSync` (пере)создаёт
  значок после рендеров — transform восстанавливается с текущим scrollTop.
- РАСКРЫТОЕ окно не скроллится (высота = scrollHeight) — transform = 0;
  у значка transition только на opacity — движение мгновенное, без лагов.
- Раскрытие/габарит бара (Task 378/379) не тронуты.

### 3. Окно «Мероприятия» — общий фон окна вместо плашек по строкам

```css
.ws-ep-item { padding: 3px 10px; margin: 0 -10px; background: rgba(0,0,0,0.45); }
.ws-events-panel { gap: 0; }
.ws-events-panel > * + * { margin-top: 3px; }
.ws-events-panel .ws-ep-item + .ws-ep-item { margin-top: 0; }
```

- Строка расходится **до краёв окна** (margin 0 -10px гасит горизонтальные
  паддинги панели 10px — текст на прежнем месте), соседние строки
  **склеиваются** (gap: 0 + margin-top: 0) — строки одного срока сливаются
  в **сплошную закраску общего фона окна**; видимый край цвета остаётся
  только на СТЫКЕ прошедших ↔ идущих-будущих.
- Вертикальный ритм сохранён: 3px сверху/снизу строки (прежние 2px+3px
  gap+2px), от заголовка и до значка — 3px (заголовок имеет свой
  margin-bottom 2px → итоговые 5px как прежде).
- Тинты Task 380 НЕ тронуты: прошедшие светлее окна
  (rgba(255,255,255,0.12)/0.55), идущие-будущие темнее
  (rgba(0,0,0,0.45)/0.12).
- Окно «Нормы» (.ws-cal-panel) НЕ тронуто — там свои отступы.

## Проверки

- Тесты: `node tests/run-all.js` → **3264/0** (+19 test-task381.js,
  адаптации test-task378/379/322/321/380 — mоки `mkPanel` получили
  `scrollTop`/`addEventListener`).
- Браузер (порт 8990, десктоп 1280 обе темы + мобайл 375): **63/63** —
  computed (прозрачные ячейки, зебра 0.09/0.07, цвета колонок, линии,
  панель #e9e7de), склейка строк (зазор ≤ 0.75px, ширина = clientWidth,
  стык зон), значок на месте (программный скролл/колесо/конец/возврат,
  transform), раскрытие и маржа бара, нулей нет, 0 JS-ошибок.
- Пиксельные пруфы: тёмная — past (43,50,60) непрерывен, upcoming (7,12,18)
  непрерывен, окно (14,22,33); светлая — (245,244,240)/(204,203,195) на
  (233,231,222); итоги — нечёт = фон панели, чёт = зебра
  (35,43,53)/(216,214,206), НЕ белый.
- VLM: бинарные кропы зон — «верхняя (прошедшие) светлее» в обеих темах ✓;
  итоги — «тёплый серо-кремовый, не белый, зебра есть» (светлая),
  «тёмный фон, зебра есть» (тёмная).

## Развёртывание

- SW: `kipia-test-v609` → `kipia-test-v610` (кэш обновится сам).
- Серверных шагов НЕТ — чистый клиентский CSS/JS.
- GitHub Pages задеплоится пушем в main (CI: Tests → Pages → sync).
