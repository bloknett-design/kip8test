# -*- coding: utf-8 -*-
# Обновление системного промта kip8test до post-Task 325
# (ширина колонки сотрудников — ПО ТЕКСТУ В САМОМ ШИРОКОМ; кнопки
#  «Сформировать»/«Сохранить (N)»/«Отменить» — ТРЕТЬЕЙ строкой ПОД
#  «Итоги учёта»/«Месяц»/«Год»; ✕ шапки шторки УДАЛЁН — закрытие
#  кнопкой тулбара (мобайл — тап мимо); в таблице итогов — ТОНКИЕ
#  ВЕРТИКАЛЬНЫЕ ЛИНИИ разделения ячеек)
with open('Системный_промт_для_приложения_КИПиА.md', encoding='utf-8') as f:
    lines = f.readlines()

new3 = '''> **Версия документа:** 2026-09-05 (post-Task 325: КОЛОНКА СОТРУДНИКОВ ПО САМОМУ ШИРОКОМУ ТЕКСТУ, ТРИ РЯДА КНОПОК, ШТОРКА БЕЗ ✕, ВЕРТИКАЛЬНЫЕ ЛИНИИ ЯЧЕЕК ИТОГОВ (заявка: «Ширину столбца сотрудников сделать по ширине текста в самом широком. Кнопки "Сформировать", "Сохранить (N)", "Отменить" разместить в третьей строке под кнопками "Итоги учёта", "Месяц" и "Год". Плюс ✕ в шапке шторки убери. В таблице итогов учёта сделай тонкие вертикальные линии разделения ячеек.». ЗАМЕТКА: настоящий промт — post-Task 325, НО строка версии post-Task 324 ниже описывает УСТАРЕВШУЮ геометрию рядов/шапки (один нижний ряд #wsBottomRow + ✕) — актуальное см. в буллете Task 325 раздела «Ключевые фичи»): 1) КОЛОНКА «СОТРУДНИК» ШАХМАТКИ — width var(--ws-emp-w, 180px/200px): JS-замер _measureEmpCol (Range-API: th «Сотрудник +» + .ws-emp-name [№+ФИО] + .ws-emp-pos [должность] каждой строки, эллипсис не искажает; max + паддинги th + 2px → переменная на #page-work-schedule; вызов из _renderGrid ДО _fitGrid + document.fonts.ready; скрытая страница max<40/нет gridWrap/Range — тихий выход, то же значение — без записи); десктоп fixed-раскладка th/td.ws-emp-col width var(--ws-emp-w, 200px) (кап max-width:180px и ровные 200px УДАЛЕНЫ, эллипсис — страховка); ТАБЛИЦА ИТОГОВ (.ws-tt-emp, год/мобайл): капы 220px/150px + text-overflow ellipsis УДАЛЕНЫ — авто-ширина по самому широкому ФИО (№ + имя + «архив»). 2) ТРИ РЯДА КОЛОНОКИ КНОПОК: ряд 1 #wsSelectsRow (селекты + «Обновить»); ряд 2 #wsTotalsRow (.ws-totals-row) — «Итоги учёта» + «Месяц»/«Год»; ряд 3 #wsActionsRow (.ws-actions-row, НИЖНИЙ) — «Сформировать» → «Сохранить (N)» → «Отменить» — действия ПОД итогами (заявка); общий ряд Task 324 #wsBottomRow и разделитель .ws-tb-sep УДАЛЕНЫ; десктоп ТРИ ряда × calc((95px−6px)/3)≈29,7px + два зазора 3px = 95px; скрытие кнопок — по праву/правкам, ряды всегда на виду. 3) ✕ ШАПКИ ШТОРКИ #wsTtClose/.ws-tt-close УДАЛЁН (заявка): единственный переключатель — кнопка «Итоги учёта» (aria-pressed); МОБАЙЛ — закрытие ТАПОМ МИМО шторки: document click в _attachTotalsSync (только <1024px, клики по #wsTotalsDrawer/#wsTotalsRow не закрывают); пустая шапка (⚠ и «Обновить» скрыты — месяц) — _updateTtHead (из _applyTtHeadVar, ДО замера) ставит класс ws-tt-head-empty → min-height 16px-филлер (зона ≥ шапки сетки — строки итогов ровно по строкам сетки). 4) ТАБЛИЦА ИТОГОВ — ТОНКИЕ ВЕРТИКАЛЬНЫЕ ЛИНИИ: .ws-tt-table th/td border-right: 1px solid (цвет = горизонтальным: rgba(255,255,255,0.07) тёмная / rgba(0,0,0,0.08) светлая); th:last-child/td:last-child — border-right: none. Тесты: 1902 → 1928/0 (+24 tests/test-task325.js; флейк-фикс tests/extract-functions.js — setInterval песочницы заглушка: реальный интервал _auditScrollState 3с переживал eval и валил прогон). Верификация: task325-browser-check.py 27/27 (порт 8945: три ряда 29,67px/95px, --ws-emp-w = max текста + паддинги [164px], ФИО без эллипсиса, ✕ нет + пустая шапка 16px (месяц)/«Обновить» (год), пол-области, вертикальные линии computed + последняя без линии, строки top ≤2,5px, колонка итогов по тексту (год/мобайл), закрытие кнопкой/тапом мимо, «Сохранить (1)» в ряду 3; светлая; мобайл 375; 0 JS-ошибок) + регресс браузером 306 (I2 актуализирован: Сформировать в #wsActionsRow) 29/29, 311 24/24, 313 30/30, 314 36/36, 315 33/33, 317 49/49, 321 45/45, 322 45/45, 323 45/45, 324 49/49; VLM ×4 (линии на мелких скринах — ложные негативы, опровергнуты computed + кропы 2×). ⚠️ ДЕПЛОЙ: ТОЛЬКО GitHub Pages + Ctrl+Shift+R ×1–2 (SW kipia-test-v563→v564) — Apps Script/листы НЕ трогать. ПЕРЕНОС В kip8: НЕ выполнен — Tasks 315–325 одной партией после проверки (k8 v417→v418) — см. scripts/DEPLOY-Task325-empcol-rows-crosslines.md)
'''

assert lines[2].startswith('> **Версия документа:**')
lines.insert(2, new3 if new3.endswith('\n') else new3 + '\n')
print('Строка 3 вставлена (цепочка версий)')

src = ''.join(lines)

reps = [
    ('> **Текущая версия кэша:** `kipia-test-v563`',
     '> **Текущая версия кэша:** `kipia-test-v564`'),
    ('| `kip8test` | PWA | `kipia-test-v563` |',
     '| `kip8test` | PWA | `kipia-test-v564` |'),
]
for old, new in reps:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Версии кэша обновлены (2 замены)')

# Счётчики тестов
reps2 = [
    ('├── tests/                      # 1902 юнит-тестов (Node.js, 42 файлов): run-all.js, test-role-access.js, и др.',
     '├── tests/                      # 1928 юнит-тестов (Node.js, 43 файлов): run-all.js, test-role-access.js, и др.'),
    ('ожидается `1902 passed, 0 failed`', 'ожидается `1928 passed, 0 failed`'),
    ('# Ожидается: 1902 passed, 0 failed', '# Ожидается: 1928 passed, 0 failed'),
]
for old, new in reps2:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Счётчики тестов обновлены (3 замены)')

# Буллет Task 325 — после буллета Task 324
entry = ('\n- **Табель учёта рабочего времени — ИТОГИ УЧЁТА: колонка сотрудников по самому широкому тексту, ТРИ ряда кнопок, шторка без ✕, вертикальные линии ячеек (Task 325)**: '
         'по заявке пользователя: «Ширину столбца сотрудников сделать по ширине текста в самом широком. Кнопки "Сформировать", "Сохранить (N)", "Отменить" разместить в третьей строке под кнопками "Итоги учёта", "Месяц" и "Год". Плюс ✕ в шапке шторки убери. В таблице итогов учёта сделай тонкие вертикальные линии разделения ячеек.». 1) КОЛОНКА «СОТРУДНИК» ШАХМАТКИ — ШИРИНА ПО ТЕКСТУ В САМОМ ШИРОКОМ (заявка): _measureEmpCol — JS-замер НАТУРАЛЬНОЙ ширины текста через Range-API (range.selectNodeContents(el).getBoundingClientRect().width — эллипсис обрезки НЕ искажает замер): заголовок th.ws-emp-col («Сотрудник +»), для каждой строки — блок .ws-emp-name (№ таб. + ФИО одной строкой) и .ws-emp-pos (должность; шире ФИО — берётся она); максимум + ГОРИЗОНТАЛЬНЫЕ ПАДДИНГИ th (getComputedStyle, фолбэк 20) + 2px запас (border-collapse + субпиксель) → Math.ceil → CSS-переменная --ws-emp-w на #page-work-schedule (style.setProperty; то же значение — БЕЗ перезаписи). Правила: база .ws-grid thead th.ws-emp-col { min-width:130px; width: var(--ws-emp-w, 180px) } (прежде max-width:180px — кап УДАЛЁН); десктоп fixed-раскладка #page-work-schedule .ws-grid thead th/tbody td.ws-emp-col { width: var(--ws-emp-w, 200px) } (прежде ровно 200px; эллипсис td остался страховкой субпиксельности/смены шрифта). ВЫЗОВЫ: _renderGrid — this._measureEmpCol() ДО this._fitGrid() (раскладка одного кадра), init — document.fonts.ready.then(повторный замер: поздний веб-шрифт меняет ширину текста; try/catch). ЗАЩИТЫ: скрытая страница (все rect=0, max<40) — прежнее значение/фолбэк НЕ трогается; нет gridWrap/querySelector/createRange — тихий выход. ТАБЛИЦА ИТОГОВ (колонка .ws-tt-emp — вкладка «Год» и мобайл): капы max-width 220px (десктоп)/150px (мобайл) и overflow:hidden+text-overflow:ellipsis УДАЛЕНЫ — авто-раскладка, колонка растягивается под самое широкое ФИО (№ + имя + «архив»), широкая таблица прокручивается ползунком шторки (Task 324). 2) ТРИ РЯДА КОЛОНКИ КНОПОК (заявка: действия — ТРЕТЬЕЙ строкой ПОД итогами): ряд 1 #wsSelectsRow (.ws-toolbar-row) — селекты месяца/года + «Обновить»; ряд 2 #wsTotalsRow (.ws-toolbar-row .ws-totals-row) — кнопка-переключатель «Итоги учёта» #wsTotalsBtn (aria-pressed) + вкладки «Месяц»/«Год» #wsTtTabMonth/#wsTtTabYear (Task 324); ряд 3 #wsActionsRow (.ws-toolbar-row .ws-actions-row, НИЖНИЙ) — «Сформировать» #wsGenerateBtn (право записи, init) → «Сохранить (N)» #wsSaveBtn (счётчик правок _updateSaveBtn) → «Отменить» #wsCancelBtn (обе — при наличии правок); общий нижний ряд Task 324 #wsBottomRow/.ws-bottom-row и РАЗДЕЛИТЕЛЬ .ws-tb-sep (HTML+CSS+десктопное правило) УДАЛЕНЫ — блоки итогов и действий теперь в РАЗНЫХ рядах; _updateSaveBtn/init трогают только КНОПКИ (ряды всегда на виду — сетка под баром не прыгает); ДЕСКТОП ≥1024px: .ws-toolbar-main height 95px (как окна), .ws-toolbar-row height calc((95px − 6px)/3) ≈ 29,67px + ДВА зазора 3px (прежде Task 324: два ряда × 46px; формула calc((95−3)/2) удалена), кнопки/селекты height 100% (padding 4px 12px, 14px текст входит). 3) ✕ ШАПКИ ШТОРКИ УДАЛЁН (заявка): #wsTtClose/.ws-tt-close (HTML-кнопка, CSS-правила базовое + hover + светлая + мобайл 44px, aria-label) — ПОЛНОСТЬЮ из файла; ЕДИНСТВЕННЫЙ переключатель шторки — кнопка «Итоги учёта» в ряду 2 (toggleTotals, aria-pressed). МОБАЙЛ (<1024px, шторка fixed-оверлей ПОВЕРХ тулбара): закрытие — ТАП/КЛИК МИМО ШТОРКИ (стандартное поведение drawer-панелей): в _attachTotalsSync добавлен document.addEventListener(\'click\'): if (!self._totalsOpen) return; if (!matchMedia(\'(max-width: 1023px)\').matches) return (десктоп НЕ затронут); клики по #wsTotalsDrawer (внутри шторки) и #wsTotalsRow (кнопка «Итоги учёта» закрывает сама — onclick кнопки отрабатывает РАНЬШЕ document-слушателя, _totalsOpen уже false — двойного тогла НЕТ) — игнорируются (ev.target.closest); иначе self.toggleTotals(). ПУСТАЯ ШАПКА ШТОРКИ (обычный месяц: ⚠ #wsTtWarn скрыта, «Обновить» #wsTtRefresh только у года): _updateTtHead (НОВЫЙ, вызывается из _applyTtHeadVar ДО замера высоты зоны — покрывает открытие/смену вкладки/рендеры с ⚠) — head.classList.toggle(\'ws-tt-head-empty\', оба скрыты); CSS .ws-tt-head.ws-tt-head-empty { min-height: 16px; padding-top: 2px; padding-bottom: 0 } — ЭТО ВЫРАВНИВАТЕЛЬ: шапка сетки (2 строки: дата + д.н.) ≥ природных ~37px, шапка таблицы итогов ~22,5px, пустые 16px доводят заголовочную зону панели до высоты шапки сетки — строки итогов идут РОВНО по строкам шахматки (заявка Task 323), var = зона ≥ природной — сетка растёт к зоне, а не наоборот; год («Обновить» виден) и сбои (⚠) — шапка обычная ≥28px. 4) ТОНКИЕ ВЕРТИКАЛЬНЫЕ ЛИНИИ РАЗДЕЛЕНИЯ ЯЧЕЕК таблицы итогов (заявка): .ws-tt-table th/td — border-right: 1px solid (ТОТ ЖЕ цвет/толщина, что горизонтальные border-bottom: rgba(255,255,255,0.07) тёмная / rgba(0,0,0,0.08) светлая — правило [data-theme="light"] дополнено border-right-color); .ws-tt-table th:last-child, td:last-child — border-right: none (правый край таблицы НЕ обводится). 5) Прочее БЕЗ ИЗМЕНЕНИЙ: геометрия Task 323/324 (строки итогов по строкам сетки _syncTotalsRows, синхронизация скролла, _fitGrid бюджет, широкий режим/ползунок шахматки, полоса шторки, tfoot sticky, зебра, слова, живые правки, закрытие gridwide 320 мс). ⚠️ ДЕПЛОЙ: ТОЛЬКО GitHub Pages + Ctrl+Shift+R ×1–2 (SW kipia-test-v563→v564) — Apps Script/листы НЕ трогать (клиентские правки, новых эндпоинтов нет) — см. scripts/DEPLOY-Task325-empcol-rows-crosslines.md. Верификация: сьют 1902→1928/0 (+24 tests/test-task325.js [HTML: три ряда порядок #wsSelectsRow→#wsTotalsRow→#wsActionsRow, ряд 2 «Итоги»+вкладки, ряд 3 Сформировать→Сохранить→Отменить, ✕/#wsTtClose/.ws-tt-close удалены, шапка ⚠+Обновить; CSS: var(--ws-emp-w) база 180/десктоп 200 + капы удалены, .ws-tt-emp без max-width/ellipsis, border-right 1px solid + :last-child none + светлая, ТРИ ряда calc((95−6)/3) + формула двух удалена, .ws-tt-head-empty 16px; VM: _measureEmpCol — максимум 142+20+2=164px/повтор без записи/скрытая страница (rect 0) не трогает/нет gridWrap|Range тихо/должность 155+16+2=173px, _renderGrid → _measureEmpCol ДО _fitGrid, init document.fonts.ready, _attachTotalsSync — document click только <1024px, closest #wsTotalsDrawer/#wsTotalsRow не закрывают, закрытая молчит, _totalsOpen=false выход; SW v564 + assertFalse v565]; ФЛЕЙК-ФИКС tests/extract-functions.js: setInterval песочницы — ЗАГЛУШКА () => 0 (реальный setInterval из eval\'а script-блоков — _startScrollAuditTimer с мок-matchMedia «мобильный» — ставил интервал _auditScrollState 3с, переживал песочницу и валил прогон на моке documentElement; периодика приложения тестам НЕ нужна; setTimeout — НАСТОЯЩИЙ); run-all.js подключение), task325-browser-check.py 27/27 (порт 8945, 3 контекста: десктоп-Админ 1280 ТЁМНАЯ [ТРИ ряда тулбара: порядок селекты→Итоги(ряд 2)→действия(ряд 3), totOrder [wsTotalsBtn,wsTtTabMonth,wsTtTabYear], actOrder [wsGenerateBtn,wsSaveBtn,wsCancelBtn], высоты 29,67px/95px, разделителя .ws-tb-sep НЕТ, «Сформировать» видна админу; КОЛОНКА: --ws-emp-w замерена (164px) = самый широкий текст 142 + паддинги 20 + 2, thW совпадает, ФИО НЕ обрезаны (scrollWidth=clientWidth); ОТКРЫТИЕ кнопкой: aria-pressed=true, ✕ НЕТ, МЕСЯЦ — пустая шапка ws-tt-head-empty 16px, шторка 640px = пол-области; ВЕРТИКАЛЬНЫЕ ЛИНИИ: td/th border-right 1px solid, последняя колонка none; строки итогов top ≤2,5px; ГОД — шапка показана («Обновить»), колонка «Сотрудник» по самому широкому тексту (colW = maxW+pad), архив Сидоров; ЗАКРЫТИЕ кнопкой: панель hidden, класс снят; ЖИВАЯ правка: попап → ОТ → «Сохранить (1)» в ряду 3; 0 JS-ошибок], светлая 1100 [линии тёмные rgba(0,0,0), ФИО не обрезаны, шапка сжата], мобильный 375 touch [ряды видны ≥28px, шторка ~86vw открылась, ✕ НЕТ, шапка сжата, колонка «Сотрудник» ПО ТЕКСТУ (не 150px), закрытие ТАПОМ МИМО (10,300) — панель hidden, aria-pressed false; 0 JS-ошибок]), регресс браузером: task306 29/29 [I2 АКТУАЛИЗИРОВАН: «Обновить» в ряду 1 #wsSelectsRow, «Сформировать» в ряду 3 #wsActionsRow — genInActions/actRowVisible/genRowGone], task311 24/24, task313 30/30, task314 36/36, task315 33/33, task317 49/49, task321 45/45, task322 45/45, task323 45/45, task324 49/49; VLM ×4 (десктоп: три ряда/✕ нет/вертикальные линии/ФИО полные — OK; мобайл 2× кроп и светлая 2× кроп — линии ПОДТВЕРЖДЕНЫ [мелкомасштабные «Дефект» — ложные негативы VLM, опровергнуты computed-проверками border-right 1px solid на мобиле/светлой]). SW kipia-test-v563→v564.')
lines2 = src.split('\n')
insert_at = None
for i, l in enumerate(lines2):
    if l.startswith('- **Табель учёта рабочего времени — ИТОГИ УЧЁТА: кнопки в НИЖНЕМ ряду тулбара'):
        j = i + 1
        while j < len(lines2) and lines2[j].startswith('  '):
            j += 1
        insert_at = j
        break
assert insert_at is not None, 'точка вставки (после буллета Task 324) не найдена'
lines2.insert(insert_at, entry)
src = '\n'.join(lines2)
print('Буллет Task 325 вставлен (после Task 324)')

with open('Системный_промт_для_приложения_КИПиА.md', 'w', encoding='utf-8') as f:
    f.write(src)
print('Готово: промт post-Task 325')
