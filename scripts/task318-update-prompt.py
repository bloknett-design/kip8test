# -*- coding: utf-8 -*-
# Обновление системного промта kip8test до post-Task 318
# (hover «Сотрудник +» сплошной; «Должность» — select из таблицы;
#  карточка «Режим работы» + «Уволить…» → dismissEmployee)
with open('Системный_промт_для_приложения_КИПиА.md', encoding='utf-8') as f:
    lines = f.readlines()

new3 = '''> **Версия документа:** 2026-09-05 (post-Task 318: HOVER «СОТРУДНИК +» — СПЛОШНОЙ ЦВЕТ #15202f/#e2e8ef как у ячеек ФИО (было полупрозрачное rgba — sticky-заголовок «просвечивал», фон «исчезал»; теперь меняет ЦВЕТ, не прозрачность); «ДОЛЖНОСТЬ» в шторке нового сотрудника — ВЫПАДАЮЩИЙ СПИСОК вариантов из таблицы «Сотрудники» файла табель_КИП_ИОС (select id=wsEmpPosition вместо input; уникальные должности АКТИВНЫХ + АРХИВНЫХ строк — listEmployees includeArchived:true при открытии шторки, по алфавиту ru, «— выберите —» первой; офлайн/сбой — fallback на активных _EMPLOYEES; выбранное сохраняется при обновлении опций; submitEmployeeForm читает select как прежде); КАРТОЧКА СОТРУДНИКА: строка «Тип» ПЕРЕИМЕНОВАНА в «Режим работы» + НОВАЯ красная строка «Уволить…» (.ws-emp-dismiss #ef5350/#c62828, только _canEdit, двойная защита) → ШТОРКА «Увольнение сотрудника» #wsDismissSheet (ФИО · таб. №; дата увольнения — сегодня; пояснение «строка останется в архиве справочника»; красная кнопка «Уволить» + «Отмена»; карточка закрывается ДО шторки — z 9401 > 201); УВОЛЬНЕНИЕ: kipConfirm danger → СЕРВЕР workSchedule.dismissEmployee {таб_номер, дата_увольнения} — пишет H (дата) + I (в_архиве=1) в таблицу «Сотрудники», строка НЕ удаляется (АРХИВ; listEmployees без includeArchived её скрывает — сотрудник УХОДИТ из шахматки/селектов) → loadGrid(true) + тост; повторный вызов перезаписывает дату (идемпотентно). ⚠️ СЕРВЕР Apps Script ОБНОВИТЬ: WorkSchedule.gs (+ метод dismissEmployee после addEmployee) + Code.gs (+ case 'workSchedule.dismissEmployee') — развёртывание пользователя AKfycbyt…, Deploy → New deployment (см. scripts/DEPLOY-Task318-dismiss-positions.md); без обновления «Уволить» даст тост «Unknown action», остальное работает. Листы/структура «Сотрудники» НЕ менялись (H/I существовали). Тесты: 1650 → 1686/0 (+36 test-task318.js: CSS hover сплошной/rgba убран из правил, .ws-emp-dismiss/.ws-dismiss-*; HTML select+шторка; JS _fillPositionSelect [includeArchived + fallback]/_fillPositionOptions [уникальные/алфавит/esc/keep]/openDismissForm/closeDismissForm/submitDismissForm/_doDismiss; VM моки DOM/API [открытие, payload, ошибка без даты, отмена kipConfirm, ошибка сервера, карточка закрыта ДО шторки]; СЕРВЕР: WorkSchedule.gs на MockSheet [H+I пишутся, строка остаётся, listEmployees скрывает архив, not_found/invalid, перезапись даты], маршрут Code.gs, шапка H/I); актуализированы test-task309/test-task310 [«Режим работы»], test-task314 [12-я мутация loadGrid(true) — увольнение]; SW-бамп v556→v557 в sw.js + 18 тест-файлах + assertFalse «лишний инкремент» → v558 в test-task315/317). Верификация: task318-browser-check.py 45/45 (порт 8937, 4 контекста: десктоп-Админ 1280 [hover заголовка computed rgb(21,32,47) сплошной, база rgb(14,22,33), уход курсора — возврат; светлая rgb(226,232,239); шторка: «Должность» SELECT, варианты = активные+архив уникальные по алфавиту, includeArchived запрос зафиксирован; карточка: «Режим работы» (не «Тип»), «Уволить…» красная до блока отпусков; шторка увольнения: ФИО · таб. №, дата=сегодня, красная кнопка #e53935, kipConfirm danger «Уволить …? …останется в архиве»; отмена подтверждения — API не зовётся, строка на месте, шторка жива; увольнение — API payload {017, 2026-09-05}, тост, строка УШЛА из сетки (rows_before−1), прочие на месте, мок-лист: в_архиве=1 + дата; select после увольнения должность из архива НЕ теряет; 0 JS-ошибок], зритель «ИТР8 pro» [заголовок НЕ кнопка, клик ничего, карточка без «Уволить…»/«+ Отпуск…», «Режим работы» виден], мобильный 375 touch [тап → карточка → «Уволить…» → шторка во всю ширину, ФИО+дата]); регресс 298 34/34, 303 20/20, 304 10/10, 305 20/20, 306 29/29, 307 20/20 (актуализирован: fill → select_option «Должность»), 308 20/20, 309 26/26, 310 20/20, 311 24/24 (актуализирован: «Режим работы»), 312 31/31, 313 30/30, 314 36/36, 315 33/33, 316 28/28, 317 49/49; VLM ×3 (карточка/шторка увольнения/мобайл — без дефектов; косметические замечания VLM [отступ даты у нативного input-даты, иконка ℹ️ у пояснения] — не блокеры). ПЕРЕНОС В kip8: НЕ выполнен — Task 318 ожидает проверки пользователем в kip8test (после проверки — партией 315+316+317+318 одним коммитом, kip8 v417→v418, ВКЛЮЧАЯ серверные scripts/*.gs). Деплой пользователя: GitHub Pages + Ctrl+Shift+R ×1–2; Apps Script ОБЯЗАТЕЛЬНО обновить — WorkSchedule.gs + Code.gs, New deployment.)
'''

assert lines[2].startswith('> **Версия документа:**')
lines.insert(2, new3 if new3.endswith('\n') else new3 + '\n')
print('Строка 3 вставлена (цепочка версий)')

src = ''.join(lines)

reps = [
    ('> **Текущая версия кэша:** `kipia-test-v556`',
     '> **Текущая версия кэша:** `kipia-test-v557`'),
    ('| `kip8test` | PWA | `kipia-test-v556` |',
     '| `kip8test` | PWA | `kipia-test-v557` |'),
]
for old, new in reps:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Версии кэша обновлены (2 замены)')

# Счётчики тестов
reps2 = [
    ('├── tests/                      # 1650 юнит-тестов (Node.js, 35 файлов): run-all.js, test-role-access.js, и др.',
     '├── tests/                      # 1686 юнит-тестов (Node.js, 36 файлов): run-all.js, test-role-access.js, и др.'),
    ('ожидается `1650 passed, 0 failed`', 'ожидается `1686 passed, 0 failed`'),
    ('# Ожидается: 1650 passed, 0 failed', '# Ожидается: 1686 passed, 0 failed'),
]
for old, new in reps2:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Счётчики тестов обновлены (3 замены)')

# Буллет Task 318 — после буллета Task 317 (новые задачи ниже)
entry = ('\n- **График работы — увольнение сотрудника + «Должность» списком + hover «Сотрудник +» (Task 318)**: '
         'по заявке пользователя: «При наведении указателя мыши на поле с кнопкой "Сотрудник +", фон поля не должен становиться прозрачным, а менять цвет, как в ячейках сотрудников под ней. В форме добавления нового сотрудника, в поле "Должность" сделай выпадающий список для выбора вариантов из таблицы "Сотрудники" файла "табель_КИП_ИОС". В открывающихся карточках сотрудников, изменить название строки "Тип" на "Режим работы", и там же добавить кнопку для формы увольнения сотрудника, с последующей записью даты увольнения в таблицу "Сотрудники". При увольнении сотрудник должен убираться из графика, оставаться только в архиве таблицы "Сотрудники".». 1) HOVER «СОТРУДНИК +»: заголовок-кнопка шапки (Task 311) — фон при наведении СПЛОШНОЙ #15202f (тёмная)/#e2e8ef (светлая), В ТОЧНОСТИ как td.ws-emp-col:hover (ячейки ФИО под шапкой); прежде rgba(74,143,199,0.16) ДЕЛАЛ sticky-заголовок прозрачным — сквозь него просвечивало прокручиваемое тело таблицы («фон исчезал»); теперь фон МЕНЯЕТ ЦВЕТ, оставаясь непрозрачным (rgba из hover-правил убран полностью; цвет совпадает с ячейками ФИО — заявка). 2) «ДОЛЖНОСТЬ» — SELECT: <select id="wsEmpPosition"> в шторке «Новый сотрудник» (был текстовый input); варианты — УНИКАЛЬНЫЕ должности ИЗ ТАБЛИЦЫ «Сотрудники» файла табель_КИП_ИОС: _fillPositionSelect при открытии шторки зовёт workSchedule.listEmployees {includeArchived: true} (АКТИВНЫЕ + АРХИВ строки листа — уволенные не теряют должность из списка), _fillPositionOptions: trim, дедупликация, сортировка localeCompare ru, «— выберите —» (value="") первой, escAttr/esc экранирование, сохранение выбранного значения при перезаголнении; офлайн/сбой — fallback на активных _EMPLOYEES (уже загружены, catch); submitEmployeeForm читает select.value как прежде (пустая опция = должность не указана — как раньше пустой input). 3) КАРТОЧКА СОТРУДНИКА: строка профиля «Тип» ПЕРЕИМЕНОВАНА в «Режим работы» (tipVal прежний: «сменный, смена №N»/«дневной»); НОВАЯ строка «Уволить…» (.ws-popup-row.ws-popup-more.ws-emp-dismiss — КРАСНАЯ #ef5350/#c62828 [как ✕ удаления], hover красная подложка, НЕ акцент-синяя) в блоке профиля (до «Отпуска · год»), только this._canEdit (двойная защита в openDismissForm — как «+ Отпуск…» Task 312). 4) ШТОРКА УВОЛЬНЕНИЯ #wsDismissSheet + #wsDismissOverlay (flow-input-sheet по образцу отпуска): заголовок «Увольнение сотрудника»; ФИО-строка #wsDismissEmp «ФИО · таб. №» (только чтение, .ws-dismiss-emp); дата #wsDismissDate (type=date, СЕГОДНЯ по умолчанию, фокус после анимации); пояснение .ws-dismiss-info «Сотрудник уйдёт из графика — строка останется в архиве справочника „Сотрудники"»; КРАСНАЯ кнопка «Уволить» (.ws-dismiss-submit #e53935) + «Отмена». КАРТОЧКА ЗАКРЫВАЕТСЯ ДО шторки (openDismissForm → closeEmpPopup: z попапа 9401 > sheet 201 — иначе карточка перекрывала бы шторку; как onEmpAddVacation Task 312). Состояние _dismissTabNo (объявлено в объекте, сбрасывается closeDismissForm). 5) SUBMIT: без даты — тост «Укажите дату увольнения» (API не зовётся); kipConfirm danger «Уволить ФИО? Дата … Сотрудник уйдёт из графика, строка останется в архиве справочника „Сотрудники"» (confirm-fallback) → _doDismiss: _api workSchedule.dismissEmployee {таб_номер, дата_увольнения ISO} → closeDismissForm + closeEmpPopup + тост «Сотрудник уволен и убран из графика» + loadGrid(true) (12-я мутация — перечитывает сервер: строка уходит из шахматки, сетка не мигает); catch — тост «Ошибка: …» (_apiErrText, шторка/состояние НЕ сбрасываются — можно повторить). 6) СЕРВЕР (⚠️ МЕНЯЛСЯ — единственный серверный файл со времён Task 283): WorkSchedule.gs — НОВЫЙ метод dismissEmployee (payload {token, таб_номер, дата_увольнения ISO}): _requireWrite (право workschedule.edit — как addEmployee); поиск строки по таб_№ в листе «Сотрудники» (A — текст, ведущие нули Task 304); setValue H (дата_увольнения — Date из _parseIsoDate) + I (в_архиве = 1) — СТРОКА НЕ УДАЛЯЕТСЯ (остаётся в листе как АРХИВ; listEmployees без includeArchived её больше не возвращает → сотрудник уходит из шахматки/селектов форм — «увольнение из графика»); перезапись существующей даты допустима (правка даты, идемпотентно); аудит WORKSCHEDULE_DISMISS_EMPLOYEE; ошибки invalid_таб_номер/invalid_дата_увольнения/not_found_таб_номер (+message); Code.gs — case \'workSchedule.dismissEmployee\' после addEmployee; шапка WorkSchedule.gs дополнена строкой эндпоинта. Колонки H/I листа «Сотрудники» СУЩЕСТВОВАЛИ с проекта (структура листа/листы/RoleMatrix — НЕ менялись; архивная логика ЧТЕНИЯ listEmployees/includeArchived была готова — сервер только учится ПИСАТЬ H/I). ДЕПЛОЙ ПОЛЬЗОВАТЕЛЯ: GitHub Pages + Ctrl+Shift+R ×1–2; Apps Script ОБЯЗАТЕЛЬНО обновить (WorkSchedule.gs + Code.gs → New deployment развёртывания AKfycbyt… — без этого «Уволить» даст тост «Unknown action: workSchedule.dismissEmployee», остальной функционал Task 318 работает на старом сервере) — см. scripts/DEPLOY-Task318-dismiss-positions.md. Верификация: сьют 1650→1686/0 (+36 tests/test-task318.js: CSS [hover сплошной #15202f/#e2e8ef, rgba из hover-правил убран, .ws-emp-dismiss красная, .ws-dismiss-emp/-info/-submit], HTML [select вместо input, шторка: заголовок/ФИО/дата/кнопки/пояснение, оверлей], JS [_fillPositionSelect includeArchived+fallback, _fillPositionOptions уникальные/алфавит/пустая опция/esc/keep, openDismissForm право+поиск+дата сегодня+closeEmpPopup ДО шторки, closeDismissForm сброс, submitDismissForm валидация+kipConfirm danger, _doDismiss API payload+loadGrid(true)+_apiErrText, _dismissTabNo объявлено], VM [моки DOM/API: наполнение опций (дедуп/trim/сортировка И<С<Э), открытие (ФИО/дата/active/карточка закрыта), не найден (тост, шторка не открыта), зритель (мимо), без даты (тост, API не зовётся), успех (payload {таб, дата}, closeEmpPopup, loadGrid(true), сброс, тост), ошибка сервера (тост с текстом, шторка жива, состояние не сброшено), kipConfirm отклонён (API не зовётся, шторка жива)], СЕРВЕР [dismissEmployee на MockSheet: H+I пишутся в строку по таб_№, строка НЕ удалялась (таб/ФИО на месте), другие строки не тронуты, listEmployees скрывает увольнённого (активные 1 из 2) но includeArchived видит (дата/в_архиве читаются), not_found/invalid_дата/invalid_таб, перезапись даты, маршрут Code.gs после addEmployee, шапка H/I], SW v557 + assertFalse v558; актуализированы: test-task309 [«Режим работы» вместо «Тип» + assertFalse «Тип»], test-task310 [«Режим работы»], test-task314 [12-я мутация loadGrid(true) = 13 вызовов], test-task315/317 [assertFalse «лишний инкремент» v557→v558]); SW-бамп v556→v557 в sw.js + 18 тест-файлах; run-all.js подключение), task318-browser-check.py 45/45 (порт 8937, мок fetch, 4 контекста: десктоп-Админ 1280 — тёмная тема (app-theme=dark): заголовок-кнопка курсор pointer, фон ДО rgb(14,22,33) (база #0e1621), hover rgb(21,32,47) СПЛОШНОЙ (rgb без альфы), уход курсора — возврат базы; светлая rgb(226,232,239); клик по заголовку — шторка «Новый сотрудник», «Должность» SELECT с вариантами АКТИВНЫЕ+АРХИВ (— выберите —, Инженер КИПиА, Мастер КИПиА, Слесарь КИПиА, Электрик — уникальные, по алфавиту; запрос includeArchived зафиксирован; мок-архив 098/099 не в сетке, но должности в списке); карточка Иванова: «Режим работы» (labels, «Тип» нет), «сменный, смена №1», «Уволить…» до «Отпуска · » (классы ws-popup-row/ws-popup-more/ws-emp-dismiss, computed color rgb(239,83,80)); клик «Уволить…»: карточка закрыта, шторка+оверлей active, «Иванов Иван Иванович · таб. №017», дата=сегодня, пояснение «архиве», кнопка rgb(229,57,53); kipConfirm danger «Уволить Иванов Иван Иванович?» + архив; отмена — API 0 вызовов, строка 017 в сетке, шторка live; «Отмена» шторки — закрыта; увольнение 2026-09-05: API payload {таб_номер 017, дата_увольнения}, шторка/карточка закрыты, тост «уволен», строка 017 УШЛА (rows 12→11), 023 на месте, мок-лист: 017 в_архиве=1 дата 2026-09-05 (СТРОКА ОСТАЛАСЬ); select после увольнения — должности архива живы; 0 JS-ошибок; зритель «ИТР8 pro» — заголовок без ws-emp-head-add/cursor default, клик шторку НЕ открывает, карточка: «Режим работы» есть, «Уволить…»/«+ Отпуск…» нет; мобильный 375 has_touch — заголовок-кнопка жив, тап → карточка → «Уволить…» → шторка ≥340px, ФИО+дата; 0 JS-ошибок), регресс браузером: 298 34/34, 303 20/20, 304 10/10, 305 20/20, 306 29/29, 307 20/20 (актуализирован: fill → select_option + wait на «Должность»), 308 20/20, 309 26/26, 310 20/20, 311 24/24 (актуализирован: «Тип» → «Режим работы»), 312 31/31, 313 30/30, 314 36/36, 315 33/33, 316 28/28, 317 49/49; VLM ×3 (карточка: «Уволить…» красная в профиле, «Режим работы» есть, hover сплошной, дефектов нет; шторка: заголовок/ФИО/дата/пояснение/кнопки — всё на месте, вёрстка ровная; мобайл: без обрезки, кнопки крупные, ФИО жив — косметика VLM [padding даты у нативного input, иконка ℹ️] не блокеры). SW kipia-test-v556→v557.')
lines2 = src.split('\n')
insert_at = None
for i, l in enumerate(lines2):
    if l.startswith('- **График работы — тултип «данные от'):
        j = i + 1
        while j < len(lines2) and lines2[j].startswith('  '):
            j += 1
        insert_at = j
        break
assert insert_at is not None, 'точка вставки (после буллета Task 317) не найдена'
lines2.insert(insert_at, entry)
src = '\n'.join(lines2)
print('Буллет Task 318 вставлен (после Task 317)')

with open('Системный_промт_для_приложения_КИПиА.md', 'w', encoding='utf-8') as f:
    f.write(src)
print('Готово: системный промт — post-Task 318 (кэш v557, 1686 тестов)')
