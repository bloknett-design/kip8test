# -*- coding: utf-8 -*-
# Обновление системного промта kip8test до post-Task 322
# (коды д/н — дни ПЕРЕРАБОТКИ в итогах: сменный 12 ч, дневной — по часам
#  из правки ячейки (малая форма в попапе + поле в шите + серверная
#  колонка K «часы»); шапка итогов — СЛОВА с кодами в скобках +
#  колонка «Переработка» (месяц и год); ЗЕБРА строк; панель итогов —
#  на всю высоту шахматки до бара с кнопками — вид переключается)
with open('Системный_промт_для_приложения_КИПиА.md', encoding='utf-8') as f:
    lines = f.readlines()

new3 = '''> **Версия документа:** 2026-09-05 (post-Task 322: КОДЫ «д»/«н» — ДНИ ПЕРЕРАБОТКИ в «Итогах учёта» (заявка: «Коды „д“ и „н“ должны учитываться в итогах учёта как дни переработки: для сменного персонала — по 12 часов, для дневного персонала — по часам, которые указали при добавлении кода в ячейку шахматки»): _totalsAgg классифицирует д/н ДО ветки явок — a.overDays++/a.over += часы; д/н больше НЕ явки/часы/Д/Н (только Переработка + Всего); _overHours: сменному 12, дневному (и без типа) — поле «часы» записи (фолбэк 8 для старых записей), карта типов _empTypeMap(сотрудники) передаётся в агрегацию (месяц — _EMPLOYEES, год — справочник года с архивом); _codeHours: д/н ИЗЪЯТЫ из карты (0 — не рабочие часы); ВВОД ЧАСОВ при добавлении кода: onPopupStatus перехватывает д/н у НЕ-сменного → _renderDnHoursForm — МАЛАЯ ФОРМА В ПОПАПЕ ЯЧЕЙКИ (заголовок дата·ФИО, строка кода со свотчем, «Часы переработки в этот день» input#wsDnHours inputmode=decimal, префилл 8 или текущие часы записи, кнопки «Готово»/«Назад к кодам», Enter применяет; applyDnHours — валидация 0,5..24, тост «Укажите часы от 0,5 до 24», округление к 0,1; dnHoursBack возвращает список кодов БЕЗ перепозиционирования); сменному д/н применяется СРАЗУ (12 ч, без формы); шит «Дополнительно…» — поле #wsCellHours «Часы (для кодов д/н — переработка)» (префилл из записи с запятой; submitCellForm валидирует, «часы»: число|null в _PENDING; _applyCellStatus 4-й параметр hours: д/н+число → «часы», без/не-д/н → null); список кодов попапа показывает «· 7,2 ч» у стоящего д/н; saveAll шлёт payload[«часы»]; СЕРВЕР WorkSchedule.gs: listEntries читает 11 колонок — поле «часы» (число/null, строки «7,2» нормализуются хелпером _wsNumOrNull); setManualEntry: payload.часы undefined (старый клиент) → колонку K НЕ трогает (обратная совместимость), null → чистит, число 0,5..24 (валидация invalid_часы) → пишет в K (обновление и вставка); структура листа: A..J прежние + K «часы» (заголовок в K1 — по желанию, данные пишутся и без него); ШАПКА ИТОГОВ — СЛОВА с кодами в скобках (заявка: «Вместо кодов в шапке итогов учёта используй слова»): Явки, Часы, День (Д), Ночь (Н), Отпуск (ОТ), Уч. отпуск (У), Отгул (ОВ), Больничный (Б), Прогул (ПР), Переработка, Прочие, Всего; колонка «Переработка» (td.ws-tt-over, янтарная #e0a23c/светлая #a06a13, тултип «дней переработки: N (коды д/н)») — между ПР и Прочими; ГОД — колонка «Перераб.» после Часов (часы д/н за год + дни в тултипе); ОФОРМЛЕНИЕ (заявка: «в виде таблицы в цвете строк зеброй, и раскрываться панель должна на всю высоту шахматки, до бара с кнопками»): ЗЕБРА tbody tr:nth-child(even) — rgba(255,255,255,0.09) тёмная / rgba(0,0,0,0.07) светлая (сила подобрана по VLM: 3,5% не читался); ПАНЕЛЬ НА ВСЮ ВЫСОТУ: toggleTotals ставит класс ws-tt-open на #page-work-schedule → CSS @media (min-width:1024px): .ws-grid-wrap display:none (сетка скрыта — итоги вместо неё), .ws-totals-panel flex:1 1 auto + min-height:0 + max-height:none (от бара итогов, поднявшегося под тулбар, до низа окна); сворачивание возвращает сетку (_fitGrid: скрытая сетка → clearFit, природные высоты); мобильный (<1024) — панель max-height 70vh, страница скроллится, сетка остаётся видимой; кап 46vh (Task 321) удалён. Тесты: 1786 → 1833/0 (+47 tests/test-task322.js: HTML [#wsCellHours поле/label/inputmode]; CSS [зебра обе темы, ws-tt-over, полная высота + скрытие сетки, .ws-dn-форма]; VM [onPopupStatus перехват сменный/дневной/без типа/прочие; _renderDnHoursForm префилл/структура; applyDnHours валидация/округление/тосты; dnHoursBack; _applyCellStatus часы число/null/мусор/округление; submitCellForm часы валидные/пусто/мусор/не-д/н; openCellForm префилл; saveAll payload; попап «· N ч»; _empTypeMap/_overHours; _totalsAgg д/н не портит явки]; СЕРВЕР [MockSheet+loadWS: listEntries 11 колонок число/null/«7,2»/мусор→null; setManualEntry валидные/строка/обновление затирает/null чистит/undefined не трогает/вставка с K/invalid_часы; текст структуры K]; шапка словами/значения переработки; SW v561 + assertFalse v562; run-all.js подключение; актуализирован test-task321 [д/н — 0 в _codeHours; _totalsAgg с типами + д/н переработка; шапка словами; зебра; полная высота панели; toggleTotals класс ws-tt-open; год Перераб.; метод-хосты с _empTypeMap/_overHours]; test-work-schedule [регекс сигнатуры _applyCellStatus + hours]). Верификация: task322-browser-check.py 45/45 (порт 8942, мок fetch с ХРАНЯЩИМ upsert setManualEntry + записи с часами, 3 контекста: десктоп-Админ 1280 ТЁМНАЯ [панель на всю высоту: сетка скрыта (0px), класс ws-tt-open, бар итогов под тулбаром (≤20px), панель до низа окна (≥788/800), высота >400; шапка словами — 12 слов, одиночных кодов нет; Иванов явки 4/43,2/День 3/Ночь 1/ОТ 2/Б 1/ПЕРЕРАБОТКА 12 (сменный д)/всего 9, Петров 2/16/переработка 7,2 (дневной д с часами записи), Итого 6/59,2/19,2, тултип «дней переработки: 1»; ЗЕБРА computed r1≠r0≠transparent; год: Перераб. 12 у Иванова; сворачивание — сетка вернулась; ВВОД ЖИВЬЁМ: клик ячейки Петрова день 10 → попап → д → форма (префилл 8) → «Назад к кодам» → снова д → 6 → «Готово» → _PENDING д/часы 6, ячейка «д», итоги live 13,2; «Сохранить» — payload часы 6, после loadGrid (мок сохранил) 13,2; сменный день 11: д применился СРАЗУ без формы, часы null; список кодов «· 7,2 ч»; шит — префилл 7,2; 0 JS-ошибок], светлая 1100 [панель на всю высоту, зебра светлая, слова, Петров 7,2], мобильный 375 touch [тап-зона ≥40, тап раскрыл ≤70vh, слова+зебра]); регресс браузером: 306 29/29, 307 20/20, 308 20/20, 309 26/26 [актуализирован: д дневному → форма часов → 8 → Готово], 310 20/20 [актуализирован аналогично], 311 24/24, 312 31/31, 313 30/30, 314 36/36 ×2 (флейк C2 не подтвердился), 315 33/33, 316 28/28, 317 49/49, 318 45/45, 319 48/48, 320 24/24, 321 45/45 [актуализирован: индексы колонок R под «Переработку»]; VLM ×4 (месяц/форма/светлая/зебра) — зебра ПОДТВЕРЖДЕНА после усиления тинта до 9% (3,5% VLM не видел), форма/шапка/цвета/полная высота — без дефектов. ⚠️ ДЕПЛОЙ: GitHub Pages + Ctrl+Shift+R ×1–2 (SW kipia-test-v560→v561) И Apps Script WorkSchedule.gs ОБЯЗАТЕЛЬНО для СОХРАНЕНИЯ часов д/н (без него клиент работает, но часы не сохраняются — фолбэк 8; нового развёртывания не нужно — тот же эндпоинт, новая Version) + заголовок «часы» в K1 листа «Записи_графика» (по желанию). ПЕРЕНОС В kip8: НЕ выполнен — Tasks 315–322 одной партией после проверки (k8 v417→v418, включая scripts/*.gs от 318/320/322) — см. scripts/DEPLOY-Task322-pererabotka-zebra-words.md)
'''

assert lines[2].startswith('> **Версия документа:**')
lines.insert(2, new3 if new3.endswith('\n') else new3 + '\n')
print('Строка 3 вставлена (цепочка версий)')

src = ''.join(lines)

reps = [
    ('> **Текущая версия кэша:** `kipia-test-v560`',
     '> **Текущая версия кэша:** `kipia-test-v561`'),
    ('| `kip8test` | PWA | `kipia-test-v560` |',
     '| `kip8test` | PWA | `kipia-test-v561` |'),
]
for old, new in reps:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Версии кэша обновлены (2 замены)')

# Счётчики тестов
reps2 = [
    ('├── tests/                      # 1783 юнит-тестов (Node.js, 39 файлов): run-all.js, test-role-access.js, и др.',
     '├── tests/                      # 1833 юнит-тестов (Node.js, 40 файлов): run-all.js, test-role-access.js, и др.'),
    ('ожидается `1783 passed, 0 failed`', 'ожидается `1833 passed, 0 failed`'),
    ('# Ожидается: 1783 passed, 0 failed', '# Ожидается: 1833 passed, 0 failed'),
]
for old, new in reps2:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Счётчики тестов обновлены (3 замены)')

# Буллет Task 322 — после буллета Task 321
entry = ('\n- **Табель учёта рабочего времени — д/н = ПЕРЕРАБОТКА в итогах, шапка словами, зебра, панель на всю высоту (Task 322)**: '
         'по заявке пользователя: «Коды „д“ и „н“ должны учитываться в итогах учёта как дни переработки: для сменного персонала — по 12 часов, для дневного персонала — по часам, которые указали при добавлении кода в ячейку шахматки. Вместо кодов в шапке итогов учёта используй слова. Оформление итогов учёта сделай в виде таблицы в цвете строк зеброй, и раскрываться панель должна на всю высоту шахматки, до бара с кнопками.». 1) ПЕРЕРАБОТКА д/н (клиент): _totalsAgg — д/н классифицируются ДО ветки явок: overDays/over (часы), в явки/часы/Д/Н/Прочие НЕ попадают (в Всего входят); _overHours — сменному 12, дневному/без типа — поле «часы» записи, фолбэк 8 (старые записи); карта типов _empTypeMap передаётся в _totalsAgg (месяц — _EMPLOYEES; год — md.employees с архивом, уволенный сохраняет тип); _codeHours — д/н изъяты из карты (0). 2) ВВОД ЧАСОВ при добавлении д/н (клиент): onPopupStatus — д/н НЕ сменному (дневной/без типа) → _renderDnHoursForm: малая форма в попапе ячейки (дата·ФИО, свотч+код+название, «Часы переработки в этот день» #wsDnHours inputmode=decimal, префилл 8/текущие часы, «Готово»/«Назад к кодам», Enter; applyDnHours — валидация 0,5..24 + тост + округление к 0,1; dnHoursBack — список кодов на месте, без перепозиционирования; CSS .ws-dn-form/.ws-dn-input/.ws-dn-ok/.ws-dn-back/.ws-dn-hint, обе темы); сменному — применяется сразу (12 ч); шит «Дополнительно…» — #wsCellHours «Часы (для кодов д/н — переработка)» (префилл с запятой из записи; submitCellForm — валидация/тост, «часы»: число|null; ошибка — форма остаётся); _applyCellStatus(isoDate, tabNo, code, hours) — 4-й параметр: д/н+число → pending[«часы»], иначе null; список кодов попапа — «· 7,2 ч» у стоящего д/н (_renderCellPopup dnH); saveAll — payload[«часы»]. 3) СЕРВЕР (WorkSchedule.gs): listEntries читает 11 колонок, поле «часы» (_wsNumOrNull: число/строка «7,2» → число; пусто/мусор/вне 0,5..24 → null); setManualEntry: payload.часы — undefined (старый клиент) → K НЕ трогаем (сохранённые часы живут), null → чистим, число 0,5..24 → пишем (invalid_часы при ошибке; округление к 0,1; обновление затирает старые, вставка — K в комплекте); лист «Записи_графика» — колонка K «часы» (заголовок K1 по желанию). 4) ШАПКА СЛОВАМИ: Явки, Часы, День (Д), Ночь (Н), Отпуск (ОТ), Уч. отпуск (У), Отгул (ОВ), Больничный (Б), Прогул (ПР), Переработка, Прочие, Всего; колонка «Переработка» (месяц) — часы д/н, td.ws-tt-over янтарная (#e0a23c/#a06a13), тултип «дней переработки: N (коды д/н)»; год — колонка «Перераб.» (после Часов; годовые часы д/н + дни в тултипе; формат «—» при нуле). 5) ОФОРМЛЕНИЕ: ЗЕБРА строк (tbody tr:nth-child(even): rgba(255,255,255,0.09) тёмная / rgba(0,0,0,0.07) светлая — итоговая строка своей заливкой, зебра её не трогает; сила по VLM-проверке); ПАНЕЛЬ НА ВСЮ ВЫСОТУ ШАХМАТКИ, ДО БАРА С КНОПКАМИ: toggleTotals ставит класс ws-tt-open на #page-work-schedule → @media (min-width:1024px): .ws-grid-wrap { display:none } (сетка скрыта — вид «итоги»), .ws-totals-panel { flex:1 1 auto; min-height:0; max-height:none } (бар итогов под тулбаром, панель до низа окна); сворачивание — класс снят, сетка возвращается (_fitGrid: скрытая сетка → clearFit; ResizeObserver дожимает); мобильный (<1024) — панель ≤70vh со своим скроллом, сетка остаётся видимой (страница скроллится); кап 46vh удалён. ⚠️ ДЕПЛОЙ: GitHub Pages + Ctrl+Shift+R ×1–2 (SW kipia-test-v560→v561) И Apps Script WorkSchedule.gs — ОБНОВИТЬ для СОХРАНЕНИЯ часов д/н (без обновления: клиент работает, часы не сохраняются — после перезагрузки фолбэк 8; нового развёртывания не нужно, новая Version того же эндпоекта) + заголовок «часы» в K1 «Записи_графика» (по желанию) — см. scripts/DEPLOY-Task322-pererabotka-zebra-words.md. Верификация: сьют 1786→1833/0 (+47 tests/test-task322.js [HTML/CSS/VM: перехват onPopupStatus, форма часов, applyDnHours, dnHoursBack, _applyCellStatus часы, submitCellForm/openCellForm/saveAll, _empTypeMap/_overHours, _totalsAgg д/н; СЕРВЕР: listEntries 11 колонок, setManualEntry часы undefined/null/число/invalid; шапка/значения; SW v561]; актуализирован test-task321 [д/н=0 в карте часов; агрегация с типами; слова в шапке; зебра; полная высота; год Перераб.]; test-work-schedule [сигнатура _applyCellStatus + hours]; SW-бамп v560→v561 в sw.js + 22 тест-файлах, assertFalse → v562 в 315/317/318/319/321; run-all.js), task322-browser-check.py 45/45 (порт 8942, мок-сервер с ХРАНЯЩИМ upsert; десктоп: панель на всю высоту + сетка скрыта + бар под тулбаром + зебра computed + шапка 12 словами + значения переработки (сменный 12/дневной 7,2/итого 19,2) + тултип дней + год «Перераб.» + сворачивание; ввод часов живьём: попап → д → форма префилл 8 → Назад → 6 → Готово → pending 6 → итоги 13,2 → «Сохранить» payload часы 6 → после loadGrid 13,2; сменный — без формы; «· 7,2 ч» в списке; шит префилл 7,2; светлая 1100; мобильный 375 тап), регресс браузером 306–321 все зелёные (309/310/321 актуализированы), VLM ×4 — зебра подтверждена (3,5%→9%), прочее без дефектов. SW kipia-test-v560→v561.')
lines2 = src.split('\n')
insert_at = None
for i, l in enumerate(lines2):
    if l.startswith('- **Табель учёта рабочего времени — год ровно 3 года, раздел переименован'):
        j = i + 1
        while j < len(lines2) and lines2[j].startswith('  '):
            j += 1
        insert_at = j
        break
assert insert_at is not None, 'точка вставки (после буллета Task 321) не найдена'
lines2.insert(insert_at, entry)
src = '\n'.join(lines2)
print('Буллет Task 322 вставлен (после Task 321)')

with open('Системный_промт_для_приложения_КИПиА.md', 'w', encoding='utf-8') as f:
    f.write(src)
print('Готово: промт post-Task 322')
