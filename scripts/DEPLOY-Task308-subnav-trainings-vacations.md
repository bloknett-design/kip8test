# DEPLOY — Task 308: вкладки «Инструктажи» и «Отпуска» удалены, кнопка «+ Отпуск» в тулбаре

**Дата:** 2026-09-04 · **Репо:** kip8test · **SW:** kipia-test-v546 → **v547**
**Заявка пользователя:** «Убери вкладки "Инструктажи" и "Отпуска", а кнопку
"Добавить отпуск" перемести в бар над шахматкой к остальным кнопкам».

## Что изменилось (только фронтенд)

1. **Страницы «Инструктажи» и «Отпуска» удалены целиком**:
   - дивы `#page-work-schedule-trainings` / `#page-work-schedule-vacations`
     (списки, селект года, сводка, бары добавления);
   - **субнавигация модуля удалена ЦЕЛИКОМ** (`.ws-subnav` — после Task 307
     было 3 полосы × 3 кнопки; модуль стал ОДНОСТРАНИЧНЫМ: только шахматка);
   - хуки `navigateTo` → `initTrainingsPage` / `initVacationsPage`;
   - записи `PAGE_PARENTS` / `PAGE_LABELS`; `_WORK_SCHEDULE_PAGES` →
     `['work-schedule']` (карта ролей);
   - JS-методы `initTrainingsPage` / `loadTrainings` / `_renderTrainings` /
     `initVacationsPage` / `onVacYearChange` / `loadVacations` /
     `_renderVacations`;
   - мёртвый CSS (`.ws-subnav*`, `.ws-trainings-list`, `.ws-tr-card*`,
     `.ws-tr-type*` — цветные плашки типов, `.ws-add-bar` / `.ws-add-btn`,
     `.ws-vac-toolbar` / `.ws-vac-summary`, `.ws-vac-card*`, `.ws-vac-badge*`).
2. **Кнопка «+ Отпуск»** (`#wsVacBtn`, класс `.ws-addvac-btn`) — в баре
   над шахматкой (`ws-toolbar-main`): ПОРЯДОК РЯДА селекты → «+ Сотрудник»
   (Task 307) → **«+ Отпуск»** → «Сформировать». Стиль — общее правило с
   `.ws-addemp-btn` (нейтральный в духе селектов, высота 34px, светлая
   тема), `title="Добавить период отпуска"`. Видимость — по `_canEdit`
   (как у «Сформировать»/«+ Сотрудник»: Админ и пишущие роли видят,
   «ИТР8 pro» — нет). Клик → прежний bottom-sheet «Новый отпуск»
   (`#wsVacSheet`).
3. **Год плана отпусков = год шахматки**: `_loadVacations` (в `loadGrid`)
   заполняет и `_VACATIONS` (план «ОТ» в ячейках), и `_VAC_PAGE` +
   `_vacYear` (номер свободной части в шторке, проверка пересечений).
   Селект года удалён вместе со страницей.
4. **После добавления отпуска перезагружается шахматка** (`loadGrid()` —
   план «ОТ» подтягивается в пустые ячейки) + тост «Отпуск добавлен.
   Нажмите «Сформировать» …».

## Что живо (проверено регрессией)

- **Мероприятия**: бейджи И/ОБ/ПЗ/ПР/* в ячейках; добавление — «+
  Мероприятие…» в попапе ячейки (Task 303, шторка `#wsTrSheet` с
  префиллом сотрудник+дата); секция «Мероприятия в этот день» в попапе.
- **План отпусков в сетке**: класс `ws-vac-plan`, код «ОТ» в пустой
  ячейке, тултип «ОТПУСК (план, часть N)»; «Сформировать» расставляет «О».
- **Шторка «Новый отпуск»**: сотрудник, часть (первый свободный номер),
  даты, «Период: N кал. дн.», подсказка ст. 125 ТК РФ.
- API-методы `deleteTraining` / `deleteVacation` и все серверные
  эндпоинты — без изменений.
- «+ Сотрудник» (Task 307) — без изменений.

## Известные особенности (следствие заявки)

- **Интерфейсного удаления периодов отпусков временно нет** (кнопки
  «Удалить» были на карточках удалённой страницы «Отпуска»). Метод
  `deleteVacation` жив; удалять до возврата UI — через Apps Script или
  правку листа «Отпуска».
- Аналогично для мероприятий (кнопка «Удалить» была на странице
  «Инструктажи»); метод `deleteTraining` жив.
- Старые ссылки/закладки `#work-schedule-trainings` /
  `#work-schedule-vacations` открывают пустой экран БЕЗ ошибки
  (как у любой несуществующей страницы) — вернуться шевроном/свайпом.

## Чего НЕ менялось

- **Сервер `WorkSchedule.gs` — БЕЗ изменений** (Apps Script НЕ трогать!).
- Листы Google-таблицы — без изменений; миграции не нужны.
- Все эндпоинты `workSchedule.*` — прежние.

## Деплой (единственный шаг)

1. **GitHub Pages** kip8test — задеплоится автоматически после пуша (1–3 мин).
2. Пользователи: **Ctrl+Shift+R ×1–2** (обновление Service Worker v547).

## Верификация

- Сьют: **1444 → 1457 passed / 0 failed** (+13 тестов Task 308 в
  `tests/test-work-schedule.js`; обновлены блоки Task 249/267/269/274/307
  и файлы test-task306.js / test-vacations-feedback.js /
  test-vacations-generate.js).
- `node --check` — OK (4 script-блока index.html).
- `scripts/task308-browser-check.py` — **21/21** (Playwright, мок fetch):
  субнавигации нет, страниц нет; «+ Отпуск» в тулбаре (порядок ряда,
  34px, title, видимость Админ/«ИТР8 pro»); шторка с автоподстановкой
  части и «Период: 14 кал. дн.»; submit → addVacation → loadGrid →
  ПЛАН «ОТ» в ячейке (ws-vac-plan, тултип «часть 1»); попап ячейки
  «+ Мероприятие…» жив (префилл 017/10.09); 375px без переполнения;
  десктоп 1280px — паритет скрытия кнопок; 0 JS-ошибок.
- Регресс: task298 29/29, task303 20/20, task304 10/10, task305 20/20,
  task306 29/29, task307 22/22 (304/306/307 актуализированы: проверки
  удалённых страниц заменены на проверки новой реальности).
- VLM ×2 (мобильный тулбар + шторка «Новый отпуск») — ок.
