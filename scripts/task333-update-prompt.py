# -*- coding: utf-8 -*-
# Обновление системного промта kip8test до post-Task 333
# (шапка шторки = шапке сетки, разделитель групп в шторке,
#  подсветка строк шторки, подпись «Вид», архив годовой таблицы,
#  полоса ФИО через ::after)
with open('Системный_промт_для_приложения_КИПиА.md', encoding='utf-8') as f:
    lines = f.readlines()

new3 = '''> **Версия документа:** 2026-09-06 (post-Task 333: ШАПКА ШТОРКИ=ШАПКЕ СЕТКИ + РАЗДЕЛИТЕЛЬ ГРУПП В ШТОРКЕ + ПОДСВЕТКА СТРОК ШТОРКИ + ПОДПИСЬ «ВИД» + АРХИВ ГОДОВОЙ ТАБЛИЦЫ + ПОЛОСА ФИО ЧЕРЕЗ ::after [заявка: «В шторке итогов учёта, цвет фона шапки сделай как в шапке шахматки табеля, разделительную горизонтальную полосу между ячейками учёта сменных и дневных сотрудников сделай как в шахматке табеля между ячейками сменных и дневных сотрудников. Перекрёстная подсветка строк и столбцов ячеек табеля так же должна распространяться на строки с ячейками в шторке итогов учёта. Кнопку переключения вида подпиши "Вид". Архив в годовой таблице всё же нужен, обновление данных только общей кнопкой "Обновить" тулбара. Разделительная полоса между столбцом ФИО и ячейками шахматки должна быть со стороны ячеек сотрудников, что бы при прокрутки шахматки она не уходила.»]: 1) ШАПКА ШТОРКИ = ШАПКЕ СЕТКИ (Task 330): .ws-tt-table th #131c28→#1e293b, светлая #efede5→#bfcad5; ЯЧЕЙКА «Сотрудник» В ШАПКЕ (thead th.ws-tt-emp — год-мобайл и таблица архива) — отдельные правила (0,3,1) #1e293b/#bfcad5, НЕ фон тела #0e1621/#e9e7de. 2) РАЗДЕЛИТЕЛЬ ГРУПП В ШТОРКЕ — КАК В ШАХМАТКЕ (Task 259): .ws-tt-table tbody tr.ws-group-first td — border-top 2px solid #4a8fc7 (светлая #6e8ba4); класс ставят _renderTotalsMonth и _renderTotalsYearTable (+архив) — логика «первый дневной после сменных». 3) ПОДСВЕТКА СТРОК ШТОРКИ: _rowClass ставит ws-hover-row и на строку ТАБЛИЦЫ ИТОГОВ (тот же индекс; #wsTtBody — ПЕРВАЯ таблица = строки сетки; за пределами — тихо); _renderTotalsMonth/_renderTotalsYearTable штампуют ws-hover-row из _hoverRow (переживает перерисовку); CSS .ws-tt-table tbody tr.ws-hover-row td — inset-заливка rgba(74,143,199,0.10)/светлая rgba(42,93,143,0.06) — приём и цвета строки сетки (Task 319). 4) ПОДПИСЬ «ВИД»: #wsViewBtn + span.ws-view-label «Вид» ПОСЛЕ иконок (иконки остаются — показывают вид); .ws-view-btn — width auto + паддинги 0 9px + gap 5px (десктоп-медиа — width auto поверх 34px), .ws-view-label 13px/600; подсказка #wsViewTip/aria/тосты/цикл/замок роли — без изменений (Task 332). 5) АРХИВ ГОДОВОЙ ТАБЛИЦЫ ВОЗВРАЩЁН («всё же нужен»): активные (строки сетки) в ГЛАВНОЙ таблице + АРХИВ ОТДЕЛЬНЫМ БЛОКОМ ПОД ней — подпись .ws-tt-arch-cap «Архив» + таблица .ws-tt-year.ws-tt-arch (справочник md.employees минус активные, _sortEmployees) со СВОЕЙ колонкой «Сотрудник» НА ЛЮБОМ экране (в сетке архивных строк НЕТ); ГЛАВНАЯ таблица года тоже рендерит ws-tt-emp — десктоп CSS прячет (.ws-tt-year:not(.ws-tt-arch)), МОБАЙЛ год получил имена АКТИВНЫХ (регресс Task 332 закрыт); обновление — ТОЛЬКО кнопкой «Обновить» ТУЛБАРА (своей нет); общий билдер строки yearRow(emp, trCls); 6) СИНХРОНИЗАЦИЯ СКРОЛЛА: прижимка «панель не уезжает дальше сетки» (Task 323) УДАЛЕНА из body-обработчика (делала хвост шторки НЕДОСТУПНЫМ — скролл панели прыгал обратно; scrollTop 99999→1); сетка едет с панелью пока хватает (браузер клампит gridWrap.scrollTop), ниже — прижата к своему низу, панель доскролливает архив. 7) ПОЛОСА ФИО/ЯЧЕЙКИ — ::after: ДИАГНОСТИКА пиксельными пробами Chromium+Firefox — border-right 2px на sticky-ячейках в border-collapse УЕЗЖАЛ при прокрутке (после scrollLeft=300 линии НЕТ в обоих движках); ФИКС: ::after на th/td.ws-emp-col — position absolute, right: 0 (ПРИНАДЛЕЖИТ ячейкам ФИО, НЕ обрезается overflow:hidden эллипсиса — left:100% за краем обрезался бы), width 2px, background #4a8fc7/#6e8ba4, pointer-events none, top/bottom -1px (непрерывность 98/100); полоса принадлежит sticky-ячейке — НЕ уезжает НИКОГДА; прежний border-right 2px УДАЛЁН. Тесты 2055/0 (+21 test-task333.js; актуализированы 321/323/327/325; SW v571→v572, «лишний инкремент»→v573 в 14); browser task333-browser-check.py 39/39 [десктоп: подпись «Вид», цикл видов, шапка #1e293b=сетке, ws-group-first 2px, подсветка строки шторки, архив блоком (десктоп: «Сотрудник» главной скрыт, у архива виден+sticky, прокрутка ДОСТИГАЕТ архива), светлая 1024 (#bfcad5, полоса после прокрутки — пиксельная проба), мобайл (месяц+год имена, архив), Firefox (полоса до/после прокрутки)]; регресс 332 44/44, 331 38/38, 328 36/36, 330 27/27 (актуализированы под 333); VLM ×3; пиксельные пробы обоих движков. ДЕПЛОЙ: ТОЛЬКО Pages + Ctrl+Shift+R ×1–2 — Apps Script/листы НЕ трогать. ПЕРЕНОС в kip8: НЕ выполнен — ждёт проверки [потом одной партией 315+…+333, k8 v418→v419])
'''

assert lines[2].startswith('> **Версия документа:**')
lines.insert(2, new3 if new3.endswith('\n') else new3 + '\n')
print('Строка 3 вставлена (цепочка версий)')

src = ''.join(lines)

reps = [
    ('> **Текущая версия кэша:** `kipia-test-v571`',
     '> **Текущая версия кэша:** `kipia-test-v572`'),
    ('| `kip8test` | PWA | `kipia-test-v571` |',
     '| `kip8test` | PWA | `kipia-test-v572` |'),
    ('├── tests/                      # 2034 юнит-тестов (Node.js, 49 файлов): run-all.js, test-role-access.js, и др.',
     '├── tests/                      # 2055 юнит-тестов (Node.js, 50 файлов): run-all.js, test-role-access.js, и др.'),
    ('ожидается `2034 passed, 0 failed`',
     'ожидается `2055 passed, 0 failed`'),
]
for old, new in reps:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Версии кэша и счётчики обновлены (4 замены)')

# Буллет Task 333 — сразу ПОСЛЕ буллета Task 332
entry = ('\n- **Task 333 — ШАПКА ШТОРКИ=ШАПКЕ СЕТКИ, РАЗДЕЛИТЕЛЬ ГРУПП В ШТОРКЕ, '
         'ПОДСВЕТКА СТРОК ШТОРКИ, ПОДПИСЬ «ВИД», АРХИВ ГОДОВОЙ ТАБЛИЦЫ, ПОЛОСА ФИО '
         'ЧЕРЕЗ ::after**: заявка: «В шторке итогов учёта, цвет фона шапки сделай как '
         'в шапке шахматки табеля, разделительную горизонтальную полосу между ячейками '
         'учёта сменных и дневных сотрудников сделай как в шахматке табеля между '
         'ячейками сменных и дневных сотрудников. Перекрёстная подсветка строк и '
         'столбцов ячеек табеля так же должна распространяться на строки с ячейками '
         'в шторке итогов учёта. Кнопку переключения вида подпиши "Вид". Архив в '
         'годовой таблице всё же нужен, обновление данных только общей кнопкой '
         '"Обновить" тулбара. Разделительная полоса между столбцом ФИО и ячейками '
         'шахматки должна быть со стороны ячеек сотрудников, что бы при прокрутки '
         'шахматки она не уходила.» 1) ШАПКА ШТОРКИ = ШАПКЕ СЕТКИ: .ws-tt-table th — '
         'background #131c28→#1e293b [Task 330: стальной сине-серый], светлая '
         '#efede5→#bfcad5 [отдельное правило]; ЯЧЕЙКА «Сотрудник» В ШАПКЕ (thead '
         'th.ws-tt-emp — год-мобайл и таблица архива) — ОТДЕЛЬНЫЕ правила '
         '(0,3,1) #1e293b/#bfcad5 [базовая th.ws-tt-emp (0,2,1) с фоном ТЕЛА '
         '#0e1621/#e9e7de перекрывается]. 2) РАЗДЕЛИТЕЛЬ ГРУПП В ШТОРКЕ (= сетка, '
         'Task 259): .ws-tt-table tbody tr.ws-group-first td { border-top: 2px solid '
         '#4a8fc7 } [светлая #6e8ba4]; КЛАСС ставят _renderTotalsMonth и '
         '_renderTotalsYearTable (обе таблицы + архив): prevTier-логика «первый '
         'дневной после сменных» по «тип» (сменный=0/дневной=1/прочий=2), список '
         '_EMPLOYEES уже отсортирован _sortEmployees; в border-collapse стык 2px '
         'побеждает 1px border-bottom строки выше — линия непрерывна. 3) ПОДСВЕТКА '
         'СТРОК ШТОРКИ (перекрестье Task 319/328 распространяется): _rowClass(ri, '
         'on) — ПОСЛЕ сетки (wsGridWrap tbody tr) ставит ТОТ ЖЕ класс ws-hover-row '
         'на строку #wsTtBody «.ws-tt-table tbody tr» С ТЕМ ЖЕ индексом [первая '
         'таблица в DOM = строки сетки; год: архив ниже — индексы совпадают с '
         'активными; за пределами длины — тихо; VM-моки без body — тихо]; '
         '_renderTotalsMonth/_renderTotalsYearTable ШТАМПУЮТ ws-hover-row из '
         '_hoverRow (переживает перерисовку — как сетка); CSS .ws-tt-table tbody '
         'tr.ws-hover-row td { box-shadow: inset 0 0 0 999px rgba(74,143,199,0.10) } '
         '[светлая rgba(42,93,143,0.06) — тот же приём/цвета, что у строки сетки; '
         'заливка поверх зебры и фона sticky-«Сотрудника»]. 4) ПОДПИСЬ «ВИД»: '
         '#wsViewBtn — span.ws-view-label «Вид» ПОСЛЕ трёх svg-иконок [иконки '
         'остаются: показывают текущий вид; «подпиши» = добавить подпись, НЕ '
         'замену]; CSS: .ws-view-btn { width: auto; padding: 0 9px; gap: 5px } + '
         'десктоп-медиа .ws-view-btn { width: auto } поверх .ws-cross-btn 34px + '
         '.ws-view-label { font-size: 13px; font-weight: 600 } [кнопка ШИРЕ '
         'иконки-подсветки, высота ряда прежняя]; подсказка #wsViewTip, aria-label, '
         'тосты, cycleView, замок роли «КИП ИОС дежурный» — БЕЗ изменений (Task '
         '332). 5) АРХИВ ГОДОВОЙ ТАБЛИЦЫ ВОЗВРАЩЁН (заявка: «Архив в годовой '
         'таблице всё же нужен»; Task 332 удалил вместе со столбцом — '
         'восстановлен): _renderTotalsYearTable — активные (this._EMPLOYEES, строки '
         'сетки) в ГЛАВНОЙ таблице + АРХИВ ОТДЕЛЬНЫМ БЛОКОМ ПОД ней: '
         '.ws-tt-arch-cap «Архив» + таблица .ws-tt-year.ws-tt-arch [годовой '
         'справочник md.employees (listEmployees includeArchived, _loadYearData) '
         'МИНУС активные таб-номера, _sortEmployees] со СВОЕЙ КОЛОНКОЙ «Сотрудник» '
         'НА ЛЮБОМ экране [в сетке архивных строк НЕТ — имена негде взять; '
         'ws-tt-emp sticky left: 0 — имена при горизонтальной прокрутке]; ГЛАВНАЯ '
         'таблица года ТОЖЕ рендерит th/td.ws-tt-emp, ДЕСКТОП прячет CSS '
         '[.ws-tt-table.ws-tt-year:not(.ws-tt-arch) display: none] — МОБАЙЛ год '
         'ПОЛУЧИЛ имена АКТИВНЫХ (мобильный регресс Task 332 закрыт); пустое '
         'состояние — «Нет сотрудников.» [активные И архив пусты]; общий билдер '
         'строки yearRow(emp, trCls) [классы: ws-group-first/ws-hover-row + имя + '
         '12 месяцев «д/ч» + Дней/Часов/Перераб. (дни) с тултипом часов]; '
         'обновление — ТОЛЬКО кнопкой «Обновить» ТУЛБАРА (своей кнопки НЕТ, Task '
         '332; loadGrid(true) сбрасывает _YEAR_DATA); ШИРИНА шторки — по ПЕРВОЙ '
         'таблице [архивная шире за счёт имён — прокручивается ползунком, имена-'
         'стик не мешают]; _syncTotalsRows берёт ПЕРВУЮ таблицу [строки архива — '
         'природной высоты, ниже строк сетки]. 6) СИНХРОНИЗАЦИЯ СКРОЛЛА (правка '
         '_attachTotalsSync, body-обработчик): прижимка «панель не уезжает дальше '
         'сетки» (Task 323: if (gridWrap.scrollTop < body.scrollTop) body.scrollTop '
         '= gridWrap.scrollTop) УДАЛЕНА — она делала хвост шторки (год: блок '
         '«Архив» ниже строк сетки) НЕДОСТУПНЫМ [воспроизведено: scrollTop=99999 → '
         'панель прыгала обратно к 1 — сетка прижата и тянет панель]; ТЕПЕРЬ: '
         'gridWrap.scrollTop = body.scrollTop [браузер сам клампит к максимуму '
         'сетки — сетка едет с панелью, ПОКА её хватает], ниже — сетка прижата к '
         'своему низу, панель ДОСКРОЛЛИВАЕТ блок архива до конца [выравнивание '
         'строк сохраняется в диапазоне сетки; в зоне архива строки итогов уже '
         'ушли вверх — рассинхрона не видно]; сетка→панель (колесо над сеткой) — '
         'как прежде. 7) ПОЛОСА ФИО/ЯЧЕЙКИ — ::after (заявка: «не уходила при '
         'прокрутке»): ДИАГНОСТИКА пиксельными пробами (scripts/task333-probe-'
         'separator.py, Chromium И Firefox): border-right 2px solid #4a8fc7 на '
         'sticky-ячейках (Task 332) в border-collapse: collapse УЕЗЖАЕТ при '
         'горизонтальной прокрутке [границы sticky-ячеек рисуются ТАБЛИЦЕЙ на '
         'исходной позиции ячейки; до прокрутки линия на границе, после '
         'scrollLeft=300 — линии НЕТ — в ОБОИХ движках]; ФИКС: ПСЕВДОЭЛЕМЕНТ '
         '::after на th/td.ws-emp-col { content: \'\'; position: absolute; right: '
         '0; top: -1px; bottom: -1px; width: 2px; background: #4a8fc7; '
         'pointer-events: none } [светлая #6e8ba4] — полоса ПРИНАДЛЕЖИТ '
         'sticky-ячейке (правый край ФИО = «со стороны ячеек сотрудников»), НЕ '
         'участвует в border-collapse — НЕ уезжает НИКОГДА [пробы: линия есть до И '
         'после прокрутки в Chromium+Firefox]; ЭКСПЕРИМЕНТЫ: left: 100% (за краем '
         'ячейки) ОБРЕЗАЕТСЯ overflow: hidden ФИО-эллипсиса (Task 325) — right: 0 '
         'виден и липнет [right: -1 частично, -2 clipped]; top/bottom -1px — '
         'непрерывность на стыках строк 98/100; прежние правила border-right 2px '
         'УДАЛЕНЫ [старые скриншоты 331/332 перепроверены: сеточный разделитель '
         'групп на DPR1 всегда рендерился антиалиасингом на дробных границах — '
         'шторка теперь ТОЧНО такая же, «как в шахматке»; на DPR2 оба — полные 2px '
         '#4a8fc7]. Верификация: сьют 2034→2055/0 [+21 tests/test-task333.js (CSS: '
         'шапка #1e293b/#bfcad5 + thead th.ws-tt-emp, разделитель ws-group-first '
         '2px, подсветка ws-hover-row inset, ::after right:0/2px/#4a8fc7/pe none + '
         'border-right 2px отсутствует, .ws-tt-arch-cap + :not(.ws-tt-arch) '
         'десктоп-скрытие, .ws-view-btn auto/13px, десктоп-медиа, SW v572/v573-'
         'guard; VM: _rowClass — обе таблицы/за пределами тихо, '
         '_renderTotalsMonth — штампы групп/подсветки, _renderTotalsYearTable — '
         'архив блоком/штампы/пусто]; актуализированы: test-task321 (год: архив '
         'блоком, 4 <tr>, «Сотрудник» в шапке, Сидоров в блоке), test-task323 (год: '
         'активные строками + архив блоком), test-task327 (строки с классами — '
         'подсчёт <tr[ >), test-task325 (окно ряда 3 4600→5000 — подпись «Вид»); '
         'SW-бамп v571→v572 [sw.js + 32 тест-файла], assertFalse «лишний '
         'инкремент» v572→v573 в 14]; браузер scripts/task333-browser-check.py '
         '39/39 (порт 8944; ДЕСКТОП 1280 тёмная [подпись «Вид» 13px/600, кнопка '
         'ШИРЕ иконки при той же высоте ряда; цикл полный→сменный→дневной→полный '
         'по кликам; шапка шторки #1e293b = шапке сетки; ws-group-first на первом '
         'дневном, border-top 2px solid rgb(74,143,199); ховер ячейки сетки → '
         'ws-hover-row ТОЙ ЖЕ строки шторки (индекс 2) + inset-заливка, уход '
         'курсора — снято в обоих; ГОД: подпись «Архив» ПОД главной таблицей, '
         'таблица архива 2 строки (Аркадий+Ульян), главная 12 активных, '
         '«Сотрудник» главной СКРЫТ на десктопе / у архива ВИДЕН + sticky, своей '
         'кнопки «Обновить» нет, прокрутка шторки ДОСТИГАЕТ архива (G9 — прижимка '
         'снята)]; СВЕТЛАЯ 1024 [шапка #bfcad5, разделитель rgb(110,139,164) 2px, '
         'полоса ФИО ОСТАЛАСЬ после прокрутки (пиксельная проба), блок «Архив» на '
         'месте]; МОБАЙЛ 375 [подпись «Вид»; месяц: колонка «Сотрудник» видна; '
         'ГОД: имена АКТИВНЫХ вернулись + архив с именами; вкладка переключается '
         'onclick-вызовом — кнопка перекрыта fixed-оверлеем шторки (top 10vh, '
         'пре-существующий дизайн)]; FIREFOX [полоса ФИО до/после прокрутки — '
         'пиксельная проба; 0 ошибок]; 0 JS-ошибок во всех контекстах); '
         'пиксельные пробы полосы (Chromium+Firefox: линия 2px #4a8fc7 до И после '
         'scrollLeft=300 — прежде исчезала); DPR2-замер разделителей групп (сетка '
         'И шторка — полные 2px #4a8fc7); регресс [актуализированы под 333]: 332 '
         '44/44 [C2/R — кнопка вида ШИРЕ с подписью; I/P — полоса через ::after '
         'computed; J3/J4/K — год: «Сотрудник» в DOM (десктоп скрыт) + архив '
         'блоком 13 строк], 331 38/38 [L — шапка года скрыта (кнопки «Обновить» '
         'нет, ⚠ скрыт); Q — годовая таблица влезает — ползунок ПУСТ, зона 12px], '
         '328 36/36 [C3 — порядок вид→перекрестье→Сформировать], 330 27/27; VLM ×3 '
         '(месяц: шапка=сетке, «Вид», без дефектов — «полос не видно» = '
         'даунсемплинг VLM, ОПРОВЕРГНУТО пиксельными замерами; год-архив после '
         'прокрутки: заголовок «Архив», ФИО, колонка, шапка единая, без '
         'дефектов); пруфы task333-proof-{month,year,desktop,light,mobile,'
         'year-archive}.png. ⚠️ ДЕПЛОЙ: ТОЛЬКО GitHub Pages + Ctrl+Shift+R ×1–2 '
         '(SW kipia-test-v571→v572) — Apps Script/листы НЕ трогать (только '
         'клиент) — см. scripts/DEPLOY-Task333-drawer-header-group-sep-hover-'
         'view-label-year-archive-fio-stripe.md.')
lines2 = src.split('\n')
insert_at = None
for i, l in enumerate(lines2):
    if l.startswith('- **Task 332 —'):
        insert_at = i + 1
        break
assert insert_at is not None, 'буллет Task 332 не найден'
lines2.insert(insert_at, entry)
src = '\n'.join(lines2)
print('Буллет Task 333 вставлен (после Task 332)')

with open('Системный_промт_для_приложения_КИПиА.md', 'w', encoding='utf-8') as f:
    f.write(src)
print('Готово: Системный_промт_для_приложения_КИПиА.md → post-Task 333')
