# DEPLOY — Task 333 (kip8test)

## Что изменилось (только клиент: index.html + sw.js)

Заявка: «В шторке итогов учёта, цвет фона шапки сделай как в шапке
шахматки табеля, разделительную горизонтальную полосу между ячейками
учёта сменных и дневных сотрудников сделай как в шахматке табеля.
Перекрёстная подсветка строк и столбцов ячеек табеля так же должна
распространяться на строки с ячейками в шторке итогов учёта. Кнопку
переключения вида подпиши "Вид". Архив в годовой таблице всё же
нужен, обновление данных только общей кнопкой "Обновить" тулбара.
Разделительная полоса между столбцом ФИО и ячейками шахматки должна
быть со стороны ячеек сотрудников, что бы при прокрутки шахматки она
не уходила.»

1. **Шапка шторки = шапке сетки**: `.ws-tt-table th` — #1e293b
   (было #131c28), светлая #bfcad5 (было #efede5); ячейка
   «Сотрудник» В ШАПКЕ (thead th.ws-tt-emp) — те же цвета.
2. **Разделитель групп в шторке**: tr.ws-group-first td —
   border-top 2px #4a8fc7 / #6e8ba4 (= сетка, Task 259); класс
   ставят _renderTotalsMonth/_renderTotalsYearTable (+архив).
3. **Подсветка строк шторки**: _rowClass ставит ws-hover-row и на
   строку шторки (тот же индекс); штамп из _hoverRow в рендерах;
   inset-заливка как у строки сетки (0.10 / 0.06).
4. **Кнопка вида**: подпись «Вид» (span.ws-view-label, 13px/600,
   width auto) — иконки/подсказка/цикл/замок роли без изменений.
5. **Архив годовой таблицы ВОЗВРАЩЁН**: отдельный блок «Архив» ПОД
   основной таблицей (подпись + таблица .ws-tt-arch со своей колонкой
   «Сотрудник» на любом экране); ГЛАВНАЯ таблица года — ws-tt-emp
   рендерится, десктоп CSS прячет (мобайл получил имена активных —
   регресс Task 332 закрыт); обновление — ТОЛЬКО «Обновить» тулбара.
6. **Синхронизация скролла**: прижимка «панель не уезжает дальше
   сетки» удалена (делала архив недоступным); сетка едет с панелью
   пока хватает, ниже — прижата к низу, панель доскролливает архив.
7. **Полоса ФИО/ячейки**: border-right 2px → ::after у sticky-ячеек
   (right: 0, 2px, #4a8fc7/#6e8ba4, pointer-events none) — полоса
   принадлежит ячейкам ФИО и НЕ уезжает при прокрутке (баг
   border-collapse воспроизведён в Chromium и Firefox, закрыт).

## Версия кэша

`sw.js`: **kipia-test-v571 → kipia-test-v572**

## Пользователю

- Открыть https://bloknett-design.github.io/kip8test/
- **Ctrl+Shift+R ×1–2** (обновление Service Worker до v572)
- Apps Script / листы НЕ трогать — изменения только клиентские

## Верификация

- Тесты: **2055/0** (было 2034; +21 test-task333.js)
- Браузер task333-browser-check.py: **39/39** (десктоп/светлая/
  мобайл/Firefox, 0 JS-ошибок; архив достигается прокруткой)
- Пиксельные пробы: полоса ФИО до/после прокрутки — Chromium+Firefox
- Регресс: 332 44/44, 331 38/38, 328 36/36, 330 27/27
- VLM ×3 (месяц/год-архив) — без дефектов
