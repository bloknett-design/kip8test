# -*- coding: utf-8 -*-
# Обновление системного промта kip8test до post-Task 314
# («.» → символ «·» + фон пустой ячейки; мероприятия «Инструктажи» —
#  бейджи-«иконки» во всех ячейках; локальная копия данных графика
#  (мгновенное открытие) + кнопка «Обновить» данных)
with open('Системный_промт_для_приложения_КИПиА.md', encoding='utf-8') as f:
    lines = f.readlines()

new3 = '''> **Версия документа:** 2026-09-04 (post-Task 314: «.» → СИМВОЛ «·» + ФОН ПУСТОЙ ЯЧЕЙКИ; МЕРОПРИЯТИЯ «ИНСТРУКТАЖИ» — БЕЙДЖИ-«ИКОНКИ» ГЛАВНАЯ ФОРМА ПОКАЗА (и на днях отсутствия/плана); ЛОКАЛЬНАЯ КОПИЯ ДАННЫХ ГРАФИКА (localStorage kip8_ws_cache_v1) + КНОПКА «ОБНОВИТЬ». Заявка пользователя: «Замени символ кода "." на символ "·", а цвет фона ячейки сделай как в пустых, и напиши код цвета - заменю в таблице. Мероприятия из таблицы "Инструктажи" должны отображаться в ячейках шахматки маленькими бейджами-«иконками». Сделай, что бы последняя загруженная информация по графику работы сохранялась локально в приложении на устройстве пользователя, что бы каждый раз при открытии графика, он открывался моментально без ожидания подгрузки новых данных, а функцию обновления данных графика с подгрузкой новых мероприятий и прочего сделай с помощью кнопки обновления данных графика (не формирования)». СОДЕРЖАНИЕ: 1) «.» = ПУСТАЯ ЯЧЕЙКА: _renderCell рендерит «.»-статус как пустую — символ «·» (U+00B7, тот же что у пустых), фон — CSS-класс ws-dot-code (тёмная var(--bg-primary), светлая #eef0f2 = фон ЯЧЕЕК сетки Task 250, правило выше специфичности базового; выходные — розовые как пустые), inline-цвет листа для «.» НЕ ставится (совпадение с пустой в любой теме); showMainCode = статус И НЕ «.» И НЕ мероприятие; попап: строка «.» — символ «·», свотч ws-swatch-dot (фон темы, без inline-стиля); select «Дополнительно…»: метка «· — Плановый выходной день» (value «.» — данные не меняются); fallback-набор: цвет «.» #FAF9F5→#EEF0F2; ЦВЕТ ДЛЯ ЛИСТА: #EEF0F2 (сообщён пользователю). 2) БЕЙДЖИ МЕРОПРИЯТИЙ: статус-мероприятие (И/ОБ/ПЗ/ПР/* основным кодом — generateMonth днём события без смены) БЕЗ большого кода: ячейка «·» + СПЛОШНОЙ бейдж; записи в «Инструктажи» нет — ВИРТУАЛЬНЫЙ бейдж из статуса; бейджи теперь ВИДНЫ и на днях отсутствия (ОТ/Б/… — раньше скрывались по Task 303, сплошные: день сформирован) и на пустых/планах (ПУНКТИРНЫЕ — «заполнится при Сформировать»); смена — основной код, мероприятие — мини-бейдж в правом нижнем углу; _ABSENCE_CODES остаётся справочником (бейджи не фильтруются). 3) ЛОКАЛЬНАЯ КОПИЯ + «ОБНОВИТЬ»: localStorage kip8_ws_cache_v1 (схема как kip8_cj_cache): справочники (коды/шаблоны/сотрудники) + отпуска ПО ГОДАМ + записи/мероприятия ПО ВИДАМ «год-месяц» (лимит 12, вытеснение старых); _ymKey/_cacheRead/_restoreCachedView (init: полный кэш → рендер БЕЗ сети мгновенно)/_cacheWrite (после каждой загрузки с сети; loadGrid(true) ×11 у мутаций)/_updateCacheStamp («данные от 04.09, 14:22»); НОВАЯ кнопка #wsRefreshBtn («Обновить», ВСЕМ ролям, refreshData(): _loadStatusCodes(true) — тихий, не затирает живые коды fallback-набором) + loadGrid(true): сетка ОСТАЁТСЯ на экране (не мигает), сбой — тост, старые данные остаются; тост «Данные графика обновлены»; это НЕ «Сформировать» (только чтение); штамп #wsCacheStamp рядом; побочно — офлайн-толерантность. СЕРВЕР и листы НЕ менялись; значение «.» в данных остаётся «.». Тесты: 1546 → 1584/0 (+38 test-task314.js; актуализированы task312/work-schedule/work-events/306/309/310/311/313; SW-бамп v552→v553 в 15 тест-файлах). Верификация: task314-browser-check.py 36/36 (4 контекста, порт 8937: кэш-открытие 0 запросов, кнопка/спиннер/тост/штамп, «·»/ws-dot-code/фон=пустой, бейджи сплошные/пунктирные, попап/select «·», светлая #eef0f2, смена месяца, офлайн, зритель, мобильный 375px); регресс 298 34/34, 303 20/20, 304 10/10, 305 20/20, 306 29/29, 307 20/20, 308 20/20, 309 26/26, 310 20/20, 311 24/24, 312 31/31, 313 30/30 (актуализированы под Task 314); VLM ×4. ПЕРЕНОС В kip8: НЕ выполнен (Tasks 312–314 вместе ожидают проверки пользователем в kip8test; следующий шаг — перенос 312+313+314, kip8 v416→v417 одним инкрементом). Деплой пользователя: GitHub Pages + Ctrl+Shift+R ×1–2; Apps Script НЕ трогать; в листе «Коды_статусов» заменить цвет «.» на #EEF0F2.)
'''

assert lines[2].startswith('> **Версия документа:**')
lines.insert(2, new3 if new3.endswith('\n') else new3 + '\n')
prev = [i for i, l in enumerate(lines[:40]) if l.startswith('> **Версия документа (предыдущая):**')]
while len(prev) > 12:
    del lines[prev[-1]]
    prev = [i for i, l in enumerate(lines[:40]) if l.startswith('> **Версия документа (предыдущая):**')]
print('Строка 3 вставлена; цепочка «предыдущих»:', len(prev))

src = ''.join(lines)

reps = [
    ('> **Текущая версия кэша:** `kipia-test-v552`',
     '> **Текущая версия кэша:** `kipia-test-v553`'),
    ('| `kip8test` | PWA | `kipia-test-v552` |',
     '| `kip8test` | PWA | `kipia-test-v553` |'),
]
for old, new in reps:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Версии кэша обновлены (2 замены)')

# Счётчики тестов
reps2 = [
    ('├── tests/                      # 1546 юнит-тестов (Node.js, 31 файлов)',
     '├── tests/                      # 1584 юнит-тестов (Node.js, 32 файлов)'),
    ('ожидается `1546 passed, 0 failed`', 'ожидается `1584 passed, 0 failed`'),
    ('# Ожидается: 1546 passed, 0 failed', '# Ожидается: 1584 passed, 0 failed'),
]
for old, new in reps2:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Счётчики тестов обновлены (3 замены)')

# Аннотация Task 251: «.» в попапе — символ «·» (Task 314)
old251 = 'Task 313: над окном кодов открывается ОТДЕЛЬНОЕ окно «Мероприятия в этот день» (события ячейки: цвет/код/название, ✎/✕) + колонка сегодняшней даты слегка подсвечена (ws-today-col/ws-today); «Дополнительно…» открывает прежный sheet'
new251 = old251  # заглушка — реальный анкор ниже
old251 = 'Task 313: над окном кодов открывается ОТДЕЛЬНОЕ окно «Мероприятия в этот день» (события ячейки: цвет/код/название, ✎/✕) + колонка сегодняшней даты слегка подсвечена (ws-today-col/ws-today); «Дополнительно…» открывает прежний sheet'
new251 = 'Task 313: над окном кодов открывается ОТДЕЛЬНОЕ окно «Мероприятия в этот день» (события ячейки: цвет/код/название, ✎/✕) + колонка сегодняшней даты слегка подсвечена (ws-today-col/ws-today); Task 314: строка «.» — символ «·», свотч ws-swatch-dot (фон пустой ячейки, без inline-стиля); «Дополнительно…» открывает прежний sheet'
assert src.count(old251) == 1, ('old251', src.count(old251))
src = src.replace(old251, new251)
print('Аннотация Task 251 (символ «·» в попапе) обновлена')

# Аннотация Task 312 (буллет): цвет «.» уточнён Task 314 — #EEF0F2
old312b = '3) ЦВЕТ «.» (Плановый выходной день): код для листа «Коды_статусов» — #FAF9F5 (точный фон пустых ячеек с точкой в светлой теме, --bg-primary; пользователь заменяет значение в листе сам); fallback клиента (офлайн) синхронизирован #CFD8DC→#FAF9F5; в тёмной теме пустые ячейки #1a2233, а лист хранит ОДИН цвет — «.»-ячейки светлые в обеих темах (осознанный выбор пользователя; код в ячейке и рамки не менялись).'
new312b = '3) ЦВЕТ «.» (Плановый выходной день): код для листа «Коды_статусов» — #FAF9F5 (точный фон пустых ячеек с точкой в светлой теме, --bg-primary; пользователь заменяет значение в листе сам); fallback клиента (офлайн) синхронизирован #CFD8DC→#FAF9F5; в тёмной теме пустые ячейки #1a2233, а лист хранит ОДИН цвет — «.»-ячейки светлые в обеих темах (осознанный выбор пользователя; код в ячейке и рамки не менялись). Task 314 УТОЧНИЛ: символ «.» → «·», «.»-ячейка красится CSS-классом ws-dot-code как ПУСТАЯ (inline-цвет листа НЕ применяется, совпадение с пустой в любой теме), актуальный код цвета для листа — #EEF0F2 (фон ЯЧЕЕК сетки светлой темы, не страницы), fallback #EEF0F2.'
assert src.count(old312b) == 1, ('old312b', src.count(old312b))
src = src.replace(old312b, new312b)
print('Аннотация Task 312 (цвет «.» — #EEF0F2, CSS-класс) обновлена')

entry = ('\n- **График работы — «.» → «·» + фон пустой ячейки, бейджи-«иконки» мероприятий во всех ячейках, локальная копия графика + кнопка «Обновить» (Task 314)**: '
         'по заявке пользователя: 1) СИМВОЛ «·» + ФОН ПУСТОЙ: «.»-статус рендерится как ПУСТАЯ ячейка (_renderCell: isDotCode/isEventStatus → showMainCode = статус И НЕ «.» И НЕ мероприятие): символ «·» (U+00B7 — тот же, что у пустых), фон — CSS-класс ws-dot-code (тёмная var(--bg-primary), светлая #eef0f2 = фон ЯЧЕЕК сетки Task 250, правило выше специфичности базового; выходные — розовые как пустые .ws-weekend.ws-status-empty), inline-цвет из листа для «.» НЕ ставится — совпадение с пустой в ЛЮБОЙ теме, класс — маркер для тестов; попап: строка «.» показывает «·», свотч ws-swatch-dot (фон пустой ячейки темы, БЕЗ inline-стиля); select шита «Дополнительно…»: метка «· — Плановый выходной день» (value «.» — данные/сервер не меняются); fallback-набор кодов: цвет «.» #FAF9F5→#EEF0F2; ЦВЕТ ДЛЯ ЛИСТА «Коды_статусов» — #EEF0F2 (сообщён пользователю; клиент красит «.»-ячейки CSS-классом, цвет листа на фон «.»-ячейки НЕ применяется). '
         '2) БЕЙДЖИ-«ИКОНКИ» — ГЛАВНАЯ ФОРМА МЕРОПРИЯТИЙ: статус-мероприятие (И/ОБ/ПЗ/ПР/* в Записи_графика — generateMonth пишет его днём события без смены) больше НЕ большой код по центру: ячейка выглядит пустой («·»), событие — СПЛОШНОЙ бейдж; записи в «Инструктажи» уже нет — ВИРТУАЛЬНЫЙ бейдж из самого статуса (день не «теряет» мероприятие); бейджи ВИДНЫ и на днях отсутствия (ОТ/Б/… — раньше скрывались по правилу Task 303, событие жило в окне «Мероприятия в этот день») — сплошные: день сформирован; пустая ячейка (в т.ч. план отпуска) — ПУНКТИРНЫЙ бейдж («заполнится при Сформировать»); смена — основной код, мероприятие — мини-бейдж в правом нижнем углу (точка переработки — в правом верхнем, конфликтов нет); бейдж плановой смены (Task 305) — при пустом evHtml (событие важнее); _ABSENCE_CODES остаётся справочником семейства отсутствий (документация/тесты), бейджи на этих днях не фильтруются. '
         '3) ЛОКАЛЬНАЯ КОПИЯ + КНОПКА «ОБНОВИТЬ»: localStorage kip8_ws_cache_v1 (схема как kip8_cj_cache — ключ общий для kip8test/kip8): справочники (коды/шаблоны/сотрудники) + планы отпусков ПО ГОДАМ + записи/мероприятия ПО ВИДАМ «год-месяц» (последние 12, вытеснение самых старых); _ymKey/_cacheRead (битый JSON = нет кэша)/_restoreCachedView (init: полный кэш текущего вида → рендер БЕЗ сети, мгновенно — заявка «открывался моментально»; кэша нет — прежний путь с «Загрузка…»)/_cacheWrite (после КАЖДОЙ загрузки с сети; loadGrid(true) — 11 мест мутаций: пакетное сохранение, генерация месяц/год, правка/удаление мероприятий и отпусков)/_updateCacheStamp (штамп «данные от 04.09, 14:22» — #wsCacheStamp рядом с кнопкой, ellipsis на узких); НОВАЯ кнопка #wsRefreshBtn (.ws-refresh-btn, 34px в общем правиле ряда, ВСЕМ ролям — зритель обновляет данные так же, как редактор): refreshData() → _loadStatusCodes(true) (тихий: сбой НЕ затирает живые коды fallback-набором) + loadGrid(true) + тост «Данные графика обновлены» + блокировка повторных кликов + крутящаяся иконка (@keyframes wsRefreshSpin); это НЕ «Сформировать» (генерации записей НЕТ, только чтение сотрудников/записей/мероприятий/планов/кодов); loadGrid(force): force=true — сеть обязательно + запись в кэш, сетка ОСТАЁТСЯ на экране (не мигает «Загрузка…» — индикатор на кнопке; «Загрузка…» — только первый показ вида без данных), сбой при живой сетке — НЕ стирает её (тост достаточен, старые данные остаются); force=false — сперва локальная копия; побочно — офлайн-толерантность (без сети график открывается из локальной копии). Значение кода в данных: «.» остаётся «.» в Записи_графика/листах/сервере — меняется только ОТОБРАЖЕНИЕ. '
         'Сервер WorkSchedule.gs/листы/Apps Script/«Сформировать» — НЕ менялись; окно «Мероприятия в этот день» и окно кодов (Task 313), подсветка «сегодня» (Task 313), рамка д/н (Task 309/310), карточка сотрудника, шторки форм — живы. Верификация: сьют 1546→1584/0 (+38 tests/test-task314.js; актуализированы test-task312 [«·»/фон #EEF0F2]/test-work-schedule [showMainCode/loadGrid(true)]/test-work-events [бейджи на днях отсутствия]/test-task306/309/310/311/313; SW-бамп v552→v553 в 15 тест-файлах), task314-browser-check.py 36/36 (порт 8937, 4 контекста: десктоп-Админ [кэш-открытие 0 запросов workSchedule.*; кнопка — спиннер/блокировка/тост/штамп/запись кэша; «·»/ws-dot-code/без inline-фона/фон=фон пустой рабочей ячейки; И-день: «·»+сплошной бейдж #B3E5FC; ОТ+бейдж; пустая+пунктирный; Д+бейдж; Б без бейджей; попап «·»/ws-swatch-dot; select «·»; светлая #eef0f2; повторное открытие — снова 0 запросов], смена месяца [некэшированный — сеть, возврат — мгновенно], офлайн [график из локальной копии; «Обновить» при сбое — сетка НЕ стёрта, тост], зритель [кнопка доступна, «Сформировать» скрыта], мобильный 375px [кнопка/штамп в кадре, обновление работает], 0 JS-ошибок); регресс 298 34/34 (D — ws-dot-code+«·»; M — «·» в попапе), 303 20/20 (H — «·»+сплошной бейдж И; I — ОТ+бейдж), 304 10/10, 305 20/20, 306 29/29 (C2/F — пунктирный ПР на дне плана, бейджа СМЕНЫ у дневного нет; I2 — «Обновить» в тулбаре), 307 20/20, 308 20/20, 309 26/26, 310 20/20, 311 24/24, 312 31/31 (D4 — «·»; F — #EEF0F2+«·»), 313 30/30 (актуализированы под Task 314), VLM ×4 (badges/light-dot/popup-dot/mobile — без дефектов). SW kipia-test-v552→v553. ДЕПЛОЙ: только фронтенд — GitHub Pages + Ctrl+Shift+R ×1–2; Apps Script НЕ трогать; в листе «Коды_статусов» заменить цвет «.» на #EEF0F2 — см. scripts/DEPLOY-Task314-dot-badges-cache.md')
lines2 = src.split('\n')
insert_at = None
for i, l in enumerate(lines2):
    if l.startswith('- **График работы — подсветка сегодняшней даты'):
        j = i + 1
        while j < len(lines2) and lines2[j].startswith('  '):
            j += 1
        insert_at = j
        break
assert insert_at is not None, 'точка вставки (после буллета Task 313) не найдена'
lines2.insert(insert_at, entry)
src = '\n'.join(lines2)

with open('Системный_промт_для_приложения_КИПиА.md', 'w', encoding='utf-8') as f:
    f.write(src)
print('Промт обновлён до post-Task 314: строка версии, цепочка, кэш v553, счётчики 1584/32, аннотации 251/312, буллет Task 314')
