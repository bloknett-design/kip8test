# DEPLOY — Task 307: вкладка «Сотрудники» удалена, кнопка «+ Сотрудник» в тулбаре

**Дата:** 2026-09-03 · **Репо:** kip8test · **SW:** kipia-test-v545 → **v546**
**Заявка пользователя:** «В разделе "График работы" убери вкладку сотрудники, а
кнопку "Добавить сотрудника" перемести в бар над шахматкой к остальным кнопкам».

## Что изменилось (только фронтенд)

1. **Страница «Сотрудники» удалена целиком**:
   - див `#page-work-schedule-employees` (список + бар добавления);
   - кнопка «Сотрудники» из субнавигации на всех страницах модуля
     (осталось **3 полосы × 3 кнопки**: Шахматка / Инструктажи / Отпуска);
   - хук `navigateTo` → `initEmployeesPage`;
   - записи `PAGE_PARENTS` / `PAGE_LABELS` и `'work-schedule-employees'`
     из `_WORK_SCHEDULE_PAGES` (карта ролей);
   - JS-методы `initEmployeesPage` / `loadEmployees` / `_renderEmployees`;
   - мёртвый CSS (`.ws-emp-card*`, `.ws-emp-card-header`, `.ws-employees-list`).
2. **Кнопка «+ Сотрудник»** (`#wsEmpBtn`, класс `.ws-addemp-btn`) — в баре
   над шахматкой (`ws-toolbar-main`): после селектов месяца/года, ДО
   «Сформировать». Нейтральный стиль в духе селектов, высота 34px,
   `title="Добавить сотрудника"`. Видимость — по `_canEdit` (как у
   «Сформировать»: Админ и пишущие роли видят, «ИТР8 pro» — нет).
   Клик → прежний bottom-sheet «Новый сотрудник» (`#wsEmpSheet`).
3. **После добавления сотрудника перезагружается шахматка**
   (`loadGrid()` — новая строка сотрудника в сетке) вместо списка.
4. `.ws-toolbar-main` — `flex-wrap: wrap` (мобильный: ряд из 5 элементов
   аккуратно складывается; десктоп — `nowrap` в media).

## Чего НЕ менялось

- **Сервер `WorkSchedule.gs` — БЕЗ изменений** (Apps Script НЕ трогать!).
- Листы Google-таблицы — без изменений; миграции не нужны.
- API `workSchedule.addEmployee` — прежний; `_loadEmployees` жив
  (шахматка/инструктажи/отпуска).
- Справочник сотрудников виден: строки шахматки, попап ячейки, селекты форм.

## Деплой (единственный шаг)

1. **GitHub Pages** kip8test — задеплоится автоматически после пуша (1–3 мин).
2. Пользователи: **Ctrl+Shift+R ×1–2** (обновление Service Worker v546).

## Верификация

- Сьют: **1444 passed / 0 failed** (+12 новых тестов Task 307).
- `scripts/task307-browser-check.py` — **22/22** (Playwright, мок fetch:
  субнав 3 кнопки; кнопка в тулбаре; шторка; submit → новая строка в
  шахматке; «ИТР8 pro» — кнопка скрыта; 375px/1280px; 0 JS-ошибок).
- Регресс: task298 29/29, task303 20/20, task304 10/10, task305 20/20,
  task306 29/29; task299-mock 76/76. VLM ×2 (скриншоты task307-proof-*.png).

## Откат

`git revert` коммита Task 307 в kip8test; SW v546→v545; Ctrl+Shift+R.
Страница «Сотрудники» и вкладка вернутся (данные в таблице не менялись).

## Что НЕ делать

- НЕ запускать StatusCodesInit / TabNumbersFix / RoleMatrixInit (уже выполнены).
- НЕ обновлять Apps Script (WorkSchedule.gs не менялся).
- НЕ трогать листы таблицы.
