# -*- coding: utf-8 -*-
# Обновление системного промта kip8test до post-Task 321
# (год в списке — РОВНО три года: один до + текущий + один после, Task 320
#  делал ±3; раздел переименован «График работы» → «Табель учёта рабочего
#  времени» — 5 мест UI, id/права не тронуты; ИТОГИ УЧЁТА — сворачиваемая
#  панель под шахматкой: месяц/год, дни/часы, архивный персонал, кэш года)
with open('Системный_промт_для_приложения_КИПиА.md', encoding='utf-8') as f:
    lines = f.readlines()

new3 = '''> **Версия документа:** 2026-09-05 (post-Task 321: ГОД В СПИСКЕ ВЫБОРА — РОВНО ТРИ ПУНКТА (заявка уточнена: «в общем количестве из трёх лет, один год до текущего года, один год после текущего»): init #wsYearSel цикл this._year−1 … this._year+1 (для 2026: 2025/2026/2027; Task 320 делал ±3 — 7 пунктов, избыточно); РАЗДЕЛ ПЕРЕИМЕНОВАН «График работы» → «ТАБЕЛЬ УЧЁТА РАБОЧЕГО ВРЕМЕНИ» — 5 мест UI: заголовок #page-work-schedule (на десктопе — хвост крошек «Главная / Документация / Документация ИОС / Табель учёта рабочего времени»), кнопка #workScheduleMenuBtn на page-docs-ios (субметка «Шахматка сменного и дневного персонала» БЕЗ изменений), PAGE_LABELS['work-schedule'], SUBSECTIONS['work-schedule'].label (ярлык закрепления), пункт сайдбара #sidebarWorkScheduleBtn — ИДЕНТИФИКАТОРЫ (work-schedule) и ПРАВА RoleMatrix («График работы — просмотр/редактирование») НЕ менялись, переименованы ТОЛЬКО надписи; ИТОГИ УЧЁТА — дни и рабочее время по итогам МЕСЯЦА и ГОДА (заявка: «так чтобы они не мешали работать с месячным графиком»): сворачиваемая секция ПОД шахматкой — бар-переключатель #wsTotalsBar «Итоги учёта — дни и часы» (клик/Enter/Пробел, role=button, aria-expanded, шеврон разворачивается) + панель #wsTotalsPanel СВЁРНУТА ПО УМОЛЧАНИЮ (сетка занимает всю высоту, как прежде; раскрытие — панель ≤46vh со своим скроллом, _fitGrid пересчитывает строки, сворачивание возвращает высоту; mobile — тап-зона бара 44px), вкладки «Месяц»/«Год» (#wsTtTabMonth/#wsTtTabYear + инфо #wsTtInfo + «Обновить» #wsTtRefresh только у года): МЕСЯЦ — по каждому сотруднику открытого месяца: Явки (дни работы), Часы, Д (дневные смены), Н (ночные), неявки ОТ/У/ОВ/Б/ПР, Прочие (И/ОБ/ПЗ/*·), Всего + строка «Итого по подразделению»; инфо: месяц/год + норма производственного календаря (ProdCalendar.monthStats 40-час) + «включая несохранённые правки (N)»; ДАННЫЕ = тот же эффективный вид, что сетка (_totalsEffectiveEntries: записи + _PENDING, __delete исключает, новые дни добавляются) — правка ячейки СРАЗУ видна в итогах (перерисовка из _renderGrid через _renderTotalsIfOpen); ЧАСЫ ПО КОДАМ (_codeHours): Д/Н/д/н = 12, Д8 = 8, Д7,2 = 7,2; неявки/пустые = 0; НЕЗНАКОМЫЙ код — часы из названия «NN-час» (справочник «Коды_статусов»), иначе 0; формат чисел с запятой (_fmtTotalsNum: 43.2 → «43,2»); ГОД — 12 колонок-месяцев «явки/часы» («11/132», пустые «—») + годовые Дней/Часов по каждому сотруднику + строка «Итого» по месяцам; строки = сотрудники ГОДА ВКЛЮЧАЯ АРХИВНЫХ (listEmployees includeArchived=true, пометка «архив» — уволенный в середине года остаётся в годовых итогах; в месячной таблице и сетке архива НЕТ — listEmployees без includeArchived); ДАННЫЕ ГОДА = 12 запросов workSchedule.listEntries (параллельно Promise.all; СБОЙ месяца → пустой список + счётчик failed + предупреждение «не загружено месяцев: N — нажмите Обновить», промис не падает) + 1 запрос справочника (сбой → активные из текущего вида); КЭШ В ПАМЯТИ _YEAR_DATA {year, ts, months:{1..12}, employees, failed} — сбрасывается: loadGrid(force) (кнопка «Обновить» бара, увольнение/сотрудник/генерация — if (force) this._YEAR_DATA = null), кнопка «Обновить» панели (reloadTotals), смена года (год кэша ≠ текущий → перезагрузка); после загрузки месяца свежие записи подкладываются в кэш года БЕЗ новой сети (loadGrid .then: _YEAR_DATA.months[this._month] = _ENTRIES + перерисовка открытой годовой вкладки); серверных эндпоинтов НЕТ — используются существующие listEntries/listEmployees. Тесты: 1742 → 1783/0 (+42 tests/test-task321.js: год [JS-цикл −1/+1, VM: 3 опции 2025..2027/selected 2026/2024,2028 нет], переименование [заголовок новое есть/старого нет, кнопка+субметка, PAGE_LABELS, SUBSECTIONS, сайдбар, id/права не тронуты], HTML [бар: клик/клавиатура/aria/шеврон; панель hidden; вкладки/инфо/кнопка/тело; порядок — ПОСЛЕ wsGridWrap внутри страницы], CSS [бар flex/cursor/hover/focus-visible/flex-shrink 0, шеврон rotate, панель max-height 46vh + overflow + [hidden], вкладка active #4ac771/светлая #1d7a37, светлая панель #e9e7de/бар #f4f2ec, sticky th, ws-tt-total border-top, часы зелёные, мобайл тап-зона 44px], VM [_codeHours: карта + фолбэк «10-час»/«7,2-час»/без часов; _totalsZero форма; _totalsAgg: явки/дневные/ночные/часы/семейства/прочие/total + grand + пустой таб игнорируется; _totalsEffectiveEntries: правка поверх записи/__delete/новый день; toggleTotals: hidden/aria/класс/_renderTotals/_fitGrid ×2; setTotalsTab: классы + «Обновить» только год; reloadTotals: сброс кэша + рендер; _renderTotalsIfOpen: закрытая не рендерит; _renderGrid вызывает _renderTotalsIfOpen; loadGrid(force) сброс + синхронизация месяца; _renderTotalsMonth: строки/значения/«Итого»/формат «39,2»/норма ProdCalendar-мок/несохранённые правки/пустые сотрудники; _fmtTotalsNum запятая; _loadYearData: 12 listEntries + 1 listEmployees includeArchived/months 1..12/failed=0/кэш, сбой 2 месяцев → failed=2 + пустые списки + промис жив; _renderTotalsYear: лоадер + загрузка + таблица, кэш другого года → перезагрузка; _renderTotalsYearTable: 12 колонок/д-ч формат/архив/суммы/failed-инфо], SW v560 + assertFalse v561; актуализированы: test-task320 [год: цикл −1/+1, VM 3 опции 2025..2027, ±3 отсутствует, SW-блок переписан под v560], test-work-schedule [Task 249: PAGE_LABELS + заголовок страницы = «Табель учёта рабочего времени»; Task 267: SUBSECTIONS label + метки цепочки], test-flowmeter-validation [Task 241: сайдбар текст]; SW-бамп v559→v560 в sw.js + 21 тест-файле, assertFalse «лишний инкремент» → v561 в test-task315/317/318/319; run-all.js подключение). Верификация: task321-browser-check.py 45/45 (порт 8941, мок fetch + закрытые legalic/isDayOff 404, 3 контекста: десктоп-Админ 1280 ТЁМНАЯ [год РОВНО 3 пункта 2025..2027/текущий выбран/±3 нет; переименование: заголовок-крошки/сайдбар/кнопка docs-ios (субметка прежняя); бар итогов ПОД сеткой/панель СКРЫТА; раскрытие: панель видна/вкладка Месяц active/сетка ужалась (_fitGrid); таблица месяца: Иванов явки 4/часы 43,2 (запятая)/Д 3/Н 1/ОТ 2/Б 1/прочие 1/всего 8, «Итого» 6/59,2; инфо: месяц + норма + несохранённые правки; несохранённая правка (Д→Б через _PENDING + _renderGrid) — итоги живьём: явки 3/часы 31,2/Б 2/пометка «(1)»; год: вкладка active + «Обновить» видна, 12 запросов listEntries, 12 колонок янв..дек, формат 1/12, архив «Сидоров» май 1/12 + итог 1/12, архивный НЕ в сетке (2 строки), Иванов год 5 дней/55,2 ч; «Обновить» — ещё 12 запросов + перерисовка; СБОЙ марта — «не загружено месяцев: 1»; сворачивание — панель hidden + высота сетки восстановлена; 0 JS-ошибок], светлая 1024 [год 3, панель rgb(233,231,222), таблица], мобильный 375 touch [тап-зона ≥44px, тап раскрыл, .ws-tt-scroll]); регресс браузером: 306 29/29, 309 26/26, 310 20/20, 311 24/24, 312 31/31, 313 30/30, 314 36/36, 315 33/33, 316 28/28, 317 49/49, 318 45/45, 319 48/48, 320 24/24 [актуализирован под 3 года: C/D/F/R/T/W]; VLM ×4 (месяц/год/светлая/мобильный — без дефектов; замечания VLM о «Сотрудник +» и бейджах ячеек — дизайн-решения Tasks 311/314, НЕ дефекты). ⚠️ ДЕПЛОЙ: ТОЛЬКО GitHub Pages + Ctrl+Shift+R ×1–2 (SW kipia-test-v559→v560) — Apps Script/листы НЕ трогать (клиентские правки, новых эндпоинтов нет). ПЕРЕНОС В kip8: НЕ выполнен — Tasks 315–321 одной партией после проверки (k8 v417→v418, включая scripts/*.gs от 318 и 320; Task 321 серверных правок НЕ содержит) — см. scripts/DEPLOY-Task321-year3-rename-totals.md)
'''

assert lines[2].startswith('> **Версия документа:**')
lines.insert(2, new3 if new3.endswith('\n') else new3 + '\n')
print('Строка 3 вставлена (цепочка версий)')

src = ''.join(lines)

reps = [
    ('> **Текущая версия кэша:** `kipia-test-v559`',
     '> **Текущая версия кэша:** `kipia-test-v560`'),
    ('| `kip8test` | PWA | `kipia-test-v559` |',
     '| `kip8test` | PWA | `kipia-test-v560` |'),
]
for old, new in reps:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Версии кэша обновлены (2 замены)')

# Счётчики тестов
reps2 = [
    ('├── tests/                      # 1742 юнит-тестов (Node.js, 38 файлов): run-all.js, test-role-access.js, и др.',
     '├── tests/                      # 1783 юнит-тестов (Node.js, 39 файлов): run-all.js, test-role-access.js, и др.'),
    ('ожидается `1742 passed, 0 failed`', 'ожидается `1783 passed, 0 failed`'),
    ('# Ожидается: 1742 passed, 0 failed', '# Ожидается: 1783 passed, 0 failed'),
]
for old, new in reps2:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Счётчики тестов обновлены (3 замены)')

# Пометка в буллете Task 320 (год ±3 → Task 321 сократил до ±1)
old320 = ('1) ГОД В СПИСКЕ (клиент, init): цикл от this._year−3 до this._year+3 — '
          '7 пунктов (2023..2029 для 2026; было −1…+2 — 4 пункта); #wsMonthSel не тронут.')
new320 = ('1) ГОД В СПИСКЕ (клиент, init): цикл от this._year−3 до this._year+3 — '
          '7 пунктов (2023..2029 для 2026; было −1…+2 — 4 пункта) → Task 321: заявку '
          'УТОЧНИЛИ — РОВНО 3 года (this._year−1…+1); #wsMonthSel не тронут.')
n = src.count(old320)
assert n == 1, (old320[:60], n)
src = src.replace(old320, new320)
print('Буллет Task 320 помечен (год уточнён в Task 321)')

# Буллет Task 321 — после буллета Task 320
entry = ('\n- **Табель учёта рабочего времени — год ровно 3 года, раздел переименован, ИТОГИ УЧЁТА дни/часы месяц/год (Task 321)**: '
         'по заявке пользователя: «Я имел ввиду список выбора года в общем количестве из трёх лет, один год до текущего года, один год после текущего. Переименуй раздел "График работы" в "Табель учёта рабочего времени". Добавь учёт дней и рабочего времени по итогам месяца и года, так что бы они не мешали работать с месячным графиком.». 1) ГОД — РОВНО ТРИ ПУНКТА (клиент, init #wsYearSel): цикл this._year−1 … this._year+1 (2025/2026/2027 для 2026; Task 320 делал ±3 — 7 пунктов, избыточно); месяцы не тронуты. 2) ПЕРЕИМЕНОВАНИЕ РАЗДЕЛА (только НАДПИСИ — id work-schedule и права RoleMatrix «График работы — просмотр/редактирование» НЕ менялись): заголовок #page-work-schedule «Табель учёта рабочего времени» (десктоп — хвост крошек PAGE_LABELS: «Главная / Документация / Документация ИОС / Табель учёта рабочего времени»); кнопка #workScheduleMenuBtn на page-docs-ios (menu-btn-label — новое имя, субметка «Шахматка сменного и дневного персонала» прежняя); PAGE_LABELS[\'work-schedule\']; SUBSECTIONS[\'work-schedule\'].label (ярлык закрепления на главной); пункт сайдбара #sidebarWorkScheduleBtn. 3) ИТОГИ УЧЁТА (сворачиваемая секция ПОД шахматкой — «не мешают работать с месячным графиком»): БАР-ПЕРЕКЛЮЧАТЕЛЬ #wsTotalsBar «Итоги учёта — дни и часы · сводка за месяц и год» + шеврон (клик/Enter/Пробел; role=button, tabindex, aria-expanded; класс ws-totals-open разворачивает шеврон) и ПАНЕЛЬ #wsTotalsPanel — СВЁРНУТА ПО УМОЛЧАНИЮ: сетка занимает всю высоту как прежде; раскрытие (toggleTotals) — панель max-height 46vh со своим скроллом (overflow-y: auto), строки шахматки пересчитывает _fitGrid (сетка ужалась), сворачивание возвращает высоту; мобайл — тап-зона бара 44px (padding 10px + min-height 44px); тёмная/светлая темы (панель как окно мероприятий Task 315: #e9e7de в светлой). ВКЛАДКИ: «Месяц» (#wsTtTabMonth, active по умолчанию) и «Год» (#wsTtTabYear) + инфо-строка #wsTtInfo + кнопка «Обновить» #wsTtRefresh (видна ТОЛЬКО на годе — месячные итоги живут данными сетки). МЕСЯЦ (_renderTotalsMonth): таблица .ws-tt-table по КАЖДОМУ сотруднику открытого месяца (порядок сетки _EMPLOYEES): Явки (дни работы), Часы (жирным зелёным), Д (дневные смены), Н (ночные), неявки ОТ/У/ОВ/Б/ПР, Прочие (И/ОБ/ПЗ/* «·» — мероприятия и плановые выходные), Всего + итоговая строка «Итого по подразделению» (усиленная граница); инфо: «сентябрь 2026 г. · норма: 22 раб. дн · 176,0 ч (40-час)» (ProdCalendar.monthStats, try/catch) + «включая несохранённые правки (N)»; ДАННЫЕ = ЭФФЕКТИВНЫЙ ВИД СЕТКИ (_totalsEffectiveEntries): записи _ENTRIES + локальные правки _PENDING (правка поверх записи меняет статус, __delete исключает день, правка пустого дня добавляется с источником «руч») — правка ячейки СРАЗУ видна в итогах (перерисовка из _renderGrid → _renderTotalsIfOpen); нет сотрудников — «Нет активных сотрудников». ЧАСЫ КОДОВ (_codeHours): Д/Н/д/н = 12, Д8 = 8, Д7,2 = 7,2 (карта); неявки (ОТ/У/ОВ/Б/ПР/И/ОБ/ПЗ/*/·/пусто) = 0; НЕЗНАКОМЫЙ код — часы пробуются из НАЗВАНИЯ «NN-час» справочника «Коды_статусов» (С/_statusMeta → name; «Смена (10-час)» → 10; «7,2-час» → 7,2), иначе 0 — консервативно; АГРЕГАЦИЯ (_totalsAgg → {byTab, grand}): явка = код с часами>0 (Н/н — ночная, прочие рабочие — дневная), семейства неявок по точному коду, мероприятия/«·»/незнакомые → other, total = все записи; записи без таб_номера игнорируются. ФОРМАТ чисел — запятая (_fmtTotalsNum: 43.2 → «43,2», 132 → «132»). ГОД (_renderTotalsYear/_renderTotalsYearTable): 12 колонок-месяцев (янв..дек) в формате «явки/часы» («11/132», пустые «—») + колонки «Дней»/«Часов» (годовые суммы сотрудника) + строка «Итого по подразделению» (суммы по месяцам); строки = сотрудники ГОДА ВКЛЮЧАЯ АРХИВНЫХ (listEmployees includeArchived=true — уволенный в середине года ОСТАЁТСЯ в годовых итогах с пометкой «архив» оранжевым; в месячной таблице и сетке архива НЕТ); ДАННЫЕ ГОДА (_loadYearData): 12 запросов workSchedule.listEntries {year, month} ПАРАЛЛЕЛЬНО (Promise.all; СБОЙ месяца → пустой список + счётчик failed — промис НЕ падает, панель работает с неполными данными, инфо «⚠ не загружено месяцев: N — нажмите Обновить») + 1 запрос справочника (includeArchived=true; сбой → активные из текущего вида + пометка «справочник не загружен — только активные»); КЭШ В ПАМЯТИ _YEAR_DATA {year, ts, months:{1..12}, employees, failed} — сбрасывается: loadGrid(force) («Обновить» бара/увольнение/сотрудник/генерация: if (force) this._YEAR_DATA = null — принудительное обновление делает годовой кэш устаревшим), кнопка «Обновить» панели (reloadTotals), год кэша ≠ открытому (перезагрузка); СИНХРОНИЗАЦИЯ: после НЕпринудительной загрузки месяца свежие записи подкладываются в кэш года БЕЗ новой сети (loadGrid .then: _YEAR_DATA.months[month] = _ENTRIES + перерисовка открытой годовой вкладки); ЛОАДЕР «Загрузка данных за год…» (анимированные точки), ОШИБКА — понятное сообщение + «Нажмите Обновить»; НОВЫХ СЕРВЕРНЫХ ЭНДПОИНТОВ НЕТ (используются существующие listEntries/listEmployees — backward compatible). Таблицы: sticky-шапки, .ws-tt-scroll (горизонтальный скролл на узких экранах), tabular-nums, эллипсис ФИО, таб. № мелким. ⚠️ ДЕПЛОЙ: ТОЛЬКО GitHub Pages + Ctrl+Shift+R ×1–2 (SW kipia-test-v559→v560); Apps Script/листы Google Таблиц НЕ трогать — см. scripts/DEPLOY-Task321-year3-rename-totals.md. Верификация: сьют 1742→1783/0 (+42 tests/test-task321.js [год: JS-цикл −1/+1 + VM 3 опции; переименование 5 мест + id/права не тронуты; HTML/CSS панели: бар/панель/вкладки/обе темы/тап-зона; VM: _codeHours/_totalsZero/_totalsAgg/_totalsEffectiveEntries/toggleTotals/setTotalsTab/reloadTotals/_renderTotalsIfOpen/_renderTotalsMonth (значения/формат/норма/правки/пусто)/_fmtTotalsNum/_loadYearData (12+1, failed, кэш)/_renderTotalsYear (лоадер/кэш другого года)/_renderTotalsYearTable (12 колонок/архив/суммы/failed); интеграции: _renderGrid → _renderTotalsIfOpen, loadGrid(force) → сброс + синхронизация; SW v560]; актуализированы test-task320 [год ±1 + SW-блок], test-work-schedule [метки Task 249/267], test-flowmeter-validation [сайдбар Task 241]; SW-бамп в 21 файле), task321-browser-check.py 45/45 (порт 8941, 3 контекста: десктоп-тёмная [год 3 пункта; переименование; бар/панель; месяц: 43,2/Д3/Н1/ОТ2/Б1/прочие1/всего8, «Итого» 6/59,2; живые правки 31,2; год: 12 запросов/12 колонок/1-12/архив Сидоров/5 дней 55,2; «Обновить» +12; сбой месяца — предупреждение; сворачивание — высота восстановлена; 0 ошибок], светлая 1024 [#e9e7de], мобайл 375 [44px тап, скролл-таблица]); регресс браузером 306–320 все зелёные (320 актуализирован под 3 года); VLM ×4 — без дефектов. SW kipia-test-v559→v560.')
lines2 = src.split('\n')
insert_at = None
for i, l in enumerate(lines2):
    if l.startswith('- **График работы — год ±3 в списке выбора'):
        j = i + 1
        while j < len(lines2) and lines2[j].startswith('  '):
            j += 1
        insert_at = j
        break
assert insert_at is not None, 'точка вставки (после буллета Task 320) не найдена'
lines2.insert(insert_at, entry)
src = '\n'.join(lines2)
print('Буллет Task 321 вставлен (после Task 320)')

with open('Системный_промт_для_приложения_КИПиА.md', 'w', encoding='utf-8') as f:
    f.write(src)
print('Готово: промт post-Task 321')
