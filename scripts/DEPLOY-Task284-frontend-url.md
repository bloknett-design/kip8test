# DEPLOY Task 284 — приложение ходило в ДРУГОЕ развёртывание
# («Unknown action: workSchedule.addVacation» при правильных файлах)

## Симптом (заявка 2026-09-01)

> «всё равно не работает Ошибка: Unknown action:
> workSchedule.addVacation, к примеру добавление инструктажа
> работает» → «Я развёртываю с этой ссылкой
> https://script.google.com/macros/s/AKfycbyt…/exec»

## Причина — ДВА разных развёртывания

| | URL | Состояние |
|---|---|---|
| Куда ходило приложение (с Task 202) | `AKfycbzgPtyya…/exec` | жив, но код — **старый снимок до Task 274** (инструктажи есть, отпусков нет) |
| Куда разворачивает пользователь | `AKfycbyt2sjb…/exec` | **полный проект, НОВЫЙ код** — роутинг отпусков есть (доказано пробами) |

Пользователь обновлял файлы и создавал версии в СВОЁМ
развёртывании (`AKfycbyt…`), а приложение продолжало вызывать
старое (`AKfycbzg…`, Task 202) — отсюда «Unknown action» при
идеально правильных файлах.

**Пробы к `AKfycbyt…` (scripts/task284-probe-user-deploy.sh):**
`listVacations` → `no_session` (маршрут есть!), `addTraining` →
`no_session`, `flowmeter.getValidationRules` → `no_session`,
`cableJournal.getColumns` → `Unauthorized`, `getCurrentUser` →
`no_session`, `adminListUsers` → `Unauthorized`, `sendOTP` →
«Некорректный email» — все модули на месте, доступ «Anyone»
работает.

## Фикс (уже в репозитории)

Фронтенд переключён на развёртывание пользователя:

- `index.html` — `KipAuth.WEB_APP_URL` = `AKfycbyt…/exec`
  (прежний `AKfycbzg…` удалён; конкатенация `?action=` не тронута);
- `sw.js` — кэш `kipia-test-v530` → `kipia-test-v531`;
- `scripts/Code.gs` — справочная константа `WEB_APP_URL`
  синхронизирована (информационная, на работу сервера не влияет);
- `scripts/VacationsDiagnose.gs` — подсказка с верным URL;
- +9 тестов `tests/test-deploy-url.js` (1230 passed / 0 failed),
  browser-check Task 282 — 21/21 (тост-цепочка цела).

Серверная часть НЕ менялась — ваши файлы в Apps Script уже
правильные, ничего не трогать.

## Шаги деплоя (1 шаг!)

1. **Ctrl+Shift+R** в приложении (2 раза, чтобы service worker
   обновился на v531). Всё — приложение начнёт ходить в ваше
   развёртывание с новым кодом.

## Проверка

1. «График работы → Отпуска» — список сотрудников с периодами.
2. «+ Добавить отпуск» → «Добавить» — тост «Отпуск добавлен…»,
   строка в списке.
3. «Сформировать» — «О» на днях отпуска.
4. Быстрая техническая: прислать в чат «готово» — ассистент
   пробами проверит, что фронтенд и деплой синхронны.

## На будущее — обновление сервера

Теперь приложение вызывает `AKfycbyt…/exec`. При изменении
`.gs`-файлов в Apps Script: **Управление развертываниями →
✏️ (у развёртывания с ЭТИМ URL) → Версия: Новая версия →
Развернуть**. Если создать «Новое развёртывание» — URL сменится,
и приложение снова пойдёт мимо (ситуация Task 284).

## Откат

Вернуть `WEB_APP_URL` = `AKfycbzg…` и поднять SW-кэш (коммит
Task 283, 7d27698). Старое развёртывание по-прежнему живо.
