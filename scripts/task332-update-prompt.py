# -*- coding: utf-8 -*-
# Обновление системного промта kip8test до post-Task 332
# (подсказка значка подсветки + кнопка ВИДА ТАБЕЛЯ с ролями,
#  шторка «Год» без «Обновить» и столбца сотрудников,
#  разделительная полоса столбца ФИО шахматки)
with open('Системный_промт_для_приложения_КИПиА.md', encoding='utf-8') as f:
    lines = f.readlines()

new3 = '''> **Версия документа:** 2026-09-06 (post-Task 332: ПОДСКАЗКА ЗНАЧКА ПОДСВЕТКИ + КНОПКА ВИДА ТАБЕЛЯ (полный/сменный/дневной, роль КИП ИОС дежурный — только сменный) + ШТОРКА «ГОД» БЕЗ «ОБНОВИТЬ» И СТОЛБЦА СОТРУДНИКОВ + РАЗДЕЛИТЕЛЬНАЯ ПОЛОСА СТОЛБЦА ФИО [заявка: «Сделай подсказку кнопки включения подсветки строк и столбцов (как в других кнопках в баре), слева от неё добавь новую кнопку переключения вида 1-полный (доступны все элементы), 2-сменный (не доступны шахматка дневных сотрудников и шторка), 3-дневной (не доступны шахматка сменных сотрудников и шторка), (так же с подсказкой), для роли КИП ИОС дежурный, доступен только вид 2-сменный. В шторке на год убери кнопку "Обновить" и столбец сотрудников. В шахматке табеля сделай разделительную полосу между столбцом сотрудников и ячейками шахматки такой же как левая полоса в шторке (полоса 2px #4a8fc7/светлая #6e8ba4 — точно как разделитель сменных/дневных в шахматке).»]: 1) ПОДСКАЗКА #wsCrossTip — информационное окно формата «Обновить»/«Сформировать» (Task 317/328: заголовок + описание, показ при НАВЕДЕНИИ/фокусе, скрытие — уходом/кликом/прокруткой, перенос в <body> приёмом Task 319, pointer-events none) + ЖИВОЕ состояние «включена/выключена» (#wsCrossTipState, обновление перед показом и сразу после toggleCross). 2) КНОПКА ВИДА #wsViewBtn (класс ws-cross-btn + ws-view-btn, САМАЯ ЛЕВАЯ ряда 3 — ЛЕВЕЕ значка подсветки): 3 вида по кругу (cycleView: full→shift→day→full), состояние — localStorage kip8_ws_view_v1 (по умолчанию полный); ИКОНКА — 4 полосы двумя группами (верх — сменные, низ — дневные, между группами зазор): у «чужой» группы opacity 0.25 — 3 svg #wsViewIconFull/Shift/Day переключает _updateViewBtn; aria-label с текущим видом; ПОДСКАЗКА #wsViewTip (тот же формат; заголовок «Вид табеля: <вид>», описание трёх видов; для запертой роли — «доступен только сменный»); тосты при переключении. 3) ПРИМЕНЕНИЕ ВИДА (_applyView): полный — «Итоги учёта»/вкладки/шторка доступны; сменный — строки сетки = только «тип»=сменный (_viewEmployees — фильтр ОТРИСОВКИ, _EMPLOYEES не трогаем), кнопка «Итоги учёта» СКРЫТА, открытая шторка ЗАКРЫВАЕТСЯ; дневной — только дневные; гейты: toggleTotals/setTotalsTab не открывают шторку из неполного вида (толерантно к отсутствию _view); перерисовка сетки при смене вида; пустой вид — «Сотрудники вида «…» отсутствуют». 4) РОЛЬ «КИП ИОС дежурный» — вид ЗАПЕРТ на «сменный» (_viewLocked: _initView при init игнорирует сохранённый, клики — тост-пояснение, кнопка приглушена ws-view-locked opacity 0.6); хук WorkSchedule._onRoleUpdate из KipAuth._applyRoleToUI (роль может прийти ПОЗЖЕ init: медленный путь bootstrap/фоновая проверка — обновляет _canEdit/«Сформировать» и замок; идемпотентно, до init — тихо). 5) ШТОРКА «ГОД»: кнопка «Обновить» шапки УДАЛЕНА (HTML/CSS/логика _updateTtHead/setTotalsTab; метод reloadTotals остался API) — годовые данные обновляет кнопка «Обновить» ТУЛБАРА (loadGrid(true) сбрасывает _YEAR_DATA, Task 321; открытая вкладка года перезагружается автоматически; ⚠-тексты переадресованы); СТОЛБЕЦ «Сотрудник» и АРХИВНЫЕ строки таблицы года УДАЛЕНЫ — строки года = строки сетки (только активные, как вкладка «Месяц»; имена — в стик-колонке ФИО шахматки), шапка начинается с «янв», CSS .ws-tt-arch удалён. 6) РАЗДЕЛИТЕЛЬНАЯ ПОЛОСА шахматки: border-right 2px solid #4a8fc7 (светлая #6e8ba4) на th/td.ws-emp-col (= левая полоса шторки .ws-tt-edge = разделитель сменных/дневных ws-group-first); полоса на sticky-колонке — при горизонтальной прокрутке остаётся на границе; специфичность (0,2,3)/(0,3,3) выше базовых td (0,1,2)/светлой (0,2,2). Тесты 2034/0 (актуализированы 317/321/323/324/325/329/331: окна слушателей, «Обновить»-удаление, год без emp/архива, порядок ряда 3, VM _updateTtHead; SW v570→v571 в 32 файлах, «лишний инкремент»→v572 в 9+13); browser task332-browser-check.py 44/44 [4 контекста: десктоп 1280 тёмная (кнопка вида левее подсветки 34×ряда, иконки 3 состояний, обе подсказки с живым состоянием, сменный 11 строк/дневной 1/полный 12, гейт шторки, год: 12 месяцев без «Сотрудника», 12 строк без архива, обновление года кнопкой тулбара, полоса 2px rgb(74,143,199) до/после прокрутки) + дежурный (вид заперт, day из localStorage проигнорирован, клик — тост, подсказка «только сменный») + светлая (#6e8ba4 у шапки/тела/разделителя групп) + мобайл 375 (34px, сменный 11 строк, «Итоги учёта» скрыта); 0 JS-ошибок]; VLM ×3 (год/сменный+светлая/подсказки — замечания объяснены дизайном/моком). ДЕПЛОЙ: ТОЛЬКО Pages + Ctrl+Shift+R ×1–2 — Apps Script/листы НЕ трогать. ПЕРЕНОС в kip8: НЕ выполнен — ждёт проверки пользователем [потом одной партией 315+…+332, k8 v418→v419])
'''

assert lines[2].startswith('> **Версия документа:**')
lines.insert(2, new3 if new3.endswith('\n') else new3 + '\n')
print('Строка 3 вставлена (цепочка версий)')

src = ''.join(lines)

reps = [
    ('> **Текущая версия кэша:** `kipia-test-v570`',
     '> **Текущая версия кэша:** `kipia-test-v572`'),
    ('| `kip8test` | PWA | `kipia-test-v570` |',
     '| `kip8test` | PWA | `kipia-test-v572` |'),
]
for old, new in reps:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Версии кэша обновлены (2 замены)')

# Буллет Task 332 — сразу ПОСЛЕ буллета Task 331
entry = ('''\n- **Task 332 — ПОДСКАЗКА ЗНАЧКА ПОДСВЕТКИ + КНОПКА ВИДА ТАБЕЛЯ (ПОЛНЫЙ/СМЕННЫЙ/ДНЕВНОЙ, РОЛЬ КИП ИОС ДЕЖУРНЫЙ — ТОЛЬКО СМЕННЫЙ) + ШТОРКА «ГОД» БЕЗ «ОБНОВИТЬ» И СТОЛБЦА СОТРУДНИКОВ + РАЗДЕЛИТЕЛЬНАЯ ПОЛОСА СТОЛБЦА ФИО**: заявка: «Сделай подсказку кнопки включения подсветки строк и столбцов (как в других кнопках в баре), слева от неё добавь новую кнопку переключения вида 1-полный (доступны все элементы), 2-сменный (не доступны шахматка дневных сотрудников и шторка), 3-дневной (не доступны шахматка сменных сотрудников и шторка), (так же с подсказкой), для роли КИП ИОС дежурный, доступен только вид 2-сменный. В шторке на год убери кнопку "Обновить" и столбец сотрудников. В шахматке табеля сделай разделительную полосу между столбцом сотрудников и ячейками шахматки такой же как левая полоса в шторке (полоса 2px #4a8fc7/светлая #6e8ba4 — точно как разделитель сменных/дневных в шахматке).» 1) ПОДСКАЗКА #wsCrossTip (заявка: «как в других кнопках в баре»): информационное окно формата «Обновить»/#wsRefreshTip (Task 317) — заголовок «Перекрёстная подсветка строк и столбцов» + описание + ЖИВОЕ состояние #wsCrossTipState («Сейчас подсветка включена/выключена» — обновляется в _showCrossTip перед показом и в toggleCross сразу после клика); слушатели mouseenter/mouseleave/focus/blur/click(скрытие через 120 мс) в init, перенос в <body> (Task 319), глобальный scroll-сброс (список _hideCrossTip/_hideViewTip добавлен к «Обновить»/«Сформировать»), pointer-events none, позиция НАД кнопкой (не хватает места с учётом верхнего бара — ПОД, приём Task 319); нативного title НЕТ (тест 328 C-инвариант сохранён). 2) КНОПКА ВИДА #wsViewBtn (заявка: «слева от неё»): САМАЯ ЛЕВАЯ ряда 3 #wsActionsRow (порядок: вид → подсветка → «Сформировать» → «Сохранить» → «Отменить»); класс ws-cross-btn (размер/вид = значок подсветки: десктоп 34×высота ряда, мобайл 34×100%) + модификатор ws-view-btn; ИКОНКИ 3 состояний #wsViewIconFull/Shift/Day (svg 24×24, 4 горизонтальные полосы ДВУМЯ группами с зазором: верхняя пара — сменные, нижняя — дневные; у «чужой» группы opacity 0.25 — сразу видно текущий вид), _updateViewBtn переключает hidden + aria-label «Переключение вида табеля: сейчас <вид>» + класс ws-view-locked; ПЕРЕКЛЮЧЕНИЕ cycleView по кругу полный→сменный→дневной→полный (тосты «Полный вид — все сотрудники, итоги учёта доступны»/«Сменный вид — только сменные сотрудники»/«Дневной вид — только дневные сотрудники»; сохранение kip8_ws_view_v1; иконка-кнопка без текста — тост обязателен); ПОДСКАЗКА #wsViewTip — тот же формат: заголовок «Вид табеля: <полный|сменный|дневной>» (обновляется перед показом), описание «Переключение вида табеля: полный — все сотрудники и шторка «Итоги учёта»; сменный — только сменные сотрудники; дневной — только дневные. Клик по кнопке переключает вид по кругу.», для запертой роли — «Роли «КИП ИОС дежурный» доступен только сменный вид: шахматка сменных сотрудников, шторка итогов и шахматка дневных недоступны.». 3) ПРИМЕНЕНИЕ ВИДА: состояние _view ('full'/'shift'/'day', по умолчанию full) + _viewLocked; _applyView(opts): полный — #wsTotalsBtn видна; сменный/дневной — #wsTotalsBtn СКРЫТА (ряд 2 существует — сетка не прыгает), открытая шторка ЗАКРЫВАЕТСЯ (toggleTotals: закрытие гейтом НЕ блокируется), вкладки «Месяц»/«Год» спрятаны (_updateTtTabsVisible); ФИЛЬТР СТРОК — _viewEmployees (в _renderGrid): сменный — только «тип»=«сменный», дневной — только «тип»=«дневной» (список уже отсортирован _sortEmployees; _EMPLOYEES/итоги/карточки/справочник — ПОЛНЫЙ список, фильтр только отрисовки; шторка в этих видах закрыта — рассинхрона строк нет); пустой вид — «Сотрудники вида «сменные|дневные» отсутствуют.»; ГЕЙТЫ: toggleTotals `if ((this._view||\'full\') !== \'full\' && !this._totalsOpen) return;` (открытие из неполного вида запрещено, закрытие всегда можно — смена вида из полного с открытой шторкой), setTotalsTab аналогично; толерантность к отсутствию _view (VM-моки/легаси = полный); _initView(role) в init ПОСЛЕ восстановления подсветки: роль «КИП ИОС дежурный» → _view=\'shift\' насильно (сохранённый kip8_ws_view_v1 игнорируется), прочие роли — сохранённый. 4) РОЛЬ «КИП ИОС дежурный» (заявка: «доступен только вид 2-сменный»): _viewLocked=true; cycleView — тост «Роли «КИП ИОС дежурный» доступен только сменный вид табеля», состояние НЕ меняется; кнопка приглушена (.ws-view-btn.ws-view-locked opacity 0.6, cursor default); ХУК РОЛИ: KipAuth._applyRoleToUI (конец функции) → try { WorkSchedule._onRoleUpdate(this._cachedRole) } — роль может прийти ПОЗЖЕ init модуля (медленный путь bootstrap без кэша/фоновая проверка сессии): _onRoleUpdate обновляет _canEdit/«Сформировать» и замок (смена замка меняет вид + перерисовка; вызовы идемпотентны; до init — тихо, модуль сам прочитает роль). 5) ШТОРКА «ГОД» (заявка: «убери кнопку "Обновить" и столбец сотрудников»): кнопка #wsTtRefresh УДАЛЕНА из HTML шапки + CSS .ws-tt-refresh (3 правила) + логика (setTotalsTab/_updateTtHead больше не читают; метод reloadTotals остался как API); ГОДОВЫЕ ДАННЫЕ обновляет кнопка «Обновить» ТУЛБАРА: loadGrid(true) сбрасывает _YEAR_DATA (Task 321) → _renderGrid → _renderTotalsIfOpen → вкладка года перезагружается автоматически (проверено браузером: ts свежий, 12 строк); ⚠-текст «не загружено месяцев: N — обновите данные (кнопка «Обновить»)», ошибка загрузки — «Нажмите «Обновить» в панели кнопок, чтобы повторить»; СТОЛБЕЦ «Сотрудник» таблицы года УДАЛЕН (шапка начинается с «янв»; строка года = 12 месяцев + Дней + Часов + Перераб. (дни)); АРХИВНЫЕ строки года УДАЛЕНЫ (employees = this._EMPLOYEES — активные ровно по строкам сетки, как вкладка «Месяц»; имена — в стик-колонке ФИО шахматки; архив без имён бессмыслен; годовой справочник md.employees остаётся для ТИПОВ переработки д/н); CSS: бейдж .ws-tt-arch удалён, комментарий .ws-tt-emp — «только мобайл и месячная таблица» (селекторы :not(.ws-tt-year) — мобайл-месяц); строки года теперь ровно по строкам сетки — _syncTotalsRows без хвоста. 6) РАЗДЕЛИТЕЛЬНАЯ ПОЛОСА шахматки (заявка: «между столбцом сотрудников и ячейками — как левая полоса в шторке»): .ws-grid thead th.ws-emp-col, .ws-grid tbody td.ws-emp-col { border-right: 2px solid #4a8fc7; } [data-theme="light"] → #6e8ba4 [= .ws-tt-edge шторки Task 331 = разделитель tr.ws-group-first td]; полоса на СТИК-колонке ФИО — проходит по ВСЕЙ высоте (шапка «Сотрудник +» и тело), при горизонтальной прокрутке ОСТАЁТСЯ на границе (граница принадлежит sticky-ячейкам, ячейки дней уезжают ПОД полосу); специфичность (0,2,3)/(0,3,3) выше базовых .ws-grid tbody td (0,1,2) и светлой [data-theme="light"] .ws-grid tbody td (0,2,2) — цвет не перекрашивается; border-collapse: стык 2px побеждает 1px границу первого дня — линия непрерывна. Верификация: сьют 2034/0 [актуализированы: test-task317 (окно поиска слушателей 2400→5000 — добавлены проводки #wsCrossTip/#wsViewTip), test-task321 (HTML шапки без «Обновить»; CSS .ws-tt-refresh отсутствует; setTotalsTab-VM без кнопки + вкладки; год: 2 строки (шапка+актив), без ws-tt-emp/имён/архива), test-task323 (год: только активные, без ws-tt-emp/бейджа архива), test-task324 (HTML шапки; VM _updateTtHead без wsTtRefresh), test-task325 (ряд 3: окно 1900→4600 + порядок вид→подсветка→Сформировать; ✕-тест без «Обновить»), test-task329 (шапка без «Обновить»), test-task331 (VM _updateTtHead без wsTtRefresh — пустая шапка только по ⚠); SW-бамп v570→v571 в 32 файлах + «лишний инкремент»/«не существует» assertFalse v571→v572 в 9]; браузер task332-browser-check.py 44/44 (порт 8944, 4 контекста: ДЕСКТОП 1280 тёмная [кнопка вида в ряду 3 САМАЯ ЛЕВАЯ, размер = значку подсветки 34×29,7; иконки Full/Shift/Day переключаются; #wsViewTip: показ при наведении, в <body>, pointer-events none, НАД кнопкой, «Вид табеля: полный» + описание 3 видов; #wsCrossTip: заголовок + живое «включена»→«выключена»; сменный: 11 строк (дневных 0), «Итоги учёта»/вкладки скрыты, aria «сменный», kip8_ws_view_v1=shift, шторка программно НЕ открывается (гейт), тост; дневной: 1 строка (Петров), сменных 0, saved=day; полный: 12 строк, «Итоги учёта» доступна; ГОД: #wsTtRefresh нет в DOM, шапка шторки спрятана, шапка начинается с «янв», БЕЗ «Сотрудника»/ws-tt-emp, строк 12 = активные, «Архивный» НЕТ, Дней/Часов/Перераб. (дни) в конце; «Обновить» тулбара перезагрузил год (_YEAR_DATA.ts свежий, 12 строк); ПОЛОСА: th/td 2px solid rgb(74,143,199) = разделителю групп, после scrollLeft=400 на месте (sticky); 0 ошибок]; ДЕЖУРНЫЙ [вид заперт shift (подсовутый day проигнорирован), 11 строк, ws-view-locked, aria «(доступен только сменный)», клик — вид НЕ меняется + тост, подсказка «сменный» + «доступен только сменный»; 0 ошибок]; СВЕТЛАЯ [#6e8ba4 у th/td/разделителя групп, 2px, подсказка показана #333; 0 ошибок]; МОБАЙЛ 375 [кнопка 34px, сменный 11 строк, «Итоги учёта» скрыта; 0 ошибок]); VLM ×3 (десктоп-год: иконки/год без «Сотрудника»/полоса ✓ — «наложение шторки»/«обрезка» = дизайн Task 323/331 (шире капа 60% — ползунок), «Дней=13 при 3 ячейках» = мок (годовые записи всех месяцев); сменный+светлая: полоса/отсутствие Петрова/зебра ✓; подсказки: оба окна одинаковы, читаются, НАД кнопкой, без дефектов); пруфы task332-proof-{desktop,shift,year,viewtip,crosstip,duty,light,mobile}.png]. ⚠️ ДЕПЛОЙ: ТОЛЬКО GitHub Pages + Ctrl+Shift+R ×1–2 (SW kipia-test-v570→v571) — Apps Script/листы НЕ трогать (только клиент) — см. scripts/DEPLOY-Task332-view-button-tips-year-drawer-separator.md.''')
lines2 = src.split('\n')
insert_at = None
for i, l in enumerate(lines2):
    if l.startswith('- **Task 331 —'):
        insert_at = i + 1
        break
assert insert_at is not None, 'буллет Task 331 не найден'
lines2.insert(insert_at, entry)
src = '\n'.join(lines2)
print('Буллет Task 332 вставлен (после Task 331)')

with open('Системный_промт_для_приложения_КИПиА.md', 'w', encoding='utf-8') as f:
    f.write(src)
print('Готово: Системный_промт_для_приложения_КИПиА.md → post-Task 332')
