# DEPLOY Task 370 — недоставленные показания: цвет значений ярче и ближе к жёлтому

**Дата:** 2026-09-14
**Заявка пользователя:** «внеси изменения сразу и в тестовый и в боевой проекты,
жёлто-оранжевые значения недоставленных вместо баннера, измени цвет чуть ярче
и ближе к желтому.»

> Изменение внесено **синхронно** в kip8test (этот репо, SW `kipia-test-v599`)
> и в боевой kip8 (SW `kipia-v447`) — по прямой заявке, без стадии приёмки
> на тестовом.

## 1. Что изменилось (клиент, PWA)

Цвет значений карточек расходомеров с недоставленными показаниями
(`.flow-summary-val.flow-summary-val-pending`, введён Task 367 вместо баннера)
стал **ярче и ближе к жёлтому**:

| Тема | Было (Task 367) | Стало (Task 370) |
|---|---|---|
| Тёмная | `#f5a623` (приглушённый оранжево-янтарный, оттенок ≈37°) | **`#ffc400`** (яркий золотистый, оттенок ≈46° — у жёлтого) |
| Светлая | `#c96e00` (тёмно-оранжевый, ≈33°) | **`#cc9900`** (золотистый, ≈45°) |

- Только ЦВЕТ: размер/вес шрифта как у обычного зелёного значения
  (красный «пора вводить» остался крупнее: 18px/800, Task 359).
- Контраст светлой темы `#cc9900` на белом ≈ 2.6:1 — на уровне обычного
  зелёного `#5ab870` (≈ 2.5:1), читаемость не пострадала.
- Логика НЕ менялась: приоритет pending НАД красным «пора вводить»,
  зелёный после доставки (перерендеры после `_outboxAdd` и в `finish()`
  флаша), красный недельных/месячных по ЗАКРЫТЫМ календарным периодам
  (Task 368) — всё как было.
- Соседние цвета не тронуты: зелёный `#5ab870`, красные `#ff5c47`/`#e8230a`.

**SW:** `kipia-test-v598` → **`kipia-test-v599`**.

## 2. Проверки

- Тесты: `tests/test-task370.js` +9 (SRC: новые цвета в обеих темах, старые
  хексы не остались, оттенок сместился к жёлтому (hue 46° > 37°), маркер
  Task 370 в CSS-комментарии, соседние цвета целы, приоритет pending над due
  не менялся, размер/вес не тронуты; SW v599/guard v600). Адаптирован
  `tests/test-task367.js` (ассерты цвета → `#ffc400`/`#cc9900`).
  Итог: **2980 passed / 0 failed** (было 2971).
- Браузер `scripts/task370-browser-check.py` — **26/26** (порт 8970):
  - новый жёлто-оранжевый `rgb(255,196,0)` тёмная / `rgb(204,153,0)` светлая
    (getComputedStyle, точное совпадение);
  - полный регресс Task 367/368: баннера нет; зелёный/красный; недельные/
    месячные по ЗАКРЫТЫМ периодам (№3/№9 — данные накрывают закрытую
    неделю/месяц → зелёный; №11/№5 — позапрошлые периоды → красный;
    сокращения «Еженед.»/«Ежемес.»); приоритет pending над красным;
    реальный offline-ввод → цвет сам (hook); флаш → снова зелёный;
    светлая тема; мобайл 375; 0 JS-ошибок.
  - Моки дат детерминированы (не зависят от дня запуска): границы закрытых
    недель/месяцев вычисляются от понедельника текущей недели.
- VLM-пруфы: `scripts/task370-proof-desktop.png` (№2/№4 — «ярко-золотистый,
  ближе к жёлтому»; №12/№3/№9 зелёные; №11 красный; баннера нет),
  `task370-proof-light.png` (№2 золотистый на белом, красные/зелёные,
  баннера нет), `task370-proof-mobile.png` (375, палитра та же).

## 3. Серверная часть

**Не требуется.** Изменение только в `index.html` (CSS) + `sw.js` (версия
кэша). Apps Script не менялся.

## 4. Откат

```bash
git revert <commit-sha>
```

Вернёт `#f5a623`/`#c96e00` (Task 367) и SW `kipia-test-v598`.
