# -*- coding: utf-8 -*-
# Обновление системного промта kip8test до post-Task 319
# (тултип не под баром; перекрестье строка+столбец; тёмная шахматка =
#  светлые цвета + brightness; название кода мельче/перенос; окно
#  «Мероприятия в этот день» зрителям)
with open('Системный_промт_для_приложения_КИПиА.md', encoding='utf-8') as f:
    lines = f.readlines()

new3 = '''> **Версия документа:** 2026-09-05 (post-Task 319: ТУЛТИП «ОБНОВИТЬ» НЕ ПРЯЧЕТСЯ ПОД ВЕРХНИМ БАРОМ — окно #wsRefreshTip переносится из .page-content в <body> (position:relative;z-index:2 страницы опускал z 9450 ПОД sticky .desktop-top-bar z 70 — окно «пряталось» за баром) + позиция «над кнопкой» считается С УЧЁТОМ низа бара (barBottom; не хватает места ниже бара — окно ПОД кнопкой; мобайл без бара — прежнее поведение; мок-DOM без querySelector — тихо пропускает); ПЕРЕКРЕСТЬЕ ШАХМАТКИ — движение мыши по ячейкам дней выделяет СТРОКУ и СТОЛБЕЦ, пересекающиеся в ячейке под курсором: делегирование mouseover/mouseout на #wsGridWrap (2 слушателя вместо сотен inline; строка — sectionRowIndex, столбец — data-day) → _cellHover(ri, day) → _rowHover (класс ws-hover-row на <tr>, CSS красит все td строки вкл. ФИО; _rowClass по tbody tr) + _dayHover (столбец Task 316); пересечение — насыщеннее (правило с .ws-hover 0.24/0.16), сегодня/выбранный день/рамка ручных д/н не затираются (составные правила, принцип Task 313/316); переходы МЕЖДУ ячейками без мигания (mouseout при relatedTarget-ячейке игнорируется, каждый подсвет no-op по «не менялся»); снятие — уход с ячеек (шапка/ФИО/вне сетки) и смена месяца (onMonthChange обнуляет _hoverRow); штамп ws-hover-row в _renderGrid (trClsParts рядом с ws-group-first) — переживает перерисовку; всем ролям; ТЁМНАЯ ТЕМА — ЦВЕТА ШАХМАТКИ ДНЕЙ КАК В СВЕТЛОЙ: [data-theme="dark] td.ws-cell background #eef0f2 / color #141413 / filter brightness(0.88) («немного притеняется поверхностным фильтром»), пустые выходные #f7d9e3, «·»/пустые rgba(20,20,19,0.65), пунктирный бейдж тёмный, свотч «.» #eef0f2; статусные inline-цвета справочника и так одинаковы; шапка/ФИО-колонка остаются ТЁМНЫМИ (заявка — только «шахматка дней»); правило ПОСЛЕ .ws-dot-code (равная специфичность — позднее выигрывает), базовые правила тёмной темы НЕ тронуты; ОКНО ВЫБОРА КОДОВ — НАЗВАНИЕ КОДА 12px (было 13) + ПЕРЕНОС по строкам (white-space normal + overflow-wrap break-word, ТОЛЬКО #wsCellPopup — окно «Мероприятия в этот день»/карточка прежний однострочный вид); ОКНО «МЕРОПРИЯТИЯ В ЭТОТ ДЕНЬ» — ЗРИТЕЛЯМ («График работы — просмотр»): onCellClick при !_canEdit → НОВЫЙ _openEventsOnlyPopup(td, isoDate, tabNo) — ТОЛЬКО окно #wsEventsPopup (содержимое _renderEventsPopup, кнопки ✎/✕ и так _canEdit-gated; позиция у ячейки по правилам _openCellPopup; кловер active, закрытие прежнее closeCellPopup); окно выбора кодов зрителю НЕ показывается, тост «У вас нет прав на правку графика» УДАЛЁН (заявка: окно должно быть доступно). Сервер/листы/Apps Script НЕ менялись. Тесты: 1686 → 1708/0 (+22 test-task319.js: CSS [перекрестье тёмная/светлая, составные с manual/today/sel, ФИО-ячейка; тёмная шахматка #eef0f2/#141413/brightness 0.88, weekend #f7d9e3, ev-pending, swatch-dot, порядок после .ws-dot-code; #wsCellPopup .ws-popup-name 12px/normal/break-word при живой базе nowrap], JS [_hoverRow состояние, onMonthChange-сброс, штамп trClsParts, _cellHover/_rowHover/_rowClass, init-делегирование + перенос тултипа в body, _showRefreshTip .desktop-top-bar + top < barBottom + 4, onCellClick зритель → _openEventsOnlyPopup без тоста, метод без wsCellPopup], VM [перекрестье: строка+столбец/снятие/переходы без мигания/смена строки/сброс; _rowClass мягкий; _openEventsOnlyPopup: рендер/кловер/позиция/без ✎-✕; _showRefreshTip: с баром → 112px под кнопкой, без бара → 12px над], SW v558; актуализированы test-work-schedule [trClsParts — 2 regex]; SW-бамп v557→v558 в sw.js + 19 тест-файлах, assertFalse «лишний инкремент» → v559 в test-task315/317/318). Верификация: task319-browser-check.py 48/48 (порт 8937, 4 контекста: десктоп-Адамин 1280 ТЁМНАЯ [бар sticky 56 z70; тултип: показан, parent=body, z 9450, ПОД кнопкой НИЖЕ бара, «данные от», уход — скрыт; перекрестье: строка 0 ws-hover-row + столбец 5 (th+все td), столбец 7 чист, ФИО-tint, пересечение 0.24 vs строка 0.10, переход в строке — строка ОСТАЛАСЬ/столбец сменился, смена строки, уход на шапку — снято + Task 316 жив, уход из сетки — снято, перерисовка (правка статуса «Н») — штамп жив; тёмная шахматка: пустая #eef0f2+brightness(0.88), текст #141413, статусная #FFF9C4+brightness, выходная #f7d9e3, шапка #0e1621 ТЁМНАЯ, ФИО тёмная, «·» rgba(20,20,19,0.65), бейдж тёмный; окно кодов: названия 12px normal break-word, длинное 3 строки (Range-rects), окно ≤320, «Мероприятия» 13px nowrap; 0 JS-ошибок], светлая [те же цвета БЕЗ фильтра, шапка светлая, перекрестье тон 0.06], зритель «ИТР8 pro» [окно «Мероприятия в этот день» ОТКРЫТО с «Целевой инструктаж», БЕЗ ✎/✕, окно кодов НЕ открыто, кловер, пустой день «нет мероприятий», перекрестье работает], мобильный 375 touch [сетка, бара нет, шахматка светлая в тёмной]); регресс 298 34/34, 303 20/20, 304 10/10, 305 20/20, 306 29/29, 307 20/20, 308 20/20, 309 26/26, 310 20/20, 311 24/24, 312 31/31, 313 30/30 (актуализирован: зритель теперь ПОЛУЧАЕТ окно мероприятий — заявка Task 319), 314 36/36, 315 33/33, 316 28/28, 317 49/49, 318 45/45; VLM ×2 (тултип изолированный — ЦЕЛИКОМ все 4 строки/рамка/тень; перекрестье — строка+столбец, пересечение насыщеннее; тёмная — шахматка светлая слегка притенена, шапка/ФИО тёмные, текст читается; длинное название — 3 строки целиком; зритель — окно без ✎/✕; замечание VLM «тултип обрезан» на общем скрине ОПРОВЕРГНУТО изолированным скрином + численно: scrollH 96 = clientH 96, parent body, z 9450, pointer-events none — hit-test проходит сквозь). ПЕРЕНОС В kip8: НЕ выполнен — Tasks 315–319 ожидают проверки пользователем в kip8test (после проверки — одной партией, kip8 v417→v418, ВКЛЮЧАЯ серверные scripts/*.gs от Task 318). Деплой пользователя: GitHub Pages + Ctrl+Shift+R ×1–2; Apps Script НЕ трогать.)
'''

assert lines[2].startswith('> **Версия документа:**')
lines.insert(2, new3 if new3.endswith('\n') else new3 + '\n')
print('Строка 3 вставлена (цепочка версий)')

src = ''.join(lines)

reps = [
    ('> **Текущая версия кэша:** `kipia-test-v557`',
     '> **Текущая версия кэша:** `kipia-test-v558`'),
    ('| `kip8test` | PWA | `kipia-test-v557` |',
     '| `kip8test` | PWA | `kipia-test-v558` |'),
]
for old, new in reps:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Версии кэша обновлены (2 замены)')

# Счётчики тестов
reps2 = [
    ('├── tests/                      # 1686 юнит-тестов (Node.js, 36 файлов): run-all.js, test-role-access.js, и др.',
     '├── tests/                      # 1708 юнит-тестов (Node.js, 37 файлов): run-all.js, test-role-access.js, и др.'),
    ('ожидается `1686 passed, 0 failed`', 'ожидается `1708 passed, 0 failed`'),
    ('# Ожидается: 1686 passed, 0 failed', '# Ожидается: 1708 passed, 0 failed'),
]
for old, new in reps2:
    n = src.count(old)
    assert n == 1, (old, n)
    src = src.replace(old, new)
print('Счётчики тестов обновлены (3 замены)')

# Буллет Task 319 — после буллета Task 318 (задачи по возрастанию)
entry = ('\n- **График работы — тултип над баром, перекрестье строка+столбец, тёмная шахматка светлыми цветами, название кода в окне мельче, окно мероприятий зрителям (Task 319)**: '
         'по заявке пользователя: «При наведении указателя мыши на кнопку "Обновить" информационное окошко появляется сверху и частично скрывается под самым верхним баром, поправь. При перемещении указателя мыши по ячейкам шахматки, должны выделяться в перекрестье строка и столбец которые пересекаются в ячейке на которой находится указатель. В тёмной теме цвет шахматки дней должен оставаться как в светлой теме, только немного притеняться поверхностным фильтром. В окне с выбором кодов в ячейках шахматки, текст названия кода сделай немного меньше, и если он не помещается в одну строку, переносится по строкам, а окно "Мероприятия в этот день" должно быть доступно для пользователей у которых есть доступ к "График работы — просмотр".». 1) ТУЛТИП «ОБНОВИТЬ» (Task 317 доработка): окно #wsRefreshTip ПЕРЕНЕСЕНО из #page-work-schedule в <body> (init: document.body.appendChild — .page-content {position:relative; z-index:2} создаёт stacking-контекст, который опускал z-index 9450 окна ПОД sticky .desktop-top-bar (z 70) — окно показывалось «над кнопкой» и физически пряталось ЗА баром); позиция в _showRefreshTip: прежде порог «top < 4 → под кнопкой», теперь С УЧЁТОТОМ ВЕРХНЕГО БАРА — querySelector(\'.desktop-top-bar\'), barBottom = rect.bottom (бар видим только ≥1024px; мобайл — barBottom 0, прежнее поведение); top = r.top − h − 8 < barBottom + 4 → top = r.bottom + 6 (окно ПОД кнопкой — сетка под баром всегда есть); try/catch + guard document.querySelector — мок-DOM тестов Task 317 без него работает по-прежнему (числа «152px над / 56px под» не изменились). 2) ПЕРЕКРЕСТЬЕ (новое): движение мыши по ячейкам дней (td.ws-cell) выделяет СТРОКУ и СТОЛБЕЦ, пересекающиеся в ячейке под курсором. ДЕЛЕГИРОВАНИЕ на контейнере #wsGridWrap (init): mouseover → e.target.closest(\'td.ws-cell\') → data-day + tr.sectionRowIndex → self._cellHover(ri, day); mouseout → relatedTarget НЕ ws-cell (шапка/ФИО/вне сетки) → _cellHover(null) — переходы МЕЖДУ ячейками НЕ трогают подсветку (обновит mouseover соседа — БЕЗ МИГАНИЯ), переход внутри ячейки на бейдж/текст — тоже не снимает (relatedTarget внутри td). _cellHover(ri, day): _rowHover(ri) + _dayHover(day) (столбец Task 316 переиспользуется: th ws-hover-col + td ws-hover); _cellHover(null) — оба сняты. _rowHover: класс ws-hover-row на <tr> (ставится/снимается ПРЯМО В DOM через _rowClass: wrap.querySelectorAll(\'tbody tr\')[ri].classList.toggle — без перерисовки; no-op если строка не менялась), _renderGrid ШТАМПУЕТ ws-hover-row из состояния _hoverRow (рядом с ws-group-first в trClsParts — объявление `_hoverRow: null` у _hoverDay; сброс в onMonthChange) — подсветка ПЕРЕЖИВАЕТ перерисовку (правка статуса/«Обновить»); индекс = sectionRowIndex = порядок рендера сотрудников. CSS: .ws-grid tbody tr.ws-hover-row td.ws-cell — inset-«заливка» rgba(74,143,199,0.10) (тёмная)/rgba(42,93,143,0.06) (светлая) — мягче столбца (0.16/0.10); ПЕРЕСЕЧЕНИЕ строки и столбца tr.ws-hover-row td.ws-cell.ws-hover — 0.24/0.16 (насыщеннее, как выбранный день); составные правила с ws-source-manual (рамка ручных д/н), ws-today (0.20/0.14), ws-sel (0.24/0.15 — выбор сильнее строки) — принцип Task 313/316 «рамка и смысловые тинты не затираются»; td.ws-emp-col строки — tint 0.10/0.06 («начало» перекрестья, шапка строки); специфичность tr-правил (0,3,3) ВЫШЕ столбцовых td-правил (0,3,2) — составные правила перекрывают корректно. ВСЕМ ролям (зритель тоже), мобильный — тач без hover не задействует. 3) ТЁМНАЯ ТЕМА — шахматка дней = ЦВЕТА СВЕТЛОЙ ТЕМЫ + ПОВЕРХНОСТНЫЙ ФИЛЬТР: [data-theme="dark"] .ws-grid tbody td.ws-cell { background: #eef0f2; color: #141413; filter: brightness(0.88); } (пустые ячейки — светлый фон светлой темы; текст кодов — --text-primary светлой темы; вся площадь ячейки СЛЕГКА ПРИТЕНЕНА фильтром — заявка «только немного притеняться поверхностным фильтром»; фильтр не ломает sticky: шапка z 2 и ФИО z 1 выше z-auto ячеек); [data-theme="dark"] …ws-weekend.ws-status-empty — #f7d9e3 (пыльно-розовый #6e4250 тёмной темы ЗАМЕНЁН на цвет светлой); …ws-status-empty — color rgba(20,20,19,0.65) («·»/пустые — вторичный тёмный); …ws-ev-badge.ws-ev-pending — border-color rgba(0,0,0,0.55), color #141413 (пунктирный бейдж будущего мероприятия читается на светлом); [data-theme="dark"] .ws-popup-swatch.ws-swatch-dot — #eef0f2 (свотч «.» в попапе совпадает с видом ячейки); СТАТУСНЫЕ ячейки — inline-цвета справочника (одинаковы в обеих темах всегда) + текст #141413 + фильтр; ШАПКА сетки и ФИО-КОЛОНКА остаются ТЁМНЫМИ (заявка — только «шахматка дней»); правило размещено ПОСЛЕ .ws-dot-code (равная специфичность (0,3,2) — позднее выигрывает: «·»-ячейка тоже светлая); БАЗОВЫЕ правила (var(--bg-primary), #6e4250, светлая тема) НЕ тронуты — история Tasks 250/254/255 жива. 4) ОКНО ВЫБОРА КОДОВ — НАЗВАНИЕ КОДА: #wsCellPopup .ws-popup-name { font-size: 12px (было 13 — «немного меньше»); line-height 1.35; white-space: normal; overflow-wrap: break-word; overflow: visible; text-overflow: clip; min-width: 0; } — длинные названия («Отпуск основной оплачиваемый…») видны ЦЕЛИКОМ, переносятся по строкам (высота строки растёт, ширина окна прежняя max-width 320px); ТОЛЬКО #wsCellPopup — окно «Мероприятия в этот день» (#wsEventsPopup) и карточка сотрудника сохраняют ПРЕЖНИЙ однострочный вид с ellipsis (база .ws-popup-name nowrap не менялась). 5) ОКНО «МЕРОПРИЯТИЯ В ЭТОТ ДЕНЬ» — ЗРИТЕЛЯМ («График работы — просмотр»): onCellClick: прежняя ветка !this._canEdit (тост «У вас нет прав на правку графика» + return) ЗАМЕНЕНА на _openEventsOnlyPopup(td, isoDate, tabNo) — НОВЫЙ метод: открывает ТОЛЬКО #wsEventsPopup (innerHTML = _renderEventsPopup — тот же рендер: строки-справка, кнопки ✎/✕ и так скрыты _canEdit, пустой день «нет мероприятий»), кловер #wsPopupCloser active (клик мимо / Esc закрывает — общий closeCellPopup чистит ОБА окна), позиция рядом с ячейкой по правилам _openCellPopup (правее → не влезает левее → клампы; сначала visibility hidden — замер габаритов); окно выбора кодов #wsCellPopup зрителю НЕ показывается (правка недоступна); _popupCell ставится (закрытие через общий путь). РЕДАКТОРУ — прежнее поведение (оба окна). Сервер WorkSchedule.gs/Code.gs/листы/Apps Script/кэш — НЕ менялись (чистый фронтенд). Верификация: сьют 1686→1708/0 (+22 tests/test-task319.js; актуализирован test-work-schedule [2 regex: trCls→trClsParts]; SW-бамп v557→v558 в sw.js + 19 тест-файлах + assertFalse «лишний инкремент» v558→v559 в test-task315/317/318), task319-browser-check.py 48/48 (порт 8937, мок fetch, 4 контекста: десктоп-Админ 1280 ТЁМНАЯ app-theme=dark [бар .desktop-top-bar sticky 56px z70; тултип: hover «Обновить» → показан, parentNode=BODY, z 9450 computed, top ПОД кнопкой ниже бара (112px при кнопке 76–106), pointer-events none, «данные от», уход курсора — hidden; перекрестье: hover td (017, день 5) — tr[0] ws-hover-row (прочие чисты), th[5] ws-hover-col + ВСЕ td[5] ws-hover, столбец 7 чист, ФИО-ячейка строки box-shadow tint, ПЕРЕСЕЧЕНИЕ rgba(74,143,199,0.24) vs строка 0.10; переход (017,5)→(017,7): строка ОСТАЛАСЬ (без мигания), столбец 5 снят/7 поставлен; переход на (023,5) (индекс 11 — сортировка сменные→дневные): прежняя строка снята, новая поставлена; уход на th дня 12: перекрестье снято, наведение th работает (Task 316); уход в тулбар: всё снято; правка статуса «Н» в окне кодов под курсором → _renderGrid → tr[0] ws-hover-row ШТАМП ЖИВ; тёмная шахматка: пустая #eef0f2 + computed filter brightness(0.88), текст кодов #141413, статусная (023 «Д8») #FFF9C4 + фильтр, пустая выходная #f7d9e3, шапка th.ws-day-col #0e1621 ТЁМНАЯ, ФИО-колонка тёмная, «·» rgba(20,20,19,0.65), пунктирный бейдж тёмный текст/рамка; окно кодов: ВСЕ названия 12px + white-space normal + break-word, ДЛИННОЕ название (Отпуск основной… 100 симв.) — 4 строки Range-rects, ширина окна ровно 320; окно «Мероприятия в этот день» (ячейка с мероприятием) — 13px nowrap (прежний вид); 0 JS-ошибок], светлая тема [пустая #eef0f2 БЕЗ фильтра, выходная #f7d9e3, шапка светлая rgba(240,240,240,0.95); перекрестье тон rgba(42,93,143,0.06)], зритель «ИТР8 pro» [клик по ячейке с мероприятием (день 10 «Целевой инструктаж») → #wsEventsPopup active: заголовок + тема, БЕЗ editTraining/deleteTraining, окно кодов НЕ active, кловер active; пустой день 17 → «нет мероприятий»; перекрестье hover работает], мобильный 375 touch [сетка есть, верхнего бара нет, шахматка светлая в тёмной теме]), регресс браузером: 298 34/34, 303 20/20, 304 10/10, 305 20/20, 306 29/29, 307 20/20, 308 20/20, 309 26/26, 310 20/20, 311 24/24, 312 31/31, 313 30/30 (актуализирован: проверка M — зритель теперь ПОЛУЧАЕТ окно мероприятий, окно кодов по-прежнему нет — заявка Task 319), 314 36/36, 315 33/33, 316 28/28, 317 49/49, 318 45/45; VLM ×2 (скрины: тултип+перекрестье+тёмная — «тултип обрезан» ОПРОВЕРГНУТО изолированным скрином элемента [все 4 строки, рамка, тень] и численно [scrollH=clientH=96, parent body, z 9450, pointer-events none → hit-test сквозь]; перекрестье подтверждено [строка+столбец, пересечение насыщеннее]; тёмная подтверждена [шахматка светлая слегка притенена, шапка/ФИО тёмные, текст читается]; popup-name подтверждён [длинное название 3 строки целиком]; зритель подтверждён [окно без ✎/✕]). SW kipia-test-v557→v558. ДЕПЛОЙ: только фронтенд — GitHub Pages + Ctrl+Shift+R ×1–2; Apps Script НЕ трогать — см. scripts/DEPLOY-Task319-tooltip-crosshair-dark-theme.md')
lines2 = src.split('\n')
insert_at = None
for i, l in enumerate(lines2):
    if l.startswith('- **График работы — увольнение сотрудника'):
        j = i + 1
        while j < len(lines2) and lines2[j].startswith('  '):
            j += 1
        insert_at = j
        break
assert insert_at is not None, 'точка вставки (после буллета Task 318) не найдена'
lines2.insert(insert_at, entry)
src = '\n'.join(lines2)
print('Буллет Task 319 вставлен (после Task 318)')

with open('Системный_промт_для_приложения_КИПиА.md', 'w', encoding='utf-8') as f:
    f.write(src)
print('Готово: post-Task 319')
