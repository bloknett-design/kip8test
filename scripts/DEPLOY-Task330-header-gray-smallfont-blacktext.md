# Task 330 — ШАПКА ШАХМАТКИ СИНЕ-СЕРАЯ, МЕЛКИЙ ШРИФТ ОПИСАНИЙ КОДОВ, ЧЁРНЫЙ ТЕКСТ ОКОН БАРА (СВЕТЛАЯ ТЕМА)

**Дата:** 2026-09-06
**Заявка пользователя:** «Фон шапки шахматки табеля с кнопкой "Сотрудник +" и днями недель
сделай темнее, ближе к сине-серому цвету. В окне выбора кодов ячеек, шрифт текста описания
кодов сделай маленьким. В светлой теме, весь текст в окнах мероприятий и норм времени,
в баре табеля, должен быть чёрным цветом.»

**Затронуто:** только `index.html` (клиент, CSS) + `sw.js` (версия кэша) + тесты.
**НЕ тронуто:** сервер/Apps Script/листы Google (эндпоинты и данные без изменений).

## Что сделано

1. **Фон шапки шахматки — ТЕМНЕЕ, СИНЕ-СЕРЫЙ** (строка с «Сотрудник +» и
   числами/днями недель — `.ws-grid thead th`):
   - тёмная тема: почти чёрная «морская» синева `var(--bg-tertiary,
     #0e1621)` → стальной сине-серый сланец **#1e293b** — плотная
     «крыша» над шахматкой, отчётливо отделена и от тулбара
     (#17212b), и от колонки ФИО тела (#0e1621);
   - светлая тема: прежде шапка была почти белой (240,240,240 — как
     колонка ФИО и пустые ячейки #eef0f2, сливалась с телом) →
     **#bfcad5** (светлый сине-серый, ОТДЕЛЬНОЕ правило — из
     группового правила 240,240,240 шапка исключена; селекты и
     колонка ФИО НЕ тронуты);
   - hover заголовка «Сотрудник +»: #15202f → **#2a3a4c** (светлее
     новой шапки — прежняя логика «hover светлее фона» сохранена);
     светлая тема hover #e2e8ef — без изменений (светлее #bfcad5);
   - градиенты подсветок столбцов (сегодня/hover/выбор — Tasks
     313/316) — background-image ПОВЕРХ нового фона, не затронуты.

2. **Окно выбора кодов ячеек — шрифт ОПИСАНИЙ маленький:**
   `#wsCellPopup .ws-popup-name` — font-size 12px → **10px**
   (line-height 1.35 → 1.3; перенос слов Task 319 сохранён).
   Сам код (13px bold) и заголовок окна (11px) — НЕ тронуты.
   Правило по-прежнему scoped под `#wsCellPopup`: окно «Мероприятия
   в этот день» и карточка сотрудника — прежний размер.

3. **Светлая тема — ВЕСЬ текст в окнах «Мероприятия» и «Норма /
   Праздники» бара табеля — ЧЁРНЫЙ (#000):**
   - окно мероприятий: заголовок-плашка `.ws-ep-cap` (прежде зелёный
     #1d7a37), строки `.ws-ep-item` (#666), даты `.ws-ep-date` и
     текст `.ws-ep-text` (#333), «нет мероприятий…»
     (`#wsEventsPanel .ws-cp-empty`, #999);
   - окно норм/праздников: столбики `.ws-cp-col` (#666), значения
     `.ws-cp-item b` (#333), «нет…»/легенда `.ws-cp-empty`/
     `.ws-cp-legend` (#999), плашки-заголовки `.ws-cp-norms >
     .ws-cp-cap` (#1d5f96) и `.ws-cp-days > .ws-cp-cap` (#b02c2c),
     бейдж «предварительно» `.ws-cal-prelim` (#a06b00 — перекрыто
     поздним правилом Task 330), чипы особых дней `.ws-cp-day`
     (k-hol #ff6b6b / k-reg / k-tr / k-sh / k-wk — правило после
     k-*, та же специфичность (0,2,0), побеждает позднее);
   - ЦВЕТНЫЕ ПОДЛОЖКИ сохранены (плашки/чипы/бейдж — смысловые
     «подложки», цвет остаётся только фоном);
   - тёмная тема окон — НЕ тронута (заявка только про светлую).

## Кэш

- `sw.js`: `kipia-test-v568` → `kipia-test-v569`.

## Тесты

- Сьют 1996 → **2010/0** (+14 `tests/test-task330.js`; актуализированы
  под Task 330: test-work-schedule [плашки светлой — чёрный текст],
  test-task313 [фон th #1e293b], test-task318 [hover #2a3a4c],
  test-task319 [.ws-popup-name 10px]; SW-бамп v568→v569 в 30 файлах,
  «лишний инкремент» → v570 в 12 файлах).

## Верификация

- `node tests/run-all.js` — 2010 passed, 0 failed.
- `scripts/task330-browser-check.py` — **27/27** (3 контекста):
  десктоп 1280 тёмная (шапка #1e293b в th дней и th «Сотрудник +»,
  тело ФИО — прежнее #0e1621, hover #2a3a4c; попап — описание 10px,
  код 13px/заголовок 11px не тронуты; 0 JS-ошибок), светлая (шапка
  #bfcad5, ФИО 240,240,240; мероприятия — заголовок/строки/даты/
  текст rgb(0,0,0) с зелёной подложкой; нормы — столбики/значения/
  плашки/легенда rgb(0,0,0); чипы праздника/сокращённого — чёрный
  текст, подложки цветные [мок legalic: праздник+перенос+
  сокращённый в текущем месяце]; попап 10px; 0 JS-ошибок), мобайл
  375 светлая (шапка #bfcad5, описания 10px; 0 JS-ошибок).
- Регресс: task327 30/30, task328 36/36, task329 43/43 — без
  отклонений (шторка/шеврон/бортик/дни — не задеты).
- VLM ×3 (тёмная/светлая/кроп попапа): шапка сине-серая и выделяется
  в обеих темах; текст окон бара чёрный; описание кода меньше кода,
  читаем; дефектов/наложений нет.

## Деплой

1. GitHub Pages (after push) — автоматически.
2. Пользователю: **Ctrl+Shift+R ×1–2** (SW kipia-test-v569).
3. Apps Script/листы — НЕ трогать (только клиент).

## Перенос в kip8

- НЕ выполнен — Tasks 315–330 одной партией (k8 `kipia-v417` →
  `v418`). В партии есть серверная часть (Code.gs/WorkSchedule.gs —
  Task 318 увольнение): в Apps Script уже развёрнуто (тот же бэкенд
  для обоих репо), в репозитории kip8 файлы .gs синхронизировать.
