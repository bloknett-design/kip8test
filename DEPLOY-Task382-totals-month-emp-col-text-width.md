# DEPLOY — Task 382 (kip8test)

## Заявка пользователя

1. «В мобильной версии, на странице итогов учёта, в таблице вкладки месяц
   изначальную ширину столбца с фамилиями сотрудников сделай по ширине
   текста в ячейках как на вкладке год.»

## Что изменилось (только клиент, index.html — серверной части НЕТ)

### Месячная таблица итогов — ИЗНАЧАЛЬНАЯ ширина колонки «Сотрудник» ПО ТЕКСТУ

Прежде: мобильная месячная таблица (fixed-раскладка) отводила колонке
фамилий ДОЛЮ 42% ширины таблицы — при коротких ФИО оставалось много
пустого места. Годовая таблица (авто-раскладка) всегда держала колонку
по тексту.

Теперь: базовое правило месячной таблицы

```css
.ws-tt-table:not(.ws-tt-year) th.ws-tt-emp,
.ws-tt-table:not(.ws-tt-year) td.ws-tt-emp {
    width: var(--ws-tt-emp-w, 42%);   /* Task 382: по тексту («как в году») */
    ...
}
```

— переменную `--ws-tt-emp-w` мерит новый JS-метод **`_measureTtEmpFullW`**
(приём Task 336 `_measureEmpNarrowW` / Task 325 `_measureEmpCol`):

- максимум по всем строкам **«№ таб. + 5px маржи + ФИО»** и заголовку
  «Сотрудник» (то же, что участвует в авто-раскладке годовой таблицы);
- + паддинги ячейки 8+8 + 2px запас (границы border-collapse + субпиксельность);
- спаны inline — их rect = ширина текста (эллипсис/ширина колонки не искажают);
- при АКТИВНОМ сужении (таблица прокручена): текст спанов временно
  возвращается к `data-full`, скрытый `display:none` № таб. показывается
  на время замера инлайн-стилем — один reflow — всё возвращается;
- результат на `body` (наследуют шторка И страница итогов); невидимый
  контейнер (rect = 0) пропускается; повторный замер с той же шириной
  не дёргает `setProperty`;
- **вызовы**: `_reapplyEmpNarrow` (после КАЖДОГО рендера итогов — его зовут
  последними и `_renderTotalsMonth`, и `_renderTotalsYearTable`) и
  `document.fonts.ready` (поздний шрифт меняет ширину текста).

Свойства сохранены:

| Свойство | Статус |
|---|---|
| `table-layout: fixed` месяца | НЕ тронут — столбцы данных делят остаток ПОРОВНУ |
| Фолбэк `42%` | ДО первого замера / VM-моки / скрытый контейнер |
| Эллипсис/nowrap/overflow:hidden | страховка субпиксельности осталась |
| Сужение при прокрутке (`.ws-narrow` → `var(--ws-tt-emp-nw, 48px)`) | late-правило ПОСЛЕ базового — НЕ тронуто |
| Плавный переход 0.35s cubic-bezier | НЕ тронут (теперь px→px) |
| Десктоп ≥1024px | колонка месяца по-прежнему `display:none` (замер пишется, CSS-эффекта нет) |
| Годовая таблица | авто-раскладка, переменную НЕ читает — эталон «по тексту» |

## Тесты

- `tests/test-task382.js` — 24 теста: CSS (переменная/фолбэк/эллипсис/порядок
  late-правила/fixed-авто-год/десктоп-скрытие/transition), SRC (замерщик:
  №+5+ФИО+16+2, data-full-обмен, inline-№таб., шторка⇄страница, вызовы),
  VM (5 сценариев на моках: happy/narrow-активный/max-двух/скрытые/пусто,
  дедуп setProperty), SW.
- Адаптации под новую ширину: `test-task325.js` / `test-task327.js` /
  `test-task329.js` — `width: 42%` → `width: var(--ws-tt-emp-w, 42%)`
  (доля осталась фолбэком).
- `scripts/task382-bump-sw.py`: SW `kipia-test-v610` → **`kipia-test-v611`**
  (guard v612), 70 тест-файлов.
- **run-all: 3288 passed / 0 failed** (было 3264).

## Браузерная проверка (scripts/task382-browser-check.py, порт 8992)

**33/33** — мобайл 375 (обе темы):

- M1 `--ws-tt-emp-w` установлена (210px);
- M2 ширина `th.ws-tt-emp` = переменной ±1.5px;
- M3 ширина = натуральный текст (№+5+ФИО) + 18 ±2.5px — «по ширине текста»;
- M4 ФИО НЕ обрезаны (0 клиппинга — правые края спанов внутри ячеек);
- M5 столбцы данных РАВНЫЕ ±1px (fixed жив);
- M6a вкладка «Год»: ширина её колонки ≈ месячной ±3px («как на вкладке
  год»); M6b/M6c возврат на «Месяц» — переменная и ширина восстановлены
  (замер после перерисовки);
- M7 сужение при прокрутке живо: `ws-narrow` → `--ws-tt-emp-nw`, № таб.
  скрыт; возврат scrollLeft=0 → полная ширина;
- M8 колонка УЖЕ прежних 42%×0.85 (210px против 296px при таблице 704px);
- M9 0 JS-ошибок;
- P1 пиксельный пруф: вертикальная стальная граница колонки ровно на
  `x = th.right` (rgb(64,80,102) на фоне панели 233,231,222 — светлая).

Десктоп 1280 (обе темы): D1 колонка месяца СКРЫТА; D2 переменная пишется;
D3 0 JS-ошибок.

## Service Worker

`kipia-test-v610` → `kipia-test-v611` (guard `kipia-test-v612`).

## Серверные шаги

НЕТ (чистый клиентский релиз).
