# Task 404 — три блока в одну линию (СИЗ слева от мероприятий); кнопки ✎/✕ рядом; окно мероприятий без отпусков

**Заявка**: «В картах работников блок СИЗ размести слева от блока
мероприятия. По итогу в верхней части должно быть три блока, слева на
право, блок профиля (под ним блок отпуска), блок СИЗ и блок
мероприятия, то есть нужно подогнать размеры блоков, что бы они
поместились в одной линии. И кнопки "редактировать" в блоках отпуска
и мероприятия выравнить разместить рядом с кнопками "удалить", по
примеру как в блоке СИЗ. В разделе Табель учёта рабочего времени, в
окне мероприятий убери информацию по отпускам».

**Репозиторий**: kip8test (тестовый сайт)
<https://bloknett-design.github.io/kip8test/>

**Версия SW**: kipia-test-v630 → **v631**
**Тесты**: 3817 passed / 0 failed (было 3805/0; +12 в test-task404.js,
адаптация test-task394.js / test-task399.js / test-task393.js /
test-task395.js / test-task403.js)
**Браузер-чек**: 50/50 ×3 контекста (тёмная view / светлая edit /
мобайл), 0 JS-ошибок; VLM ×3 (glm-5v-turbo) — подтверждено визуально.

## Что изменилось

### 1 — карточка: ТРИ блока в верхней части в одну линию

- `_renderWorkerCardPanels`: **три колонки** — 1-я: **профиль** (+отпуска
  под ним), 2-я (`.ws-wcol-ppe`): **СИЗ**, 3-я (новая `.ws-wcol-tr`):
  **мероприятия**. Блок СИЗ — СЛЕВА от блока мероприятий.
- Три верхних блока — на ОДНОЙ линии: колонки равные (`flex: 1 1 0`,
  flex-wrap: nowrap) — блоки гарантированно помещаются; колонки не
  тянутся по высоте друг друга (`align-items: flex-start`); зазор 12px.
- Мобайл ≤1023px — вертикальный стек колонок: профиль → отпуска →
  СИЗ → мероприятия.

### 2 — кнопки ✎/✕ рядом (как в блоке СИЗ)

- **Причина разнесённости**: у строк отпусков (`.ws-emp-v`) и
  мероприятий (`.ws-popup-name`) контент не был растянут, а у ОБЕИХ
  кнопок `.ws-popup-act` стоит `margin-left: auto` — свободное место
  делилось между auto-маржинами пополам, ✎ висел посередине строки. В
  СИЗ тело строки `.ws-ppe-body` имеет `flex: 1` — кнопки прижаты
  вправо РЯДОМ.
- **Фикс по образцу СИЗ**: `.ws-emp-field .ws-emp-v` и
  `.ws-popup-name` получили `flex: 1; min-width: 0` — свободного места
  нет, кнопки ✎/✕ встали рядом у правого края (эллипсис сохранён).
- Затронуло обе поверхности: страницу «Работники» И попап ячейки
  (мероприятия дня) — единообразно.

### 3 — окно мероприятий: информация по отпускам УДАЛЕНА

- `_renderMonthEventsPanel` (окно «Мероприятия» тулбара табеля):
  секция «Отпуска» удалена целиком — сборка vacList, рендер плашки и
  строк, точка секции. Остались «Мероприятия» и «СИЗ».
- CSS: `.ws-ep-cap-vac` / `.ws-ep-dot-vac` удалены (классов-жителей
  нет); светлая тема — только `.ws-ep-cap-ppe`.
- Данные отпусков живы: карточки (блок «Отпуска · год»), шахматка
  (план «ОТ»), шторки правки.

## Проверки

- `tests/test-task404.js` (+12): SRC (три колонки colMain/colPpe/colTr;
  медиаправила — равные, nowrap, flex-start; мобайл-стек; .ws-emp-v и
  .ws-popup-name flex:1; образец .ws-ppe-body; окно — без vacList/
  «Отпуска ·»/ws-ep-dot-vac, СИЗ жив; CSS-плашки) + VM (панели 2|1|1,
  порядок DOM профиль→отпуска→СИЗ→мероприятия; кнопки ✎/✕ во всех
  трёх блоках, у зрителя их нет; окно — отпуск в данных не
  показывается, мероприятия/СИЗ живы, только отпуска — окно пустое)
  + SW (v631).
- Адаптация: test-task394.js (плашки/сборка/VM-отпуска/порядок/
  прошедшие — теперь только СИЗ; DOM-порядок панели), test-task399.js
  (фильтр мастеров — двухсекционный; счётчики без отпусков),
  test-task393.js (порядок панелей), test-task395.js (три колонки),
  test-task403.js (SRC/VM панели — новая раскладка).
- `scripts/task404-browser-check.py` 50/50: тёмная view — окно
  мероприятий: секции «Мероприятия · сентябрь · 2» и «СИЗ · сентябрь ·
  1» (Каска), НЕТ «Отпуска ·»/«Отпуск ·»/ws-ep-dot-vac при живом
  отпуске 20–26 в данных; регресс ячейки «5 р.»/без группы; попап без
  СИЗ; светлая edit — ТРИ колонки (профиль+отпуска | СИЗ |
  мероприятия), верхние блоки на одной высоте (допуск 2px), СИЗ левее
  мероприятий, колонки равные; кнопки ✎/✕ отпусков/мероприятий/СИЗ —
  рядом (зазор ≤ 12px, прижаты к правому краю ≤ 22px); правка
  комментария → updateEmployee (регресс 403); мобайл — стек
  профиль → отпуска → СИЗ → мероприятия. 0 JS-ошибок ×3.
- VLM ×3 (glm-5v-turbo): страница «Работники» — три колонки (Иванов |
  СИЗ | Мероприятия) в одну линию, пары ✎/✕ у правого края; окно
  мероприятий (element-скрин) — только «Мероприятия · 2» и «СИЗ · 1»,
  секции «Отпуска» нет; тёмная сетка — дефектов нет.

## Серверные шаги

1. Нет (WorkSchedule.gs не менялся; GAS-фикс комментария Task 403
   уже в Apps Script).
